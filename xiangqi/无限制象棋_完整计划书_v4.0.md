# 无限制象棋 - 完整项目计划书

## Unrestricted Chinese Chess - Complete Project Plan

**版本**: v4.1（机制引擎 + AI性格系统版）
**日期**: 2026-07-15
**目标读者**: AI Agent / 开发团队

---

## 目录

1. [项目概述与核心理念](#1-项目概述与核心理念)
2. [系统架构总览](#2-系统架构总览)
3. [数据结构设计](#3-数据结构设计)
4. [AI系统详细设计](#4-ai系统详细设计)
5. [前端技术实现](#5-前端技术实现)
6. [后端API设计](#6-后端api设计)
7. [规则引擎与对弈AI](#7-规则引擎与对弈ai)
8. [HTML区段替换系统](#8-html区段替换系统)
9. [Token消耗统计](#9-token消耗统计)
10. [测试体系](#10-测试体系)
11. [当前存在的问题与不足](#11-当前存在的问题与不足)
12. [开发路线图与展望](#12-开发路线图与展望)

---

## 1. 项目概述与核心理念

### 1.1 项目愿景

**无限制象棋**是一款突破传统规则束缚的创新型中国象棋游戏。核心创新点在于：

> **玩家可以通过自然语言指令，实时修改游戏规则、棋盘状态、棋子能力，甚至游戏界面本身。**

这不是一款普通的象棋游戏，而是一个**规则可塑的、活的**游戏系统。

### 1.2 核心玩法

| 阶段 | 描述 |
|-|-|
| **正常对弈** | 玩家与AI进行标准中国象棋对局 |
| **输入指令** | 玩家随时在输入框输入自然语言指令（如"让我的马可以斜着走"、"把車变成两个"、"我要悔三步棋"） |
| **AI解析** | 系统通过多级AI协作，解析玩家意图并分类 |
| **代码生成** | 第二级AI根据分类生成JSON Patch或HTML修改 |
| **校验执行** | Schema校验 + 规则验证，通过后应用到游戏 |
| **实时生效** | 修改即时应用到游戏中，无需重启 |

### 1.3 技术栈

| 层级 | 技术 |
|-|-|
| 前端层 | HTML5 + CSS3 + 原生 JavaScript (ES2022+)，移动端优先响应式设计 |
| 后端层 | Python 3.11+ / FastAPI / Uvicorn，HTTP API 通信 |
| AI层 | DeepSeek API (deepseek-v4-pro / deepseek-v4-flash 模型)，二级AI协作（意图解析→代码生成） |
| 规则引擎 | jump/ray 双原子移动体系 + where 条件表达式引擎 |
| 存储层 | 内存状态 + JSON配置文件持久化 + 撤销栈 |
| 校验层 | JSON Schema (Draft7) + 规则变化检测 |

### 1.4 设计哲学

- **🌟 灵活编码（最高纲领）**：项目以"用AI现场生成代码实现大部分要求"为核心创新形式。除已完成的硬编码部分外，所有新功能皆不可以用硬编码实现，必须用AI编码实现（如棋盘旋转、自定义棋子等）。为此可以牺牲稳定性、token花销、速度等，因为灵活编码是项目设立的初衷。
- **JSON驱动架构**：所有可变元素（规则、棋盘、棋子）都抽象为JSON数据，游戏引擎从JSON读取配置，AI只需修改JSON即可改变游戏行为；配置在进程内缓存、每类只读一次，仅在 AI 修改成功应用、重置/重开/关卡等显式规则变更后才会重新读盘，走棋 / valid_moves / AI 计算不重复读盘。
- **jump/ray 原子体系**：将所有棋子的移动规则解构为 jump（离散跳跃）和 ray（射线滑行）两种底层原子，配合 where 条件表达式，使 AI 能够修改规则概念本身。
- **JSON Patch优先**：AI输出RFC 6902标准的JSON Patch操作数组，而非全量JSON，减少token消耗和出错概率。
- **最小改动原则**：AI修改时只改动最必要的部分，通过区段标记系统实现HTML精准替换。
- **多级校验**：Schema校验 → 规则验证 → 范围检查，多重保障修改质量。
- **可撤销**：所有AI修改都进入撤销栈，玩家可随时撤回。

---

## 2. 系统架构总览

### 2.1 二级AI协作流水线

```
玩家输入自然语言指令
         │
         ▼
┌─────────────────────────────────────┐
│  第一级：意图解析AI (ChatAI)         │
│  模型: deepseek-v4-flash, temp=0.3   │
│  职责: 理解意图 → 分类 → 生成指令    │
│  输出: 结构化JSON (classification,  │
│        structured_instruction, ...) │
└──────────────┬──────────────────────┘
               │
    ┌──────────┼──────────┬──────────┬──────────┐
    │          │          │          │          │
    ▼          ▼          ▼          ▼          ▼
  A类        B类        C类        D类        E类
机制修改    棋盘变换    规则修改    界面修改    搞笑
(A1硬代码
+A2 CodeAI)(CodeAI)   (CodeAI)   (CodeAI)   (硬代码)
               │          │          │
               │          │     ┌────┴────┐
               │          │     │         │
               │          │    D1子类    D2子类
               │          │  配置修改  HTML结构
               │          │  (board/   (index.html)
               │          │   ui_config)
               │          │          │
    ┌──────────┴──────────┴────┬────┴────────┐
    │                          │             │
    ▼                          ▼             │
┌─────────────────────┐  ┌──────────────┐   │
│  第二级：代码生成AI  │  │  前端D2处理   │   │
│  (CodeAI)           │  │  (区段替换)   │   │
│  模型: deepseek-v4-pro │  └──────────────┘   │
│  输出: JSON Patch    │                     │
└──────────┬──────────┘                     │
           │                                │
           ▼                                │
┌─────────────────────┐                     │
│  Schema 校验        │                     │
│  (jsonschema D7)    │                     │
└──────────┬──────────┘                     │
           │                                │
           ▼                                │
┌─────────────────────┐                     │
│  规则/范围验证       │                     │
│  (坐标/类型/重叠)    │                     │
└──────────┬──────────┘                     │
           │                                │
           ▼                                │
    ┌──────┴──────┐                         │
    │             │                         │
 校验通过      校验失败                      │
    │             │                         │
    ▼             ▼                         │
 应用修改      自动重试(最多2次)             │
 保存配置      提升temperature              │
 刷新前端      重新生成                     │
    │                                        │
    └────────────────────────────────────────┘
```

### 2.2 分类体系详解

| 分类 | 名称 | 处理方式 | 修改目标 | 说明 |
|-|-|-|-|-|
| A | 机制修改 | A1硬代码 + A2 CodeAI | board_state.json + rules.json | A1: 悔棋/输赢（硬编码）；A2: 游戏机制+AI性格（灵活编码，原子原语组合） |
| B | 棋盘变换 | CodeAI | board_state.json | 添加/删除/移动棋子、修改棋子属性 |
| C | 规则修改 | CodeAI | pieces.json | 修改棋子移动方式、添加特殊能力、阵营专属规则 |
| C+ | 自定义棋子 | CodeAI | pieces.json + board_state.json | 创建全新棋子类型，AI生成规则和棋盘实例 |
| B+C | 棋盘+规则 | CodeAI×2 | board_state + pieces.json | 先B后C，B的修改结果传入C |
| D | 界面修改 | CodeAI | board.json / ui_config.json / index.html | D1子类改配置，D2子类改HTML结构 |
| E | 搞笑 | 硬代码 | 无 | 幽默回复，不修改任何配置 |
| F | 高级功能 | 不可行 | - | 需要修改核心引擎代码的请求，标记不可行 |

**A类子分类详解：**
| 子类 | 名称 | 处理方式 | 修改目标 | 说明 |
|-----|------|---------|---------|------|
| A1 | 硬编码操作 | 硬代码 | board_state.json | 悔棋（undo_move）、设置赢家（set_winner） |
| A2 | 机制修改 | CodeAI | board_state.json + rules.json | 游戏机制原语 + AI性格系统，原子化组合实现 |

### 2.3 D1与D2子类判断

- **D1 (配置文件修改)**：`structured_instruction` 中**不含** `target_sections` 字段 → 修改 `board.json`（棋盘外观）或 `ui_config.json`（全局主题）
- **D2 (HTML结构修改)**：`structured_instruction` 中**含** `target_sections` 字段 → 通过区段替换修改 `index.html`（增删改DOM元素）

---

## 3. 数据结构设计

### 3.1 board.json — 棋盘几何与渲染

```
board
├── _metadata: {version, description}
├── geometry:                 # 几何定义
│   ├── width: 9              # 列数
│   ├── height: 10            # 行数
│   ├── river_line: 5         # 楚河汉界所在行
│   ├── palace:               # 九宫格范围
│   │   ├── red: {top_left: [x,y], bottom_right: [x,y]}
│   │   └── black: {top_left: [x,y], bottom_right: [x,y]}
│   └── regions:              # 自定义区域
│       ├── red_side: {y_range: [5,9]}
│       ├── black_side: {y_range: [0,4]}
│       └── river: {y_range: [4,5]}
└── appearance:               # 视觉渲染配置
    ├── background_color      # 背景色
    ├── line_color            # 网格线颜色
    ├── palace_line_color     # 九宫线颜色
    ├── grid: {               # 网格线配置
    │   line_thickness,
    │   show_horizontal,
    │   show_vertical,
    │   river_gap
    │ }
    ├── palace: {             # 九宫格配置
    │   enabled,
    │   show_diagonals,
    │   line_thickness
    │ }
    ├── river: {              # 楚河汉界配置
    │   enabled, text, text_size, gap_ratio
    │ }
    ├── layout: {             # 布局配置
    │   viewbox_padding_left/right/top/bottom,
    │   board_size
    │ }
    └── decorations: {        # 装饰元素
        border: {enabled, thickness, color},
        custom_lines: [],
        background_pattern
    }
```

**说明**：board.json 是 v2.0 架构新增的核心配置文件，合并了旧版 board_state.json 的 board 字段（几何定义）和 board_layout.json（视觉渲染）。

### 3.2 pieces.json — 棋子移动原语（核心创新）

采用 **jump + ray** 两种原子移动原语定义所有棋子的移动规则。这是 v4.0 架构的核心创新。

```
pieces
├── _metadata: {version, description}
├── pieces:                   # 预定义棋子（7种标准棋子）
│   ├── general: {
│   │   label: {red: "帥", black: "將"},
│   │   is_king: true,
│   │   moves: [
│   │     {
│   │       kind: "jump",         // 移动原子类型
│   │       to: [0, 1],           // jump: 目标偏移
│   │       block: [],            // jump: 关卡格（必须为空）
│   │       land: "any",          // 落点要求: empty/enemy/any
│   │       sym: "rotate4",       // 对称展开: none/rotate4/rotate4_mirror/mirror_x
│   │       where: [              // 条件表达式（额外约束）
│   │         {in_region: "$dest", region: "$palace"}
│   │       ]
│   │     }
│   │   ]
│   │ }
│   ├── advisor: {...}
│   ├── elephant: {
│   │   moves: [{
│   │     kind: "jump",
│   │     to: [2, 2],
│   │     block: [[1, 1]],       // 象眼阻挡
│   │     sym: "rotate4",
│   │     where: [
│   │       {not: {crossed_river: "$dest", by: "$side"}}
│   │     ]
│   │   }]
│   │ }
│   ├── horse: {
│   │   moves: [{
│   │     kind: "jump",
│   │     to: [1, 2],
│   │     block: [[0, 1]],       // 马腿阻挡
│   │     sym: "rotate4_mirror"
│   │   }]
│   │ }
│   ├── chariot: {
│   │   moves: [{
│   │     kind: "ray",            // 射线滑行
│   │     dir: [1, 0],           // ray: 方向向量
│   │     max: -1,               // ray: 最大步数（-1=无限）
│   │     screens: 0,            // ray: 需跳过的棋子数（炮架）
│   │     land: "any",
│   │     sym: "rotate4"
│   │   }]
│   │ }
│   ├── cannon: {
│   │   moves: [
│   │     {kind: "ray", dir: [1,0], max: -1, screens: 0, land: "empty", sym: "rotate4"},  // 移动（空落点）
│   │     {kind: "ray", dir: [1,0], max: -1, screens: 1, land: "enemy", sym: "rotate4"}   // 吃子（隔1子）
│   │   ]
│   │ }
│   └── soldier: {
│       moves: [
│         {kind: "jump", to: "$forward", block: [], land: "any", sym: "none"}, // 前进
│         {                                                                   // 过河后横走
│           kind: "jump", to: [1,0], block: [], land: "any", sym: "mirror_x",
│           where: [{crossed_river: "$self", by: "$side"}]
│         }
│       ]
│     }
├── side_overrides: {           # 阵营专属覆盖规则
│   red: {},                    // key为棋子type, value为覆盖字段
│   black: {}
│ }
└── custom_pieces: []            # 自定义棋子类型数组
```

#### 移动原子详解

**jump（离散跳跃）**：
- `to`: `[dx, dy]` 相对偏移，或 `$forward`（向前，根据阵营自动确定方向）
- `block`: `[[dx, dy], ...]` 关卡格列表——这些格子必须为空才能跳过去（马腿、象眼）
- `land`: `"empty"`(只能落在空格) | `"enemy"`(只能吃敌方) | `"any"`(任意)

**ray（射线滑行）**：
- `dir`: `[dx, dy]` 方向向量（单位向量）
- `max`: 最大步数（`-1` = 无限）
- `screens`: 需要跳过的棋子数（炮架机制，0=不能跳子，1=隔1子吃，2=隔2子吃）
- `land`: 落点要求

**通用字段**：
- `sym`: 对称展开方式
  - `none`: 不展开，原样使用
  - `rotate4`: 4向旋转（上下左右）
  - `rotate4_mirror`: 4向旋转+镜像（8方向）
  - `mirror_x`: 水平镜像（左右对称）
- `where`: 条件表达式列表（额外约束，详见 3.2.1 节）

#### where 条件表达式

条件表达式用于约束移动的额外条件，支持逻辑组合：

| 条件原语 | 语法 | 含义 | 示例 |
|-|-|-|-|
| `in_region` | `{"in_region": "$dest", "region": "$palace"}` | 目标在指定区域内 | 将/士只能在九宫 |
| `crossed_river` | `{"crossed_river": "$self", "by": "$side"}` | 是否已过河 | 象不能过河 |
| `not` | `{"not": {...}}` | 否定条件 | 象的过河约束取反 |
| `and` | `{"and": [{...}, {...}]}` | 与条件 | 多个条件同时满足 |
| `or` | `{"or": [{...}, {...}]}` | 或条件 | 任一条件满足 |
| `same_side` | `{"same_side": "$dest"}` | 目标在己方区域 | |
| `has_property` | `{"has_property": "$self", "key": "flying"}` | 棋子有某属性 | 自定义棋子能力 |

**变量引用**：
- `$self`: 当前棋子位置
- `$dest`: 目标位置
- `$side`: 当前棋子阵营（red/black）
- `$opponent`: 对方阵营
- `$palace`: 己方九宫格区域
- `$forward`: 前进方向（红方[0,-1], 黑方[0,1]）

**side_overrides 合并机制**：引擎深拷贝默认棋子规则，然后用阵营覆盖字段递归合并（dict递归合并，list/标量整体替换）。只需包含需要覆盖的字段，其他自动继承。

### 3.3 rules.json — 游戏规则

```
rules
├── _metadata: {version, description}
├── win_conditions:           # 胜利条件
│   ├── checkmate: {enabled, description}
│   ├── stalemate: {enabled, description, result}
│   └── general_captured: {enabled, description}
├── special_rules:            # 特殊规则
│   └── flying_general: {enabled, description}
├── turn_rules: {             # 回合规则
│   turn_order: "alternating",
│   first_player: "red"
│ }
└── ai_difficulty:            # AI难度
    ├── current: "medium"
    └── levels: {
        easy: {depth: 2, randomness: 0.3},
        medium: {depth: 3, randomness: 0.1},
        hard: {depth: 4, randomness: 0.0}
    }
```

### 3.4 board_state.json — 运行时状态

```
board_state
├── _metadata: {version, last_modified, format}
├── pieces: [                 # 棋子实例数组
│   {
│     id: "r_chariot_1",      # 唯一ID: {r|b}_{type}_{num}
│     type: "chariot",        # 棋子类型(英文标识符)
│     name: "車",             # 显示名称
│     side: "red",            # 阵营: red|black
│     position: [0, 9],       # [x, y]坐标
│     is_alive: true,         # 是否存活
│     custom_properties: {}   # 自定义属性
│   }
│   ...
│ ]
├── current_turn: "red"       # 当前回合
├── move_history: [...]       # 移动历史（用于悔棋）
└── game_status:              # 游戏状态
    ├── state: "playing"      # playing|ended
    ├── winner: null          # 获胜方
    ├── win_condition: null   # 获胜条件
    └── custom_rules_active: [] # 已激活的自定义规则列表
```

**坐标系统**：`[x, y]` 格式，x: 0~width-1（左到右），y: 0~height-1（上到下）。黑方在上(y小)，红方在下(y大)。

### 3.5 ui_config.json — 界面配置

```
ui_config
├── _metadata: {version}
├── theme:
│   ├── board: {background_color, line_color, river_text}
│   ├── pieces: {red_color, black_color, red_bg, black_bg, font_family}
│   └── highlight: {selected, valid_move, last_move}
├── layout: {board_size, input_position, response_area}
├── custom_css: ""            # 自定义CSS字符串
└── custom_js: ""             # 自定义JS代码
```

### 3.6 token_stats.json — Token消耗统计

```
token_stats
├── total_prompt_tokens: 0    # 总提示词token
├── total_completion_tokens: 0 # 总回复token
├── total_calls: 0            # 总API调用次数
├── daily_stats:              # 每日统计
│   └── "2026-07-13": {prompt_tokens, completion_tokens, calls}
└── estimated_cost_usd: 0     # 估算费用（美元）
```

---

## 4. AI系统详细设计

### 4.1 第一级：意图解析AI (Intent Parser)

**模型**: deepseek-v4-flash
**温度**: 0.3
**系统提示词**: `INTENT_PARSER_SYSTEM` (定义于 prompts.py)

**输入**: 玩家自然语言指令 + 上下文
**输出**: 结构化JSON

```json
{
  "classification": "A|B|C|C+|D|E|F|B+C",
  "feasible": true,
  "confidence": 0.95,
  "reasoning": "判断理由",
  "target_files": ["board_state.json"],
  "structured_instruction": {
    "action": "add_piece",
    "target": "pieces",
    "parameters": {"type": "chariot", "side": "red", "position": [4, 4]},
    "constraints": []
  },
  "response_to_player": "给玩家的回复",
  "next_ai_prompt": "传递给第二级AI的完整提示词"
}
```

### 4.2 第二级：代码生成AI (Code Generator)

**模型**: deepseek-v4-pro（开启思考模式）
**温度**: 0.1~0.3（分类不同，温度不同）
**输出格式优先**: RFC 6902 JSON Patch 数组

**分类专家提示词**（定义于 prompts.py）：
- `BOARD_TRANSFORMER_SYSTEM`: B类棋盘变换AI
- `RULE_MODIFIER_SYSTEM`: C类规则修改AI
- `PIECE_CREATOR_SYSTEM`: C+类自定义棋子AI
- `UI_MODIFIER_SYSTEM`: D类界面修改AI

### 4.3 JSON Patch 优先策略

AI 优先输出 JSON Patch（RFC 6902）而非全量JSON，原因：
1. **Token效率高**：只描述变化，不重复未修改内容
2. **精确度高**：Patch操作语义明确，减少AI误改其他字段
3. **便于审计**：操作记录清晰，便于前端展示修改历史
4. **回滚方便**：反向Patch即可撤销

**Patch操作类型**：
- `add`: 添加字段/数组元素
- `remove`: 删除字段/数组元素
- `replace`: 替换字段值
- `move`: 移动字段
- `copy`: 复制字段

### 4.4 校验与重试机制

> **设计决策**：项目不使用独立的"校验AI"。理由：(1) 校验AI采用fail-open策略，
> 实际无法有效拦截错误输出；(2) 玩家可通过撤销机制自行处理AI的错误输出；
> (3) 硬编码检查（Schema + 业务规则）足以防止运行时错误。校验完全依赖硬编码实现，
> 仅在输出解析失败或Schema/业务校验失败时触发重试。

```
AI输出
  │
  ▼
JSON格式解析 ──失败──► 重试 (温度+0.1)
  │
  ▼ 成功
JSON Schema校验 ──失败──► 重试 (温度+0.1)
  │
  ▼ 成功
业务规则验证 ──失败──► 重试 (温度+0.1)
                    最多重试2次，温度上限0.3
  ▼ 成功
应用修改
```

**Schema校验**：使用 Draft7 JSON Schema，确保数据结构正确
**业务验证**：
- 坐标范围检查
- 棋子类型合法性
- 位置重叠检查
- 自定义棋子 type 冲突检查
- move.kind 合法性检查（jump/ray）

---

## 5. 前端技术实现

### 5.1 技术栈

- HTML5 + CSS3 + 原生 JavaScript (ES2022+)
- SVG 渲染棋盘网格
- DOM 渲染棋子（div + 文字）
- 响应式设计，移动端优先
- 无第三方框架依赖

### 5.2 核心模块

| 模块 | 功能 |
|-|-|
| `loadConfigs()` | 加载所有配置文件 |
| `renderBoard()` | 渲染棋盘网格（SVG） |
| `renderPieces()` | 渲染所有棋子（DOM） |
| `get_valid_moves()` | 获取棋子合法移动（调用后端API） |
| `showValidMoves()` | 显示合法移动指示器 |
| `executeMove()` | 执行玩家走棋 |
| `sendCommand()` | 发送自然语言指令 |
| `updateActiveRules()` | 更新自定义规则显示 |
| `_getBoardLayoutConfig()` | 获取棋盘布局配置（从 board.appearance） |

### 5.3 棋盘渲染

使用 SVG 渲染网格线，DOM 渲染棋子。坐标系统：
- 棋盘交叉点坐标：`[0, 0]` 到 `[width-1, height-1]`
- 像素位置通过百分比计算，确保响应式
- SVG viewBox 包含边距，棋子通过 `left/top: 5%-90%` 定位

---

## 6. 后端API设计

### 6.1 核心API

| 方法 | 路径 | 功能 |
|-|-|-|
| GET | `/` | 主页面 |
| GET | `/api/config/all` | 获取所有配置 |
| GET | `/api/config/{name}` | 获取单个配置 |
| POST | `/api/command` | 发送自然语言指令 |
| POST | `/api/move` | 玩家走棋 |
| POST | `/api/ai_move` | AI走棋 |
| POST | `/api/undo` | 悔棋 |
| POST | `/api/restart` | 重新开始 |
| POST | `/api/difficulty` | 设置AI难度 |
| POST | `/api/apikey` | 设置API Key |
| GET | `/api/apikey/status` | 获取API Key状态 |
| GET | `/api/token_stats` | 获取Token统计 |
| POST | `/api/undo_ai` | 撤回AI修改 |

### 6.2 /api/command 响应格式

```json
{
  "success": true,
  "type": "applied",
  "message": "修改成功",
  "classification": "C",
  "modified_configs": {
    "pieces": {...}
  },
  "refresh_page": false
}
```

`type` 取值：
- `"applied"`: 修改成功已应用
- `"fun"`: 搞笑回复（E类）
- `"rejected"`: 被拒绝
- `"error"`: 错误

---

## 7. 规则引擎与对弈AI

### 7.1 规则引擎 (RuleEngine)

**文件**: `rule_engine.py`

**核心方法**:
- `get_valid_moves(piece, board_state)`: 获取棋子所有合法移动
- `is_in_check(side, board_state)`: 检查是否被将军
- `is_checkmate(side, board_state)`: 检查是否被将死
- `is_general_captured(side, board_state)`: 检查将帅是否被吃

**移动计算流程**:
```
pieces.json 的 moves[]
    ↓ 展开对称（sym）
每一条 move 定义
    ↓ 解析 where 条件
    ↓ 执行 jump/ray 原子
    ↓ 过滤条件不满足的位置
    ↓ 合并所有合法位置
最终合法移动列表
```

**_king_types 集合**: 初始化时构建，包含标准 `general` + 所有标记为 `is_king: true` 的自定义棋子。将军/将死/吃帅判断统一使用该集合，支持自定义国王棋子。

### 7.2 对弈AI (ChessAI)

**文件**: `chess_ai.py`

**算法**: Minimax + Alpha-Beta 剪枝
**评估函数**: 棋子价值 + 位置奖励 + 中心控制奖励

**难度等级**:
- easy: depth=2, randomness=0.3
- medium: depth=3, randomness=0.1
- hard: depth=4, randomness=0.0

**自定义棋子价值评估**:
- 遇到未知棋子类型时，调用 AI 动态评估价值
- 评估结果缓存，避免重复调用
- 通过 `token_stats_callback` 回调记录 token 消耗

### 7.3 机制引擎 (MechanismEngine)

**文件**: `mechanism_engine.py`

**设计理念**: 机制原语硬编码实现，AI通过修改JSON配置来组合使用这些原语（类比规则引擎的jump/ray原子体系），遵循"灵活编码"原则。

**5种原子化机制原语**：

| 原语 | 说明 | 数据路径 | 效果 |
|-----|------|---------|------|
| `skip_turns` | 跳过回合（冻结） | `/mechanisms/skip_turns/-` | 指定方跳过N回合，无法走棋 |
| `ai_control` | AI接管 | `/mechanisms/ai_control/-` | 指定方接下来N回合由AI代为走棋 |
| `random_moves` | 随机走棋 | `/mechanisms/random_moves/-` | 指定方接下来N步棋随机选择合法走法 |
| `extra_turns` | 额外回合 | `/mechanisms/extra_turns/-` | 指定方获得N次额外回合（连续走棋） |
| `move_limits` | 每回合步数限制 | `/mechanisms/move_limits/-` | 指定方每回合可以走N步 |

**核心方法**:
- `apply_pre_turn_mechanisms(board_state, side)`: 回合开始前应用机制（跳过回合检查、AI接管检查）
- `apply_post_move_mechanisms(board_state, side)`: 走棋后应用机制（消耗随机走棋、额外回合判定等）
- `get_active_mechanisms_summary(board_state)`: 获取当前激活的机制列表摘要
- `apply_personality_to_ai(chess_ai)`: 将性格配置应用到ChessAI实例

**机制组合能力**: AI可以自由组合多个原语实现复杂效果，例如：
- 冻结AI 2回合 = skip_turns(black, 2)
- 我的下两步棋由AI接管 = ai_control(red, 2)
- 我这步棋随便下 = random_moves(red, 1)
- 让AI变得狂暴 = aggressive性格 + random_moves各取所需

### 7.4 AI性格系统

**配置路径**: `rules.json → ai_difficulty.personality`

**预设性格类型**:

| 类型 | 说明 | 特点 |
|------|------|------|
| `normal` | 标准型 | 攻守平衡，标准AI |
| `aggressive` | 激进型 | 高进攻倾向，重视中心控制和过河兵 |
| `defensive` | 保守型 | 高防守倾向，重视将帅安全和棋子保护 |
| `random` | 随机型 | 低搜索深度，高随机度 |
| `custom` | 自定义型 | 自由调整所有参数 |

**性格参数**:
- `type`: 预设性格类型
- `aggressiveness`: 进攻倾向 0.0-1.0（影响位置评估权重）
- `conservatism`: 保守程度 0.0-1.0（影响防守评估权重）
- `randomness_override`: 覆盖默认随机度（null或0.0-1.0）
- `depth_override`: 覆盖默认搜索深度（null或正整数）
- `value_biases`: 棋子价值偏差 `{piece_type: bias_multiplier}`
- `custom_prompt`: 自定义提示词（字符串或null，预留扩展）

**评估函数中的性格影响**:
- 进攻倾向越高，过河兵、中心控制的权重越高
- 保守程度越高，将帅附近棋子的防守加分越高
- 棋子价值偏差可调整各棋子的重要性

---

## 8. HTML区段替换系统

### 8.1 设计理念

D2类修改通过**区段替换**方式修改 index.html，而非全量重写。优点：
1. **精准**：只改需要改的部分
2. **安全**：不会意外破坏其他区域
3. **高效**：AI只需输出目标区段内容，token消耗少

### 8.2 区段标记

在 index.html 中使用特殊注释标记可修改区段：
```html
<!-- SECTION: header -->
<header>...</header>
<!-- END_SECTION: header -->
```

### 8.3 可用区段

- `thinking_overlay`: AI思考遮罩
- `settings_modal`: 设置弹窗
- `logs_modal`: 日志弹窗
- `header`: 页头
- `board_section`: 棋盘区域
- `side_panel`: 侧边栏
- `input_section`: 输入区域

---

## 9. Token消耗统计

### 9.1 统计范围

- 意图解析AI调用
- 代码生成AI调用（所有分类）
- 自定义棋子价值评估AI调用
- 重试调用

### 9.2 持久化

- 存储在 `token_stats.json`（运行时生成，不在初始配置中）
- 每次API调用后异步更新
- 每日统计按日期分组
- 加载时深合并 daily_stats，避免内存统计被文件旧值覆盖

---

## 10. 测试体系

### 10.1 测试文件

| 文件 | 功能 |
|-|-|
| `test_json_patch.py` | JSON Patch 工具单元测试 |
| `tests/test_custom_pieces.py` | 自定义棋子功能测试 |
| `tests/test_custom_pieces_integration.py` | 自定义棋子集成测试 |
| `run_integration_test.py` | 集成测试脚本 |

### 10.2 验证清单

- [ ] Python AST 语法检查通过
- [ ] JSON 格式校验通过
- [ ] JavaScript 语法检查通过
- [ ] 标准棋子移动计算正确
- [ ] 将死/将军判断正确
- [ ] 自定义棋子创建流程正常
- [ ] JSON Patch 应用正确
- [ ] Schema 校验生效
- [ ] 前端渲染正常
- [ ] AI 走棋功能正常

---

## 11. 当前存在的问题与不足

### 11.1 已知问题

1. **棋盘尺寸修改自动同步逻辑过时**：`ai_orchestrator.py` 中 D 类棋盘尺寸修改的自动同步逻辑仍在操作 `board_state["board"]`，但新架构中尺寸在 `board.geometry` 中。该功能当前可能失效。

2. **B+D 分类已移除但代码仍有残留**：新架构移除了 B+D 分类（棋盘外观修改归入纯 D 类，操作 board.json），但 `ai_orchestrator.py` 中 B+C 处理函数附近还有 B+D 的影子代码。

3. **测试文件可能过时**：`tests/` 下的测试文件和 `run_integration_test.py` 可能仍基于旧架构（piece_rules/game_rules），需要更新或验证。

4. **规则引擎 rules.json 未完全利用**：当前 `rules.json` 主要存储 AI 难度配置，胜利条件和特殊规则仍由规则引擎硬编码，未实现完全数据驱动。

5. **机制原语组合测试尚不充分**：A2类机制修改的多种原语组合场景需要更多实际测试验证。

### 11.2 待优化项

1. 将死/困毙判断的性能优化
2. AI 评估函数的改进（更多位置因素）
3. 更多机制原语的扩展（如棋子变形、道具系统等）
4. 移动端触摸体验优化
5. AI性格系统的更多维度（风险偏好、防守风格等）

---

## 12. 开发路线图与展望

### 已完成功能

- [x] 基础象棋功能（走棋、吃子、将军、将死）
- [x] AI对战（Minimax + Alpha-Beta）
- [x] 自然语言指令解析（意图分类）
- [x] 棋盘变换系统（B类）
- [x] 规则修改系统（C类）
- [x] 自定义棋子创建（C+类）
- [x] 界面修改系统（D1配置/D2 HTML）
- [x] JSON Patch 优先输出
- [x] Token消耗统计
- [x] jump/ray 双原子规则引擎
- [x] where 条件表达式引擎
- [x] JSON Schema 校验
- [x] 撤销栈（撤回AI修改）
- [x] 阵营专属规则（side_overrides）
- [x] A类机制修改重构（A1硬编码 + A2灵活编码）
- [x] 机制引擎（5种原子化原语：跳过/AI接管/随机/额外回合/步数限制）
- [x] AI性格系统（4种预设 + 自定义性格）
- [x] 前端机制状态显示 + AI性格卡片 + 视觉动画

### 待实现功能

- [ ] WebSocket实时通信
- [ ] 多人对战模式
- [ ] 更多预设棋子类型
- [ ] rules.json 完全数据驱动（胜利条件/特殊规则可配置）
- [ ] 棋谱保存与加载
- [ ] 更多AI难度等级
- [ ] 音效系统
- [ ] 更多机制原语（道具系统、棋子变形等）
- [ ] AI性格系统的更多维度
- [ ] 国际化（多语言支持）

### 长远展望

1. **完全可塑的游戏系统**：不仅是象棋，而是一个可以变成任何棋类游戏的平台
2. **AI自主游戏设计**：AI不仅修改规则，还能自主设计全新游戏
3. **社区分享**：玩家可以分享自己创造的规则变体
4. **进化系统**：规则可以代际进化，探索有趣的游戏变体

---

*本文档版本 v4.1，对应机制引擎 + AI性格系统架构。如有变更请同步更新本文档。*
