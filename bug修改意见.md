# 棋圣 (ChessSage) RPG 项目 Bug 修改意见

> **说明**：本文件仅包含 bug 修改意见，暂不包含具体代码修改。

---

## 一、后端 Bug（Python）

### 1.1 rpg_server.py

**B1-01：注释与实际端口不一致**
- **位置**：第 14 行
- **问题**：文件头部注释写的是"端口 80"，但实际代码使用的是端口 8080
- **影响**：误导开发者，增加维护成本
- **建议**：将注释中的端口改为 8080

**B1-02：识破概率随机数生成缺乏种子控制**
- **位置**：`apply_cheat_detection` 函数（第 241 行）
- **问题**：使用 `random.uniform(0, 100)` 生成随机数，但未设置随机种子。在多线程环境下可能导致预测性问题；服务器重启后随机数序列重复
- **影响**：识破判定可能存在可预测性，影响游戏公平性
- **建议**：使用 `random.seed()` 或 `secrets` 模块增加随机性，或使用时间戳作为种子

**B1-03：`reset_battle` 参数默认值问题**
- **位置**：第 204-211 行
- **问题**：`player_side` 参数默认值为 `None`，但调用时传入 `chapter.get("player_side", "red")`。如果 `chapter["player_side"]` 不存在，会传入 `"red"`，但函数内部判断 `player_side or "red"` 会导致逻辑冗余
- **影响**：代码可读性差，增加维护成本
- **建议**：统一默认值处理，函数参数直接设置默认值 `"red"`

**B1-04：围棋提子能量计算可能异常**
- **位置**：`move_complete` 函数（第 626-628 行）
- **问题**：`go_captures` 类型为 `int`，但前端传入的可能是数组或其他类型。当 `go_captures` 为非整数时，乘法运算可能导致错误结果或异常
- **影响**：能量计算错误，影响游戏平衡
- **建议**：增加类型校验，确保 `go_captures` 为正整数

**B1-05：`to_dict` 中结局判定时机不准确**
- **位置**：第 276-278 行
- **问题**：只有在 `current_chapter == "ch06_finale_go"` 且 `battle_started == False` 时才计算结局。但实际游戏流程中，终局对战结束后 `battle_started` 会被设为 `False`，此时才能正确判定结局。然而在章节切换时，如果状态没有正确同步，可能导致结局判定延迟
- **影响**：结局可能显示不正确或延迟显示
- **建议**：在 `battle_end` 路由中明确传递结局信息，而不是依赖状态推断

### 1.2 go/main.py

**B1-06：围棋 `/api/command` 缺少 dry_run 支持**
- **位置**：第 199-217 行
- **问题**：围棋的 `/api/command` 路由没有 `dry_run` 参数，与象棋和五子棋不一致
- **影响**：RPG 作弊评估流程（`cheat/assess`）无法正确工作，导致围棋章节无法评估作弊消耗
- **建议**：添加 `dry_run` 参数支持，与 xiangqi/wuziqi 保持一致

### 1.3 所有棋类游戏

**B1-07：配置文件加载缺少异常处理**
- **位置**：三个棋类的 `load_configs` 方法
- **问题**：加载配置文件时没有 try-except 包裹，如果配置文件损坏或格式错误会导致启动失败
- **影响**：服务器无法启动，用户无法游玩
- **建议**：添加异常处理，配置文件加载失败时使用默认值或提供明确的错误提示

**B1-08：API Key 广播失败无降级处理**
- **位置**：`broadcast_api_key` 函数（rpg_server.py 第 409-424 行）
- **问题**：如果某个棋类服务未启动，`broadcast_api_key` 会返回失败，但不会影响其他服务。然而前端没有区分哪些服务成功，用户体验不佳
- **影响**：用户设置 API Key 后，部分棋类服务可能仍未配置密钥，但用户无法知晓
- **建议**：在返回结果中明确标记每个服务的状态，并在前端显示相应提示

**B1-09：棋类颜色命名不一致**
- **位置**：三个棋类的 `board_state` 中 `current_turn` 和 `player_side` 字段
- **问题**：象棋使用 `red`/`black`，五子棋使用 `black`/`red`，围棋使用 `black`/`white`。虽然 `rpg_server.py` 中的 `move_complete` 路由根据 `chess_type` 区分处理，但这种不一致增加了出错概率
- **影响**：容易导致颜色判断错误，特别是在跨棋类逻辑中
- **建议**：统一使用 `red`/`black` 或 `black`/`white`，或在转换层进行统一处理

---

## 二、前端 Bug（JavaScript）

### 2.1 rpg_shell.js

**B2-01：Web Component 加载失败无错误处理**
- **位置**：`_mountBoard` 函数（第 276-292 行）
- **问题**：`loadChessComponent` 动态 import 失败时会抛出异常，但 `_mountBoard` 没有完全处理。虽然有 try-catch 包裹，但组件初始化失败后没有清理已创建的元素
- **影响**：棋盘组件加载失败后可能残留空元素，影响后续章节加载
- **建议**：在 catch 块中添加 `_unmountBoard()` 调用，确保清理已创建的组件

**B2-02：`_onBoardMove` 中事件数据可能未定义**
- **位置**：第 317-350 行
- **问题**：`e.detail` 中的 `captured`、`mover` 等字段可能为 `undefined`，但代码中使用 `|| null` 处理。然而 `is_check`、`is_five_in_a_row` 使用 `!!` 转换，可能导致布尔值判断错误
- **影响**：能量计算错误，影响游戏平衡
- **建议**：统一使用 `??`（空值合并运算符）或明确的默认值处理

**B2-03：章节切换时棋盘未正确清理**
- **位置**：`_goNextChapter` 函数（第 393-403 行）
- **问题**：调用 `_unmountBoard()` 后直接加载下一章节，但如果 `_unmountBoard()` 是异步操作（虽然目前不是），可能导致状态混乱。此外，章节切换时没有等待 VN 舞台完全隐藏
- **影响**：UI 状态混乱，可能出现棋盘残留或 VN 舞台重叠
- **建议**：确保 `_unmountBoard()` 完成后再加载下一章节，添加适当的等待机制

**B2-04：`_computeEnding` 与后端逻辑不一致**
- **位置**：第 405-409 行
- **问题**：前端 `_computeEnding()` 计算结局的逻辑与后端 `determine_ending()` 一致，但前端在 `_showFinalEnding` 中使用了前端计算的结局。如果前后端状态不同步，会导致结局不一致
- **影响**：结局显示错误，破坏游戏体验
- **建议**：始终使用后端返回的结局，前端仅作为展示层

### 2.2 cheat_panel.js

**B2-05：作弊执行时 `applyCheatPatch` 调用缺少防护**
- **位置**：`_onUse` 函数（第 126-131 行）
- **问题**：调用 `boardEl.applyCheatPatch()` 前检查了方法存在性，但没有检查 `boardEl` 是否为 `null`。如果 `RpgShell.getBoardElement()` 返回 `null`，会导致 TypeError
- **影响**：作弊执行失败，用户无法获得作弊效果
- **建议**：添加 `boardEl` 是否为 `null` 的检查

**B2-06：`cheats_used` 更新方式脆弱**
- **位置**：第 122 行
- **问题**：`cheats_used: RpgShell.getState().cheats_used + 1` 依赖前端状态同步。如果前端状态没有及时更新，会导致 `cheats_used` 计数错误
- **影响**：作弊次数统计错误，影响结局判定
- **建议**：直接使用后端返回的 `cheats_used` 值，或从后端重新获取状态

### 2.3 story_layer.js

**B2-07：VN 引擎调用前缺少存在性检查**
- **位置**：`playOpponentDialogue`、`playSystemMessage`、`playNarration` 函数
- **问题**：调用 `Preview.playStory()` 前没有检查 `Preview` 对象是否存在。如果 `preview.js` 加载失败，会导致 TypeError
- **影响**：剧情无法播放，游戏流程中断
- **建议**：在调用前添加 `Preview` 对象存在性检查，并提供降级方案

**B2-08：临时故事对象缺少错误处理**
- **位置**：`playOpponentDialogue` 函数（第 97-162 行）
- **问题**：动态构造的临时故事 JSON 如果格式错误（如缺少必要字段），`Preview.playStory()` 可能无法正确解析
- **影响**：对手台词无法正常显示，破坏核心爽点
- **建议**：添加临时故事对象的验证逻辑

### 2.4 所有棋类 Web Component

**B2-09：`destroy()` 方法不完整**
- **位置**：三个棋类的 `destroy()` 方法
- **问题**：`destroy()` 方法没有清理所有事件监听器和定时器。例如 `thinkingPollInterval`、键盘事件监听器等可能没有被清除
- **影响**：内存泄漏，切换章节后旧组件的事件监听器仍在运行
- **建议**：完善 `destroy()` 方法，确保清理所有资源

**B2-10：`apiBase` 为空时未处理**
- **位置**：`get apiBase()` 方法
- **问题**：`apiBase` 返回空字符串时，后续的 API 请求会使用空 URL，导致请求失败
- **影响**：棋类游戏无法加载配置，无法正常运行
- **建议**：在 `init()` 方法中检查 `apiBase` 是否有效，无效时抛出错误或提供默认值

**B2-11：`_onBoardMove` 和 `_onBoardGameEnd` 双重调用问题**
- **位置**：`_onBoardMove` 函数（第 343-345 行）和 `_onBoardGameEnd` 函数（第 352-377 行）
- **问题**：当游戏结束时，棋类组件会同时触发 `move` 和 `gameend` 事件。`_onBoardMove` 在收到 `game_ended: true` 时会调用 `_onBoardGameEnd`，而 `_onBoardGameEnd` 本身也会被 `gameend` 事件触发，导致 `battle/end` 路由被调用两次
- **影响**：后端状态可能被重复修改，导致数据不一致；结局判定可能被错误触发
- **建议**：在 `_onBoardMove` 中移除对 `_onBoardGameEnd` 的调用，仅依赖 `gameend` 事件触发游戏结束逻辑

**B2-12：围棋组件缺少 `go_captures` 字段传递**
- **位置**：`go/static/app.js` 的 `dispatchEvent('move')` 处（第 2411-2427 行）
- **问题**：围棋组件在触发 `move` 事件时没有传递 `go_captures` 字段，但 rpg_shell.js 期望接收该字段用于能量计算
- **影响**：围棋提子能量加成无法正确计算，影响游戏平衡
- **建议**：在围棋组件的 `move` 事件中添加 `go_captures` 字段，传递提子数量

**B2-13：`_mountBoard` 中事件监听重复绑定问题**
- **位置**：`_mountBoard` 函数（第 283-286 行）
- **问题**：每次挂载新棋盘时都会绑定新的事件监听器，但 `_unmountBoard` 只是调用组件的 `destroy()` 方法并移除 DOM 元素。如果组件的 `destroy()` 方法没有正确移除事件监听器，会导致事件监听器累积
- **影响**：同一个事件可能触发多次处理逻辑，导致状态混乱
- **建议**：确保 `destroy()` 方法正确移除所有事件监听器，或在 `_mountBoard` 前确保完全清理旧组件

**B2-14：`_computeEnding` 在非终章也被调用**
- **位置**：`_goNextChapter` 函数（第 396 行）
- **问题**：当没有下一章时，代码会调用 `_computeEnding()` 计算结局。但该函数使用前端状态计算，而不是后端返回的结局
- **影响**：如果前后端状态不同步，结局显示错误
- **建议**：始终使用后端返回的结局，或在 `battle_end` 时提前获取结局信息

---

## 三、架构设计问题

### 3.1 状态管理

**A3-01：状态同步机制不完整**
- **问题**：前端状态与后端状态通过 `refreshState()` 和 `updateState()` 手动同步，但没有实时同步机制。如果用户在多个标签页打开游戏，状态会不一致
- **影响**：多标签页状态不同步，可能导致游戏逻辑错误
- **建议**：考虑使用 WebSocket 或轮询机制实现状态实时同步

**A3-02：全局状态集中在内存中**
- **问题**：`RpgState` 和所有棋类的 `GameState` 都存储在内存中。服务器重启后所有状态丢失，用户需要重新开始游戏
- **影响**：用户体验差，无法保存游戏进度
- **建议**：实现存档系统（如项目计划书中提到的 U9），将状态持久化到文件或数据库

### 3.2 错误处理

**A3-03：缺少统一的错误处理机制**
- **问题**：前后端都缺少统一的错误处理机制。后端使用 try-except 捕获异常，但返回的错误信息不够详细；前端使用 console.error 输出错误，但用户无法看到
- **影响**：用户遇到错误时无法了解原因，难以报告问题；开发者难以定位问题
- **建议**：实现统一的错误处理中间件，返回结构化的错误信息；前端添加全局错误提示

**A3-04：网络请求失败无重试机制**
- **问题**：前端所有网络请求都没有重试机制。如果网络不稳定，请求失败后直接报错，用户需要手动重试
- **影响**：网络不稳定时游戏体验差，用户可能频繁遇到错误
- **建议**：为关键网络请求添加重试机制（如 2-3 次重试），使用指数退避策略

### 3.3 安全问题

**A3-05：CORS 设置过于宽松**
- **问题**：所有服务都设置 `allow_origins=["*"]`，允许任何来源的请求
- **影响**：安全风险，可能被恶意网站利用进行 CSRF 攻击
- **建议**：限制允许的来源，只允许本地开发环境和生产环境的域名

**A3-06：API Key 传输未加密**
- **问题**：API Key 通过 HTTP 传输，没有使用 HTTPS
- **影响**：API Key 可能被中间人攻击窃取
- **建议**：生产环境使用 HTTPS；开发环境至少在传输前对 API Key 进行简单加密

**A3-07：后端状态没有并发保护**
- **问题**：`RpgState` 和所有棋类的 `GameState` 都是全局单例，没有使用锁或其他并发控制机制。在多线程环境下，多个请求可能同时修改状态，导致数据竞争
- **影响**：状态数据不一致，可能导致游戏逻辑错误
- **建议**：使用线程锁或异步锁保护状态修改操作

**A3-08：缺少请求限流机制**
- **问题**：所有 API 路由都没有限流机制，如果用户频繁请求（如频繁作弊评估），可能导致服务器资源耗尽
- **影响**：服务器性能下降，甚至崩溃
- **建议**：添加请求限流中间件，限制每个 IP 的请求频率

---

## 四、代码质量问题

### 4.1 代码重复

**Q4-01：三个棋类游戏的 main.py 存在大量重复代码**
- **问题**：xiangqi、wuziqi、go 的 main.py 结构几乎完全相同，只有少数细节差异
- **影响**：维护成本高，修改一处需要同步修改三处
- **建议**：提取共享基类或使用模板方法模式减少重复代码

**Q4-02：前端 CSS 样式重复**
- **问题**：三个棋类游戏的前端样式有大量重复（如按钮、模态框、布局等）
- **影响**：维护成本高，样式不一致时难以统一修改
- **建议**：提取共享 CSS 模块，使用 CSS 变量统一主题

### 4.2 代码风格

**Q4-03：变量命名不一致**
- **问题**：项目中存在多种命名风格，如 `player_side`（snake_case）、`playerSide`（camelCase）、`playerSide`（PascalCase）
- **影响**：代码可读性差，增加理解成本
- **建议**：统一命名风格，前端使用 camelCase，后端使用 snake_case

**Q4-04：缺少代码注释**
- **问题**：部分关键函数和复杂逻辑缺少注释，如 `rule_engine.py` 中的规则验证逻辑
- **影响**：难以理解代码意图，增加维护难度
- **建议**：为关键函数添加文档字符串，为复杂逻辑添加注释

---

## 五、测试问题

### 5.1 测试覆盖率

**T5-01：缺少单元测试**
- **问题**：项目中没有单元测试，只有一个端到端测试脚本（e2e_test.py）
- **影响**：无法保证代码质量，修改代码后难以验证是否引入新问题
- **建议**：为核心逻辑（如规则验证、能量计算、识破概率）添加单元测试

**T5-02：端到端测试不完整**
- **问题**：e2e_test.py 可能没有覆盖所有游戏流程和边界情况
- **影响**：无法确保游戏整体功能正常
- **建议**：扩展端到端测试，覆盖更多场景（如作弊、结局判定、章节切换等）

---

## 六、性能问题

### 6.1 性能优化

**P6-01：配置文件频繁读写**
- **问题**：每次走棋或配置变更后都调用 `save_config()` 将配置写入文件。在频繁走棋的场景下，磁盘 I/O 成为瓶颈
- **影响**：游戏响应变慢，特别是在慢速存储设备上
- **建议**：使用内存缓存，定期批量写入文件；或使用更快的存储机制（如 SQLite）

**P6-02：前端渲染效率**
- **问题**：棋盘渲染使用 DOM 操作，每次走棋都重新渲染整个棋盘。在棋子数量多的情况下（如围棋），渲染效率低
- **影响**：棋盘响应变慢，用户体验差
- **建议**：使用 Canvas 或 WebGL 渲染棋盘，减少 DOM 操作

**P6-03：AI 思考阻塞主线程**
- **问题**：AI 思考过程中（特别是使用 Minimax 算法时）可能阻塞前端主线程，导致 UI 冻结
- **影响**：用户无法操作界面，体验差
- **建议**：将 AI 思考放在 Web Worker 中执行，或使用异步策略

---

## 七、兼容性问题

### 7.1 浏览器兼容性

**C7-01：Web Components 兼容性**
- **问题**：虽然项目目标浏览器是现代 Chrome/Edge，但 Web Components 的某些特性在旧版浏览器中不支持
- **影响**：部分用户无法正常运行游戏
- **建议**：添加浏览器兼容性检测，对不支持的浏览器提供降级方案或提示

**C7-02：ES Modules 兼容性**
- **问题**：动态 import（`import(modulePath)`）在某些旧版浏览器中不支持
- **影响**：棋类组件无法加载，游戏无法运行
- **建议**：提供降级方案，使用 `<script>` 标签动态加载脚本

---

## 八、用户体验问题

### 8.1 可用性

**U8-01：错误提示不够友好**
- **问题**：错误信息通常是技术术语（如 "API Key 未设置"），对普通用户不够友好
- **影响**：用户遇到错误时不知道如何解决
- **建议**：使用更友好的错误提示，提供解决建议

**U8-02：缺少加载状态提示**
- **问题**：章节切换、棋盘加载等操作没有加载状态提示，用户可能以为游戏卡住了
- **影响**：用户体验差，可能导致用户误以为游戏崩溃
- **建议**：添加加载状态指示器，如进度条或加载动画

**U8-03：作弊面板交互不够直观**
- **问题**：作弊面板的操作流程（评估 → 确认 → 执行）对新手不够直观
- **影响**：新手用户难以理解如何使用作弊功能
- **建议**：优化作弊面板的 UI/UX，添加操作引导

---

## 优先级汇总

| 优先级 | Bug ID | 问题描述 |
|:---:|:---:|---|
| **P0** | B1-06 | 围棋缺少 dry_run 支持，作弊评估流程无法工作 |
| **P0** | B2-01 | Web Component 加载失败无清理，导致 UI 状态混乱 |
| **P0** | B2-07 | VN 引擎调用前缺少存在性检查，剧情无法播放 |
| **P0** | B2-11 | `_onBoardMove` 和 `_onBoardGameEnd` 双重调用，导致状态重复修改 |
| **P0** | B2-12 | 围棋组件缺少 `go_captures` 字段，提子能量加成无法计算 |
| **P1** | B1-02 | 识破概率随机数缺乏种子控制，影响游戏公平性 |
| **P1** | B2-05 | 作弊执行时 `applyCheatPatch` 调用缺少防护 |
| **P1** | A3-05 | CORS 设置过于宽松，安全风险 |
| **P1** | A3-07 | 后端状态没有并发保护，多线程环境下数据竞争 |
| **P2** | B1-04 | 围棋提子能量计算可能异常 |
| **P2** | B1-09 | 棋类颜色命名不一致（red/black vs black/white） |
| **P2** | B2-02 | `_onBoardMove` 中事件数据可能未定义 |
| **P2** | B2-13 | `_mountBoard` 中事件监听重复绑定问题 |
| **P2** | B2-14 | `_computeEnding` 使用前端状态，可能与后端不一致 |
| **P2** | A3-02 | 全局状态集中在内存中，服务器重启后丢失 |
| **P2** | A3-08 | 缺少请求限流机制，可能导致服务器资源耗尽 |
| **P3** | Q4-01 | 三个棋类游戏的 main.py 存在大量重复代码 |
| **P3** | P6-01 | 配置文件频繁读写，影响性能 |
| **P3** | U8-01 | 错误提示不够友好 |

---

*本文件列出了项目中发现的所有 bug 和潜在问题，按优先级排序。建议优先修复 P0 和 P1 级别的问题，以确保游戏核心功能正常运行。*