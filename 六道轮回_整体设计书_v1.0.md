# 棋圣 · 六道轮回 —— 整体设计书

> **v1.0 · 识破重构与节奏压缩版**
> 基于"灵活编码"核心理念，以六种棋类为战斗场景，打造一款以"AI 作弊"为核心玩法的 Roguelike 大游戏。
> 一切以趣味 / 创新 / 实验性为纲，为游戏创作者大赛而设计。

---

## 一、设计总览

### 1.1 一句话定位

你是一缕在六道中轮回的灵魂。每一轮 = 穿越 6 界，每界有 5-6 个预制关卡；你可以用自然语言作弊改规则，但**每次作弊都消耗业力**——业力在限度内正常使用不会增加识破概率，只有**透支业力**才会非线性暴涨识破概率，一旦被识破则游戏结束。业力靠正常下棋事件回复，一局 20 回合内完成。六道各有守道者 Boss。无 AI 通关有额外奖励。死则轮回，带着技能点重新来过。

### 1.2 核心体验

| 体验维度 | 描述 |
|---------|------|
| **作弊的爽** | 每条指令 100% 即时生效，正常使用业力不增加识破概率 |
| **透支的怕** | 业力不够时可以透支——但溢出越多，识破概率涨得越疯 |
| **业力的紧** | 作弊消耗很大（改一个马为炮=40点），必须精打细算，以正常下棋为主 |
| **节奏的快** | 每局 20 回合内完成，紧张刺激 |
| **关卡的稳** | 全部预制关卡，正常棋局为主，难度由易到难 |
| **无 AI 的尊** | 不用作弊通关 = 额外技能点 + 尊重 |
| **技能的成长** | 每关通关获得技能点，解锁更强大的作弊能力 |

### 1.3 核心机制总结

> **"下好棋赚业力 → 花业力作弊 → 作弊帮你赢 → 业力不够可以透支 → 透支会让识破概率暴涨 → 被识破就轮回"**

---

## 二、架构总览

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        轮回之门（Hub）                        │
│              hub/index.html · hub/app.js                     │
│     六道转轮图 · 关卡选择 · 技能树 · 成就殿堂 · 业力/识破显示  │
└────────────────────────┬────────────────────────────────────┘
                         │  FastAPI（main.py）
                         │  /api/samsara/*  轮回元状态 API
                         │  /api/achievements/*  成就 API
                         │  静态文件服务
┌────────────────────────┴────────────────────────────────────┐
│                    六道轮回核心引擎（samsara/）               │
│                                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ state.py │ │ karma.py │ │detection│ │bosses.py │       │
│  │ 元状态   │ │ 业力系统 │ │ 识破系统 │ │ Boss系统 │       │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘       │
│       │             │            │             │            │
│  ┌────┴─────┐ ┌────┴─────┐ ┌────┴─────┐ ┌────┴─────┐       │
│  │skills.py │ │progress.│ │levels.py │ │objective│       │
│  │ 技能树   │ │ 进程系统 │ │ 关卡系统 │ │ 目标引擎 │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │              api.py · FastAPI 路由                    │    │
│  └──────────────────────────────────────────────────────┘    │
└─────────────────────────────┬───────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
┌───────▼───────┐    ┌────────▼────────┐  ┌───────▼───────┐
│   象棋 人道    │    │  五子棋 天道     │  │  围棋 阿修罗道  │
│  xiangqi/     │    │   wuziqi/       │  │   weiqi/      │
│  端口 8000    │    │  端口 8001      │  │  端口 8002    │
└───────────────┘    └─────────────────┘  └───────────────┘
        │                     │                     │
┌───────▼───────┐    ┌────────▼────────┐  ┌───────▼───────┐
│  动物棋 畜生道  │    │   跳棋 饿鬼道    │  │ 黑白棋 地狱道  │
│  dongwuqi/    │    │   tiaoqi/       │  │  heibaiqi/    │
│  端口 8003    │    │  端口 8004      │  │  端口 8005    │
└───────────────┘    └─────────────────┘  └───────────────┘
```

### 2.2 六大棋境对应

| 道 | 棋类 | 目录 | 端口 | 主题 | 守道者 | Boss 技能 |
|:-:|------|------|:----:|------|--------|----------|
| 地狱道 | 黑白棋 | `heibaiqi/` | 8005 | 痛苦·翻转·暗与光 | **翻覆者** | 翻转：修改规则30%概率被翻转 |
| 饿鬼道 | 跳棋 | `tiaoqi/` | 8004 | 贪婪·永远不够·无尽之路 | **饕餮者** | 多拿：每作弊2次额外偷改1次 |
| 畜生道 | 动物棋 | `dongwuqi/` | 8003 | 愚痴·等级秩序·本能 | **秩序者** | 等级锁：高等级棋子修改+20业力 |
| 人道 | 象棋 | `xiangqi/` | 8000 | 算计·平衡·人道初窥 | **算计者** | 无：最公平的对决 |
| 阿修罗道 | 围棋 | `weiqi/` | 8002 | 战斗·混乱·混沌气局 | **狂乱者** | 混沌：作弊后25%概率随机规则变化 |
| 天道 | 五子棋 | `wuziqi/` | 8001 | 禅定·五连登仙·天道无情 | **禅定者** | 消业：每局3次，清除最近1条修改 |

### 2.3 统一骨架（每棋类相同）

每个棋类目录结构完全一致，遵循"灵活编码"统一架构：

```
{棋类}/
├── main.py                  # FastAPI 路由（走棋/指令/配置 API）
├── ai_orchestrator.py       # 两级 AI 编排器（意图解析 + 代码生成）
├── chess_ai.py              # AI 走棋算法（Minimax + Alpha-Beta）
├── rule_engine.py           # 规则引擎（jump/ray 原语解析）
├── mechanism_engine.py      # 机制引擎（A2 类机制执行）
├── prompts.py               # System Prompts（原语手册/分类/Patch）
├── configs/                 # 6 个 JSON 配置 + initial/ 备份
│   ├── board_state.json     # 运行时棋盘状态
│   ├── board.json           # 棋盘几何与外观
│   ├── pieces_red.json      # 红方棋子规则
│   ├── pieces_black.json    # 黑方棋子规则
│   ├── rules.json           # 游戏规则 + AI难度 + win_conditions
│   └── ui_config.json       # 界面配置
├── static/                  # 前端（Web Component + Shadow DOM）
│   ├── index.html
│   ├── app.js
│   └── style.css
└── requirements.txt
```

---

## 三、核心系统详解

### 3.1 业力系统（Karma System）

**业力 = 作弊的消耗性资源**。有上限，可回复，可透支。

| 属性 | 初始值 | 说明 |
|-----|:----:|------|
| 当前业力 | 0 | 玩家当前可用的业力量 |
| 业力上限 | **150** | 不透支时，业力不能超过此值 |
| 单次使用上限 | **80** | 单次作弊最多消耗的业力（可通过技能提升） |

#### 3.1.1 业力消耗（AI 评估）

每次作弊，调用 AI 大模型根据以下因素评估业力消耗：

- **作弊强度**（核心因素）：改 1 个棋子 vs 改多个棋子
- **作弊类型**：C+ 类最贵，D 类最便宜
- **当前棋盘局势**：大优时更贵；劣势时稍便宜
- **守道者特性**：某些道对特定类型加价

**基础分类价目表**：

| 分类 | 含义 | 基础价格 |
|-----|------|:-------:|
| E 类 | 聊天/搞笑 | 0-5 点 |
| D 类 | 界面修改/外观 | 5-15 点 |
| A 类 | 机制修改 | 20-40 点 |
| B 类 | 棋盘变换/棋子位置 | 15-35 点 |
| C 类 | 规则修改/棋子走法 | 30-60 点 |
| C+ 类 | 创建新棋子 | 50-100 点 |

**强度倍数**（×基础价）：1个棋子×1.0，2个×2.0，3个及以上×3.0

#### 3.1.2 业力透支（Overdraft）

当消耗 > 当前业力时，可选择**透支**：
- 透支后，当前业力变为负值
- 透支是**唯一增加识破概率的方式**
- 业力为负时，可正常下棋回复，回到正数后才能再次作弊

#### 3.1.3 业力回复（事件驱动）

业力**不随回合自然回复**，而是通过**正常下棋的游戏事件**回复。每棋类事件不同：

| 棋类 | 代表事件 | 回复量 |
|-----|---------|:-----:|
| 人道·象棋 | 吃子（车）/将军/将死/兵过河 | 25/20/35/10 |
| 天道·五子棋 | 成三/成四/堵三/堵四/获胜 | 10/20/8/18/35 |
| 阿修罗·围棋 | 提子(4+)/做活/占角/获胜 | 20/15/12/35 |
| 畜生·动物棋 | 吃子(越级)/逼近兽穴/获胜 | 25/12/35 |
| 饿鬼·跳棋 | 连跳5+/入营/全部入营 | 20/15/35 |
| 地狱·黑白棋 | 翻转5+/占角/获胜 | 25/20/35 |

> **设计意图**：一局内正常下棋大约能获得 60-100 业力（足够改 1-2 个棋子），确保"以正常下棋为主，作弊为辅"的节奏。

### 3.2 识破概率系统（Detection System）

**识破概率 = 被"天道"发现作弊的风险**。只增不减，达到 100% 触发识破判定。

**核心规则：只有当业力透支时，才会增加识破概率。正常使用业力不增加。**

#### 识破概率增长公式

```
Δ识破概率 = 0                     （正常使用业力时）
Δ识破概率 = C × O^α               （仅当业力透支时）
```

其中：
- O = 透支溢出量（业力为负时的绝对值）
- C = 0.5（基础惩罚系数，可通过技能降低）
- α = 1.8（非线性指数，可通过技能降低）

| 溢出量 O | 透支惩罚 | 说明 |
|:-------:|:-------:|------|
| 5 | ≈ 9% | 小透支，尚可承受 |
| 15 | ≈ 63% | 中透支，暴涨 |
| 30 | ≈ 214% | 大透支，几乎必死 |

#### 识破判定流程

```
1. 如果未透支 → Δ识破概率 = 0 → 跳过判定
2. 如果透支 → 计算透支惩罚
3. 累加到当前识破概率
4. 掷骰子：R ∈ [0, 100)
5. 如果 R < 当前识破概率 → 被识破！
   → 检查"金蝉脱壳"技能
   → 有则消耗，识破概率回退到触发前的 50%
   → 无则游戏结束
6. 如果 R ≥ 当前识破概率 → 安全
```

### 3.3 关卡系统（Level System）

每道 = **固定的关卡序列**，全部预制，难度由易到难。

#### 关卡类型

| 类型 | 说明 | 占比 |
|-----|------|:----:|
| **正常对弈**（Standard） | 标准棋类对弈，击败 AI | 60%（每道 ≥3 个） |
| **残局**（Puzzle） | 预制特殊局面，限回合内达成目标 | 20% |
| **特殊目标**（Objective） | 非传统胜利条件（人道及以上） | 10% |
| **Boss 战**（Boss） | 守道者亲自下场，高强度 AI | 10%（每道最后 1 关） |

#### 各道关卡配置

| 道 | 棋类 | 关卡数 | 正常 | 残局 | 特殊目标 | Boss |
|:-:|------|:----:|:---:|:---:|:------:|:----:|
| 地狱道 | 黑白棋 | 5 | 3 | 1 | 0 | 1 |
| 饿鬼道 | 跳棋 | 5 | 3 | 1 | 0 | 1 |
| 畜生道 | 动物棋 | 5 | 3 | 1 | 0 | 1 |
| 人道 | 象棋 | 6 | 3 | 1 | 1 | 1 |
| 阿修罗道 | 围棋 | 6 | 3 | 1 | 1 | 1 |
| 天道 | 五子棋 | 6 | 3 | 1 | 1 | 1 |

### 3.4 游戏节奏（20 回合限制）

所有关卡（除围棋）的目标必须在 **20 回合内**完成。围棋为 40 回合。

**节奏保障机制**：
1. **AI 强度动态调整**：前 10 回合毫无进展，AI 适当放水
2. **中期提示**：第 10 回合进度 <50%，显示"时间紧迫，考虑作弊"
3. **后期压力**：第 15 回合后，AI 加速进攻

### 3.5 多样化胜利条件（Objective Engine）

所有胜利条件通过扩展 `rules.json` 的 `win_conditions` 字段实现，使用 JSON 原语声明式定义。

| 类型 | 说明 |
|-----|------|
| `checkmate` | 将死/标准胜利（默认） |
| `capture_count` | 吃子数目标（如吃对方 4 子） |
| `turn_limit` | 竞速（如 15 回合内获胜） |
| `evacuation` | 撤离（指定棋子到达目标区域） |
| `board_coverage` | 棋盘覆盖率目标 |
| `formation` | 特定阵型达成 |
| `color_coverage` | 颜色覆盖率（⭐核心创新） |
| `survival` | 生存（坚持 N 回合，至少保留 M 子） |
| `assassination` | 刺杀（指定回合内吃掉对方关键棋子） |
| `escort` | 护送（护送指定棋子沿路径推进） |
| `compound` | 复合条件（AND/OR 组合） |

### 3.6 技能树系统（Skill Tree）

#### 技能点获取

| 来源 | 技能点 | 说明 |
|-----|:-----:|------|
| 每关通关 | 1 | 基础奖励 |
| 击败 Boss（首次） | 1 | 额外奖励 |
| **无 AI 通关**（一关内 0 次作弊） | **+2** | 最高尊重奖励 |
| 清业通关（最终业力 <30） | +1 | 干净通关 |

#### 技能树结构（4 分支 × 3 层）

```
                    ┌─ 业力掌控 ── L1 → L2A/L2B → L3A/L3B
                    │
                    ├─ 隐匿之术 ── L1 → L2A/L2B → L3A/L3B
   技能树 ──────────┤
                    ├─ 作弊精通 ── L1A/L1B → L2A/L2B → L3A/L3B
                    │
                    └─ 六道悟道 ── L1A/L1B → L2A/L2B → L3A/L3B
```

**规则**：每层 1 技能点，同分支从 L1 开始逐层解锁；L2/L3 各有两个互斥选项。

#### 分支详情

**业力掌控（Karma Mastery）**
- L1 容量扩充：业力上限 +30
- L2A 灵力吞吐：单次使用上限 +30
- L2B 业力潮汐：所有业力回复 +30%
- L3A 透支缓冲：α 从 1.8 降至 1.4
- L3B 业力池：每局开局业力从 50 开始

**隐匿之术（Stealth Arts）**
- L1 藏锋：C 从 0.5 降至 0.35
- L2A 首次透支免判：每局第一次透支跳过判定
- L2B 连续规避：连续 3 回合不作弊，下次透支惩罚减半
- L3A 金蝉脱壳：被识破后 1 次复活（识破概率回退 50%）
- L3B 迷雾：识破>70%后，30%概率 Δ=0

**作弊精通（Cheat Mastery）**
- L1A 自定义棋子解锁：允许 C+ 类作弊
- L1B 前端修改解锁：允许 D 类作弊
- L2A 效率欺诈：AI 评估 30% 概率降 1 档
- L2B 高级规则解锁：允许修改胜利条件/核心规则
- L3A 白嫖：每局 1 次不消耗业力
- L3B 保险：修改被拒绝时额外退还 20%

**六道悟道（Realm Insight）**
- L1A 地狱之眼：地狱/饿鬼道作弊 -25%
- L1B 天道之耳：天道/阿修罗道透支惩罚 -25%
- L2A 守道者之隙：Boss 技能触发概率 -30%
- L2B 轮回记忆：每次轮回开局自带 1 个随机 buff
- L3A 超脱之种：清业通关后，全局 C 永久 -0.05
- L3B 六道轮转：升降道时，额外 1 次免费作弊

### 3.7 轮回与 Meta 进程

#### 结算与升降道

每道所有关卡通关后结算：

| 结果 | 升降 | 说明 |
|-----|:----:|------|
| 全关通关 + 无透支记录 | **升 2 道** | 极干净，大超脱 |
| 全关通关 | **升 1 道** | 正常通关 |
| 中途失败（被识破/超时/输棋） | **降 1 道** | 败了下坠 |

**通关条件**：天道全关通关 → 超脱 → New Game+（所有道 +1 难度）

#### 技能点永久保留

- 技能点**跨轮回永久保留**
- **不可洗点**——每次选择都是永久的

---

## 四、灵活编码核心架构

### 4.1 两级 AI 流水线

每条自然语言指令经过两级 AI 处理：

```
玩家输入 ──▶ 第一级：意图解析器 ──▶ 第二级：代码生成器 ──▶ JSON Patch ──▶ Schema校验 ──▶ 应用配置
            (DeepSeek)              (专用 Prompt)          (RFC 6902)      (JSON Schema)
```

**第一级 — 意图解析器**：将自然语言分类为 A/B/C/C+/D/E/F 七大分类，评估可行性、业力消耗、置信度，生成结构化 action 列表。

**第二级 — 代码生成器**：根据分类调用专用 System Prompt，输出 RFC 6902 JSON Patch 操作数组，经 Schema 校验后写入配置文件。

### 4.2 意图分类体系

| 分类 | 含义 | 修改目标 | 示例 |
|-----|------|---------|------|
| **A1** | 硬编码操作 | 直接执行 | 悔棋、投降、直接判胜 |
| **A2** | 灵活编码机制 | `board_state.mechanisms` | 冻结 AI、AI 接管、随机走棋、额外回合 |
| **B** | 棋盘变换 | `board_state.json` | 增删棋子、变换类型、旋转棋盘 |
| **C** | 规则修改 | `pieces_red/black.json` | 改走法、改吃子规则、改特殊能力 |
| **C+** | 自定义棋子 | `pieces` + `board_state` | 创造全新棋子类型 |
| **D1** | 界面配置 | `ui_config.json` / `board.json` | 改颜色、线条、棋盘尺寸 |
| **D2** | HTML 结构 | `index.html` 区段 | 增删侧边栏、按钮、模态框 |
| **E** | 纯搞笑 | 无修改 | 闲聊、玩笑、无关话题 |
| **F** | 高级功能 | **直接拒绝** | 需改核心引擎代码的请求 |

### 4.3 规则引擎原语设计

所有棋子走法用两种原子原语声明式定义，AI 只需输出 JSON Patch 修改这些原语。

#### jump — 离散跳跃

```json
{
  "kind": "jump",
  "to": [dx, dy] | {"mode": "region", "region": "$full_board"},
  "block": [[dx, dy], ...],
  "land": "empty|enemy|any",
  "sym": "none|rotate4|rotate4_mirror|mirror_x",
  "where": [...]
}
```

#### ray — 射线滑行

```json
{
  "kind": "ray",
  "dir": [dx, dy],
  "max": -1,
  "screens": 0,
  "land": "empty|enemy|any",
  "sym": "none|rotate4|rotate4_mirror|mirror_x",
  "path_constraint": {"must_be": "water|land|any", "no_blocker": true},
  "where": [...]
}
```

#### where 条件表达式

```json
{"not": {"in_water": {"pos": "$dest"}}}   // 不在水中
{"in_enemy_trap": {"pos": "$dest"}}       // 在敌方陷阱中
{"in_region": {"pos": "$dest", "region": "red_side"}}  // 在红方区域
{"and": [{"at_row": {"pos": "$self", "row": 3}}, {"in_water": "$self"}]}
```

**操作符**：`in_water`、`in_trap`、`in_enemy_trap`、`in_own_den`、`in_enemy_den`、`in_region`、`at_row`、`at_col`、`not`、`and`、`or`

**变量**：`$self`（当前位置）、`$dest`（目标位置）

### 4.4 地形棋子原语（category: "terrain"）

陷阱等场地原语以**地形棋子**形式存在，与动物棋子共存于同一格。

```json
{
  "type": "trap",
  "label": {"red": "✖️", "black": "✖️"},
  "category": "terrain",       // 标记为地形棋子（不参与移动/吃子/胜负）
  "rank": 0,
  "moves": [
    {"kind": "jump", "to": [0, 0], "block": [], "land": "empty", "sym": "none"}
  ],
  "effects": [                  // 地形效果列表
    {
      "type": "rank_override", // 效果类型：rank_override=覆盖等级
      "target": "enemy",       // 作用对象：enemy/all/self
      "value": 0,              // 覆盖值
      "scope": "while_occupying" // 作用范围
    }
  ]
}
```

**地形棋子规则**：
- `category: "terrain"` 标记后，该棋子不可移动、不可吃子、不被吃、不参与胜负判定
- 与动物棋子共存于同一格（引擎在 `_get_piece_at` 中自动跳过）
- `effects` 中的效果对**占据同格**的棋子持续生效

### 4.5 机制原语（A2 类）

通过修改 `board_state.json` 的 `mechanisms` 字段实现运行时机制：

| 机制 | 效果 |
|-----|------|
| `skip_turns` | 跳过指定方 N 回合 |
| `ai_control` | AI 接管指定方 N 回合 |
| `random_moves` | 随机走 N 步 |
| `extra_turns` | 额外 N 回合 |
| `move_limits` | 每回合限走 N 步 |
| `player_control` | 设置玩家控制阵营 |

### 4.6 JSON Patch + Schema 校验

- **修改方式**：RFC 6902 JSON Patch（`add` / `remove` / `replace`）
- **安全保障**：JSON Schema 校验，防止格式错误
- **扩展空间**：`custom_properties` + `additionalProperties: true` 为 AI 留足创造空间

---

## 五、六道守道者 Boss 系统

每道最后 1 关固定为 Boss 战，守道者各有专属反作弊技能。

| 道 | 守道者 | 专属技能 | 效果 | AI 性格 |
|:-:|--------|----------|------|---------|
| 地狱道 | **翻覆者** | **翻转** | 修改规则有 30% 概率被翻转效果 | aggressive |
| 饿鬼道 | **饕餮者** | **多拿** | 每作弊 2 次，额外偷改 1 次 | aggressive |
| 畜生道 | **秩序者** | **等级锁** | 高等级棋子修改业力消耗额外 +20 点 | defensive |
| 人道 | **算计者** | **无** | 最公平，标准规则 | normal |
| 阿修罗道 | **狂乱者** | **混沌** | 作弊后 25% 概率随机规则变化 | aggressive + random |
| 天道 | **禅定者** | **消业** | 每局 3 次，直接清除最近 1 条修改 | defensive + hard |

---

## 六、前端与界面

### 6.1 轮回之门（Hub）

- **六道转轮图**：可视化当前所处道位，点击可查看各道信息
- **关卡选择**：当前道的关卡列表，显示类型/难度/Boss 标记
- **技能树界面**：4 分支 × 3 层，可视化技能解锁状态
- **成就殿堂**：37 个成就，普通/稀有/传说三档
- **状态栏**：业力条、识破概率条、当前境界、技能点

### 6.2 棋局界面（各棋类统一）

- **业力条**：显示当前业力 / 上限，透支时红色闪烁
- **识破概率条**：显示当前识破概率，高时红色警告
- **20 回合倒计时**：回合数 / 上限，接近时警告
- **目标进度条**：特殊目标关显示进度
- **Boss 技能图标**：Boss 战显示守道者头像和技能状态
- **作弊预览框**：输入指令后显示预估业力消耗，确认后执行
- **透支警告**：业力不足时显示"念力透支·天道警觉骤增"提示

---

## 七、数据层

### 7.1 配置文件体系

```
configs/
├── samsara_state.json       # 轮回存档（元状态）
├── boss_definitions.json    # Boss 定义
├── skill_tree.json          # 技能树配置
├── karma_events.json        # 每棋事件→业力回复映射
├── level_pools.json         # 六道预制关卡定义
├── objective_types.json     # 胜利条件类型 Schema
├── formations.json          # 阵型定义库
└── puzzles.json             # 预制残局数据库
```

### 7.2 各棋类配置

每个棋类 6 个 JSON 配置文件，全部可通过 AI 实时修改：

| 文件 | 作用 |
|------|------|
| `board_state.json` | 运行时棋盘状态（pieces、current_turn、game_status、mechanisms） |
| `board.json` | 棋盘几何与外观（geometry、appearance、regions） |
| `pieces_red.json` | 红方棋子规则（pieces、custom_pieces） |
| `pieces_black.json` | 黑方棋子规则 |
| `rules.json` | 游戏规则（win_conditions、special_rules、ai_difficulty） |
| `ui_config.json` | 界面配置（theme、layout、custom_css） |

---

## 八、核心循环

### 一轮回的完整流程

```
┌─────────────────────────────────────────────────────┐
│                轮回之门（总坛）                        │
│  当前道：[X]  关卡：[X/Y]  业力：0/150  识破：0%     │
│  技能点：[X]   已解锁技能：[列表]                      │
│               [进入下一关]                            │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│                关卡加载                                │
│  • 显示关卡信息：类型 / 目标 / 20 回合限制            │
│  • 显示提示："本关目标：将死对方"                     │
│  • 重置业力 = 0、识破 = 0%、回合 = 0                  │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│                对弈阶段（20 回合倒计时）                │
│  • 正常走棋 → 触发事件 → 回复业力                     │
│  • 达成子目标 → 额外业力 + 进度提示                   │
│  • 输入作弊指令 → AI 评估（显示预估消耗）→ 确认       │
│  • AI 修改成功 → 扣除业力 → 检查是否透支             │
│  • 透支 → 计算识破概率 → 识破判定                    │
│  • Boss 技能触发                                      │
│  • 被识破 → 检查金蝉脱壳 → 无则轮回结束               │
│  • 20 回合到 → 失败                                  │
└──────────────────────┬──────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────┐
│                   结算阶段                            │
│  通关？ → 是 → 计算奖励：                             │
│    • 基础：1 技能点                                   │
│    • 无 AI？+2 技能点                                 │
│    • 未透支？+1 技能点                                │
│    • 首次 Boss？+1 技能点                             │
│  → 显示总奖励 → [选择技能] / [下一关]                 │
│                                                       │
│  失败？ → 轮回结束 → 保留技能点 → 回到轮回之门        │
└──────────────────────┬──────────────────────────────┘
                       ↓
              当前道所有关卡通关？
                  /      \
                是        否
                ↓         ↓
            结算道位    继续下一关
           /    \
         升道    降道
```

---

## 九、爽感与难度平衡

### 9.1 爽感保障

1. **正常作弊不增加识破概率** → 放心作弊（只要有业力）
2. **作弊 100% 即时生效** → 当下的快乐不打折
3. **业力可预览** → 知道要花多少
4. **失败退还** → 不白花
5. **正常下棋赚业力** → 不是纯消耗
6. **节奏快** → 20 回合一局，不拖沓

### 9.2 难度保障

1. **透支悬崖** → 大透支几乎必死
2. **作弊消耗大** → 改一个马为炮=40点，一局只能改 1-2 个
3. **20 回合限制** → 时间压力
4. **Boss 差异化** → 每道有独特机制
5. **技能不可洗** → 选择有重量

---

## 十、技术栈

| 层 | 技术 |
|----|------|
| 前端 | 原生 Web Components + Shadow DOM，无框架依赖 |
| 后端 | FastAPI + Uvicorn，每棋类独立进程 |
| AI | DeepSeek（deepseek-chat），两级流水线 |
| 配置修改 | RFC 6902 JSON Patch + JSON Schema 校验 |
| 总坛 | FastAPI 单服务，彩色日志，一键拉起全部子进程 |
| 存档 | JSON 文件持久化（`samsara_state.json` + `achievements.json`） |

---

## 十一、项目结构

```
workspace/
├── main.py                      # 统一启动器 + Hub 后端 + 成就 API
├── config.json                  # 全局 API 密钥
├── achievements.json            # 成就物理存档
├── requirements.txt
│
├── hub/                         # 六道众生总坛（轮回之门）
│   ├── index.html               # 首页（六道转轮 + 关卡选择 + 技能树）
│   ├── achievements.html        # 成就殿堂页面
│   ├── app.js / style.css       # 首页逻辑与样式
│   └── achievements.js / .css   # 成就页面逻辑与样式
│
├── shared/                      # 共享模块
│   ├── achievement_checker.js   # 成就检测前端模块
│   ├── json_patch_utils.py      # RFC 6902 JSON Patch 实现
│   ├── schema_validator.py      # JSON Schema 校验
│   └── schemas/                 # 配置文件 Schema 定义
│
├── samsara/                     # 六道轮回核心引擎
│   ├── main.py                  # 入口
│   ├── api.py                   # FastAPI 路由
│   ├── state.py                 # 轮回元状态管理
│   ├── karma.py                 # 业力系统
│   ├── karma_assessor.py        # AI 业力评估
│   ├── detection.py             # 识破概率系统
│   ├── bosses.py                # Boss 技能系统
│   ├── skills.py                # 技能树系统
│   ├── progression.py           # 升降道与技能点获取
│   ├── levels.py                # 关卡管理（全预制）
│   ├── objectives.py            # 多样化目标判定引擎
│   └── turn_limit.py            # 回合限制系统
│
├── configs/                     # 全局配置
│   ├── samsara_state.json       # 轮回存档
│   ├── boss_definitions.json    # Boss 定义
│   ├── skill_tree.json          # 技能树
│   ├── karma_events.json        # 业力事件映射
│   ├── level_pools.json         # 六道预制关卡
│   ├── objective_types.json     # 胜利条件 Schema
│   ├── formations.json          # 阵型定义库
│   └── puzzles.json             # 预制残局数据库
│
├── xiangqi/                     # 无限制象棋（人道 · 端口 8000）
├── wuziqi/                      # 无限制五子棋（天道 · 端口 8001）
├── weiqi/                       # 无限制围棋（阿修罗道 · 端口 8002）
├── dongwuqi/                    # 无限制动物棋（畜生道 · 端口 8003）
├── tiaoqi/                      # 无限制跳棋（饿鬼道 · 端口 8004）
└── heibaiqi/                    # 无限制黑白棋（地狱道 · 端口 8005）
    ├── main.py                  # FastAPI 路由
    ├── ai_orchestrator.py       # 两级 AI 编排器
    ├── chess_ai.py              # AI 走棋算法
    ├── rule_engine.py           # 规则引擎（原语解析）
    ├── mechanism_engine.py      # 机制引擎
    ├── prompts.py               # System Prompts
    ├── configs/                 # 6 个 JSON 配置
    └── static/                  # 前端
```

---

## 十二、设计风险与应对

| 风险 | 严重程度 | 应对 |
|-----|:-------:|------|
| 作弊消耗太高 → 玩家不敢作弊 | 高 | 业力回复提高 + 业力池技能 + 白嫖技能 |
| 作弊消耗太低 → 玩家狂作弊 | 高 | AI 评估含具体示例约束 + Boss 加价 |
| 透支惩罚太轻 → 玩家频繁透支 | 高 | α=1.8 的非线性曲线 + 大透支几乎必死 |
| 透支惩罚太重 → 玩家不敢透支 | 中 | 小透支(O<10)惩罚可控 + 透支缓冲技能 |
| 20 回合太紧张 → 玩家来不及 | 中 | 中期提示 + AI 动态调整 + 围棋延长到 40 |
| AI 评估不稳定 | 中 | prompt 含具体示例 + 缓存 + 兜底 |
| 残局不够有趣 | 中 | 从经典残局库精选 + 人工审核 |

---

*v1.0 · 2026-07-24 · 识破重构与节奏压缩版*
