# 棋圣 · 六道轮回 —— 整体设计书

> **v2.0 · 业障模型与章节溢出叠加版**
> 基于"灵活编码"核心理念，以六种棋类为战斗场景，打造一款以"AI 作弊"为核心玩法的 Roguelike 大游戏。
> 业力为业障，作弊则增业，下棋则消业；超阈则非线性涨识破，被识破则轮回重来。

---

## 第一章 设计总览

### 1.1 一句话定位

玩家是一缕在六道中轮回的灵魂，从地狱道逐级轮转至天道，每道由 5-6 个预制关卡组成。玩家通过正常下棋推进局势，可用自然语言向 AI 发出作弊指令实时改写棋盘 / 棋子 / 规则 / UI / 机制。作弊产生"业障"（业力增加），下棋事件"消业"（业力减少）；业力超出安全阈值会非线性增加本道识破概率，被识破则非永久进度清零、轮回重来。

### 1.2 核心体验

| 体验维度 | 描述 |
|:--------|:-----|
| **作弊的爽** | 自然语言一句话改写棋局，AI 实时生成 JSON Patch 应用到配置 |
| **业障的压** | 作弊越多业力越高，必须靠下棋消业才能继续作弊 |
| **透支的怕** | 业力超阈值后非线性涨识破，溢出 100 点 = 识破概率 +100% |
| **章节的连** | 同一道内每关溢出叠加给下一关，迫使玩家"控制业力" |
| **技能的成长** | 通关获得技能点，4 分支 × 3 层技能树持久保留 |
| **关卡的稳** | 全部预制关卡，难度由易到难，无 AI 通关有额外奖励 |
| **被识破的悟** | 识破后只清当前进度，技能树 / 技能点 / Boss 记录 / 成就保留 |

### 1.3 核心循环

```
下棋消业 → 业力空间扩大 → 花业力作弊 → 改写局势取胜 → 通关获技能点
   ↑                                              │
   └── 胜/负都把业力溢出叠加给本道下一关 ──────────┘
                                                  │
                                  换道/被识破 → 溢出清零
```

### 1.4 与 v1.0 的关键差异

| 维度 | v1.0（资源模型） | v2.0（业障模型） |
|:----:|:-----------------|:-----------------|
| 业力方向 | 作弊**消耗**业力（资源） | 作弊**增加**业力（业障） |
| 初始业力 | 150（满） | 50（基础值） |
| 安全阈值 | 150 | 120 |
| 单次上限 | 80 | 120 |
| 业力回复 | 透支后回复资源 | 下棋消业（事件驱动） |
| 溢出处理 | 仅识破概率 | 识破概率 + 章节内叠加给下一关 |
| 识破公式 | C×O^1.8 | C×O^1.5（O=100 时 Δ=100%） |
| 保险技能 | 存在（退款） | 删除 |
| 技能树 | 单次上限+30 | 单次上限+30/+40、净身、深层作弊 |

---

## 第二章 业力系统（业障模型）

### 2.1 模型定义

业力 = 玩家作弊产生的"业障"。初始为 50，安全阈值为 120，单次作弊业力增量上限为 120。业力最小为 0，最大无硬上限（但溢出会触发识破）。

### 2.2 关键参数

| 参数 | 字段 | 默认值 | 说明 |
|:----:|:-----|:------:|:-----|
| 初始业力 | `initial_karma` | 50 | 关卡起始业力 |
| 安全阈值 | `karma_max` | 120 | 超出后识破概率非线性增长 |
| 单次上限 | `karma_single_max` | 120 | 单次作弊业力增量上限 |
| 本道溢出叠加 | `realm_overshoot_carryover` | 0 | 本章节上一局溢出量，叠加到下一局起始 |

### 2.3 业力增减规则

#### 增业（作弊）

```
触发条件：AI 作弊指令成功执行（type=applied 或 type=fun）
评估机制：KarmaAssessor.assess() 调用 DeepSeek 评估
评估输出：1-120 整数
边界处理：amount > 单次上限 → 拦截，业力不变
```

#### 消业（下棋事件）

```
触发条件：吃子 / 将军 / 三连 / 翻转 / 跳子等事件
事件定义：configs/karma_events.json
消业公式：实际消业 = 事件基础值 × karma_recover_multiplier
边界处理：业力最小为 0
```

#### 退还（作弊失败）

```
触发条件：type=rejected/error 或 karma_blocked
退还方式：业力不增加（等效未发生）
特殊处理：E 类被 ChatAI 拒绝 → 退还 1 点
```

### 2.4 章节溢出叠加机制

**章节定义**：六道轮回中的一个"道"为一个章节。

#### 关卡开始

```python
level_karma = max(0, initial_karma - initial_karma_reduction) + realm_overshoot_carryover
```

- `initial_karma_reduction`：净身技能减免（默认 0，技能解锁后 25）
- `realm_overshoot_carryover`：本道上一局溢出叠加（默认 0）

#### 关卡结束（胜负都执行）

```python
overshoot = max(0, level_karma - karma_max - karma_max_bonus)
realm_overshoot_carryover = overshoot
```

#### 章节切换

```python
set_realm(new_realm):
    current_realm = new_realm
    current_level = 0
    realm_overshoot_carryover = 0   # 换道清零
```

#### 被识破

```python
reset_on_detection():
    # 重置所有非永久进度
    realm_overshoot_carryover = 0   # 溢出清零
    level_karma = initial_karma     # 业力重置为初始值
    # 保留：技能树、技能点、Boss 记录、已通关道、成就
```

### 2.5 设计意图

- **业障模型**：作弊不是消耗资源，而是积累"罪业"。这与佛教"业障"概念一致，作弊越多越接近天道识破。
- **章节叠加**：玩家在本道内必须控制每局业力，否则溢出会累积到下一局。迫使玩家"克制作弊"。
- **胜负都叠加**：失败后重试也要承担溢出，避免"故意失败重置业力"的作弊策略。
- **换道清零**：新章节新开始，避免长期累积导致后期不可玩。
- **被识破清零**：识破是惩罚，但不应让玩家彻底放弃（保留技能树等永久进度）。

---

## 第三章 识破概率系统

### 3.1 公式

```
Δ = C × O^α
```

| 参数 | 含义 | 默认值 | 技能调整 |
|:----:|:-----|:------:|:---------|
| O | 业力超出安全阈值的部分 | - | - |
| C | 惩罚系数 | 0.1 | 藏锋 → 0.07 |
| α | 非线性指数 | 1.5 | 缓冲 → 1.3 |
| Δ | 识破概率增量（%） | - | - |

### 3.2 校准表（默认 C=0.1, α=1.5）

| 溢出量 O | Δ（%） | 累计识破概率* |
|:--------:|:------:|:-------------:|
| 10 | 3.16 | 3.16% |
| 20 | 8.94 | 8.94% |
| 30 | 16.43 | 16.43% |
| 50 | 35.36 | 35.36% |
| 80 | 71.55 | 71.55% |
| 100 | **100.00** | 100% |
| 150 | 183.71 | 100%（封顶） |

*累计识破概率为本道所有溢出事件 Δ 之和，封顶 100%。

**校准目标**：超出 100 点 = 识破概率增长 100%。

### 3.3 触发流程

```
1. 玩家作弊 → increase_karma(amount)
2. 计算 overshoot = max(0, level_karma - karma_max)
3. 若 overshoot > 0：
   a. 计算 Δ = C × overshoot^α
   b. 首次透支免判（stealth_t2a）→ Δ = 0，标记已用
   c. 迷雾（stealth_t3b）→ 识破 > 70% 时 30% 概率 Δ = 0
   d. realm_detection += Δ（封顶 100）
   e. record_overdraft()
4. roll = random(0, 100)
5. 若 roll < realm_detection：
   a. 金蝉脱壳（stealth_t3a）→ realm_detection *= 0.5，免死
   b. 否则 → reset_on_detection()，轮回重来
```

### 3.4 道级全局变量

识破概率是**道级全局变量**（`realm_detections[realm]`），跨关卡保留：
- 同一道内每关的识破概率累计
- 换道时新道从 0 开始
- 被识破时所有道重置为 0

### 3.5 设计意图

- **非线性增长**：α=1.5 使小透支可控（O=10 仅 +3%），大透支几乎必死（O=100 必死）。鼓励"小步透支"而非"豪赌一次"。
- **道级而非关卡级**：识破概率跨关保留，迫使玩家在整道内控制作弊节奏。
- **技能可降低**：藏锋（C↓）、缓冲（α↓）、首次免判、迷雾、金蝉脱壳，给玩家"作弊流"build 空间。

---

## 第四章 技能树系统

### 4.1 结构

4 分支 × 3 层，每层 1-2 个选项。T1 默认解锁 2 个（karma_capacity_t1、stealth_t1），其余需技能点。每分支 T2 需先解锁 T1，T3 需先解锁 T2 中至少一个。

### 4.2 技能点获取

| 条件 | 技能点 |
|:-----|:------:|
| 基础通关 | +1 |
| 无 AI 通关（no_cheat） | +2 |
| 未透支通关 | +1 |
| 首杀 Boss | +1 |
| 沙盒胜利 | +1 |

单关最多 +5 技能点。

### 4.3 业力掌控分支 💫

| 层 | 技能 ID | 名称 | 效果 | 修饰符 |
|:--:|:--------|:-----|:-----|:-------|
| T1 | karma_capacity_t1 | 安全阈值+20 | 业力安全阈值 +20 | `karma_max_bonus += 20` |
| T2a | karma_capacity_t2a | 单次上限+30 | 单次作弊业力上限 +30 | `karma_single_max_bonus += 30` |
| T2b | karma_capacity_t2b | 业力潮汐 | 消业事件 +30% | `karma_recover_multiplier = 1.3` |
| T3a | karma_capacity_t3a | 缓冲 | 识破非线性指数 1.5→1.3 | `detection_alpha = 1.3` |
| T3b | karma_capacity_t3b | 净身 | 初始业力 50→25 | `initial_karma_reduction = 25` |

### 4.4 隐匿之术分支 👻

| 层 | 技能 ID | 名称 | 效果 | 修饰符 |
|:--:|:--------|:-----|:-----|:-------|
| T1 | stealth_t1 | 藏锋 | 惩罚系数 0.1→0.07 | `detection_coefficient = 0.07` |
| T2a | stealth_t2a | 首次透支免判 | 每局第一次透支 Δ=0 | `first_overdraft_skip = True` |
| T2b | stealth_t2b | 连续规避 | 连续3回合不作弊后透支惩罚减半 | `consecutive_avoid = True` |
| T3a | stealth_t3a | 金蝉脱壳 | 被识破后1次复活 | `golden_escape = True` |
| T3b | stealth_t3b | 迷雾 | 识破>70%时30%概率Δ=0 | `mist_fog = True` |

### 4.5 作弊精通分支 🎲

| 层 | 技能 ID | 名称 | 效果 | 修饰符 |
|:--:|:--------|:-----|:-----|:-------|
| T1a | cheat_mastery_t1a | 自定义棋子 | 允许 C+ 类作弊 | allowed_classifications += "C+" |
| T1b | cheat_mastery_t1b | 前端修改 | 允许 D 类作弊 | allowed_classifications += "D" |
| T2a | cheat_mastery_t2a | 效率欺诈 | 30%概率业力降30% | `efficiency_fraud = True` |
| T2b | cheat_mastery_t2b | 高级规则 | 允许修改胜利条件/核心规则 | - |
| T3a | cheat_mastery_t3a | 白嫖 | 每局1次不消耗业力 | `free_cheat_count = 1` |
| T3b | cheat_mastery_t3b | 深层作弊 | 单次上限+40 | `karma_single_max_bonus += 40` |

### 4.6 六道悟道分支 🔮

| 层 | 技能 ID | 名称 | 效果 | 修饰符 |
|:--:|:--------|:-----|:-----|:-------|
| T1a | realm_insight_t1a | 地狱之眼 | 地狱/饿鬼道业力-25% | `hell_hungry_discount = True` |
| T1b | realm_insight_t1b | 天道之耳 | 天道/阿修罗透支-25% | `heaven_asura_discount = True` |
| T2a | realm_insight_t2a | 守道者之隙 | Boss技能概率-30% | `boss_skill_reduction = True` |
| T2b | realm_insight_t2b | 轮回记忆 | 开局自带1随机buff | `reincarnation_buff = True` |
| T3a | realm_insight_t3a | 超脱之种 | 清业通关后全局C-0.05 | `transcendence_bonus = -0.05` |
| T3b | realm_insight_t3b | 六道轮转 | 升降道时1次免费作弊 | `free_cheat_on_realm_change = True` |

### 4.7 设计意图

- **4 分支对应 4 种 build**：业力流（多作弊）、隐匿流（多透支）、作弊流（高级作弊）、悟道流（道域专精）
- **T1 默认解锁**：玩家开局即可体验基础业力扩容和识破降低
- **永久保留**：技能树在轮回中持久保留，给玩家长期成长感

---

## 第五章 关卡与进度系统

### 5.1 六道关卡分布

| 道 | 棋类 | 关卡数 | 主题 | 守道者 | Boss 技能 |
|:--:|:----:|:------:|:----|:------|:----------|
| ☯ 地狱道 | heibaiqi（黑白棋） | 5 | 痛苦·翻转 | 翻覆者 | 修改规则30%概率被翻转 |
| 👹 饿鬼道 | tiaoqi（跳棋） | 5 | 贪婪·无尽之路 | 饕餮者 | 每作弊2次额外偷改1次 |
| 🐅 畜生道 | dongwuqi（动物棋） | 5 | 愚痴·等级秩序 | 秩序者 | 高等级棋子修改+20业力 |
| 🧠 人道 | xiangqi（象棋） | 6 | 算计·平衡 | 算计者 | 无（最公平对决） |
| ⚔️ 阿修罗道 | weiqi（围棋） | 6 | 战斗·混沌 | 狂乱者 | 作弊后25%概率随机规则变化 |
| ☸️ 天道 | wuziqi（五子棋） | 6 | 禅定·五连登仙 | 禅定者 | 每局3次清除最近1条修改 |

总计 33 关 + 6 Boss。

### 5.2 关卡类型

- `standard`：标准对局，回合限制 20（围棋 40）
- `puzzle`：预制残局，特定目标（吃子数 / 撤离 / 覆盖率等）
- `boss`：守道者战，附加 Boss 反作弊技能
- `sandbox`：通关整道后解锁，自由对弈

### 5.3 胜利条件类型

11 种目标类型（`configs/objective_types.json`）：

| 类型 | 含义 | 典型参数 |
|:-----|:-----|:---------|
| checkmate | 将死对方 | - |
| capture_count | 吃子数目标 | target, target_side |
| turn_limit | 竞速 | max_turns |
| evacuation | 撤离 | pieces, target_region |
| board_coverage | 棋盘覆盖率 | target_percentage |
| color_coverage | 颜色覆盖率 | target_percentage |
| formation | 特定阵型 | formation_id |
| survival | 生存 | min_pieces, max_turns |
| assassination | 刺杀 | target_piece |
| escort | 护送 | piece, path |
| compound | 复合条件 | operator(AND/OR), conditions |

### 5.4 升降道机制

```python
def advance_realm():
    if levels_passed >= total_levels:
        mark_realm_completed(current_realm)
        unlock_sandbox(current_realm)
        if overdraft_count == 0:
            # 无透支通关，跳级 +2
            new_index = min(current_index + 2, len(REALMS) - 1)
            direction = "up_2"
        else:
            # 普通通关，升 1 级
            new_index = current_index + 1
            direction = "up_1"
        set_realm(REALMS[new_index])  # 换道，清零溢出叠加
```

### 5.5 被识破后的重置

`reset_on_detection()` 保留：
- `skills`（技能树）
- `skill_points`（技能点）
- `bosses_defeated`（Boss 击败记录）
- `sandbox_unlocked`（已解锁沙盒）
- `realm_progress`（已通关道标记，未通关道重置）
- `total_levels_completed`（总通关数）
- `version`（版本号）

重置：
- `current_realm = "hell"`（回到地狱道）
- `current_level = 0`（第 1 关）
- `level_karma = initial_karma`（业力重置）
- `realm_overshoot_carryover = 0`（溢出清零）
- `realm_detections = {all: 0.0}`（识破概率清零）
- `cheat_count / overdraft_count = 0`

---

## 第六章 作弊指令评估系统

### 6.1 两级 AI 流水线

```
玩家指令
   │
   ├─→ [并行] 意图解析 AI (Intent Parser)
   │         输出: {classification, intent, cost_energy, ...}
   │
   └─→ [并行] 业力评估 AI (Karma Assessor)
             输入: 指令 + 分类 + 棋盘摘要 + 当前业力
             输出: 1-120 整数
   │
   ▼
合并结果 → 业力上限拦截检查
   │
   ├─→ 评估值 > 单次上限 → 拦截，业力不变
   │
   └─→ 评估值 ≤ 单次上限 → 进入代码生成 AI
              │
              ▼
        JSON Patch 生成 → Schema 校验 → 应用配置
              │
              ▼
        成功 → increase_karma(amount)
        失败 → 业力不变（等效退还）
```

### 6.2 意图分类

| 分类 | 含义 | 业力区间 | 技能解锁 |
|:----:|:-----|:--------:|:--------:|
| E | 闲聊 / 搞笑 | 1（固定） | 默认 |
| D | 界面修改 / 外观 | 5-15 | cheat_mastery_t1b |
| A | 机制修改 | 20-40 | 默认 |
| B | 棋盘变换 / 棋子位置 | 15-35 | 默认 |
| C | 规则修改 / 棋子走法 | 30-60 | 默认 |
| C+ | 创建新棋子 | 50-100 | cheat_mastery_t1a |
| F | 失败 / 拒绝 | 0 | - |

### 6.3 业力评估提示词结构

```
1. 背景说明（业障模型、初始50、阈值120）
2. 当前状态（棋类、指令、分类、棋盘摘要、当前业力、单次上限）
3. 评估标准
   a. 基础分类价目表
   b. 强度倍数（1项×1.0，2项×2.0，3+项×3.0）
   c. 棋类具体示例（每个棋类独立）
   d. 局势调整（大优×1.2，劣势×0.9）
   e. 守道者加价（畜生道：高等级棋子+20）
4. 边界约束（最低1，最高120）
5. 输出格式（仅整数）
```

### 6.4 各棋类评估示例（摘录）

**动物棋（dongwuqi）**：
- "把我的狼变成象"：40 点（C 类 ×1.0）
- "让我的鼠可以在岸上吃象"：35 点（C 类 ×1.0）
- "创建一个能飞的狮"：80 点（C+ 类）
- "改棋盘背景颜色"：10 点（D 类）
- "你好" / "讲个笑话"：1 点（E 类，固定）

**象棋（xiangqi）**：
- "把我的兵变成车"：50 点（C 类 ×1.5）
- "让我的马走田字"：45 点（C 类）
- "把对方将的位置移到中线"：30 点（B 类）
- "改棋盘颜色为红色"：8 点（D 类）

### 6.5 业力退还规则

| 场景 | 处理 |
|:-----|:-----|
| E 类被 ChatAI 拒绝 | 退还 1 点 |
| 修改失败（type=rejected） | 全额退还（业力不增加） |
| 修改错误（type=error） | 全额退还 |
| 评估超出单次上限 | 直接拦截，业力不变 |
| 修改成功（type=applied） | 增加业力 |
| 闲聊成功（type=fun） | 增加 1 点 |

---

## 第七章 灵活编码（Flexible Coding）

### 7.1 核心理念

"灵活编码"是指 AI 通过 JSON Patch 实时修改游戏配置，而非生成可执行代码。所有棋类配置（board_state / board / pieces_red / pieces_black / rules / ui_config）都是 JSON 文件，AI 输出 RFC 6902 JSON Patch 操作数组，经 Schema 校验后应用到配置。

### 7.2 规则原语

```json
{
  "moves": [
    {"type": "jump", "offsets": [[1, 0], [2, 0]], "where": "empty"},
    {"type": "ray", "direction": [1, 0], "range": 8, "where": "enemy_or_empty"}
  ]
}
```

- `jump`：离散跳跃（如马走日、王走一格）
- `ray`：射线滑行（如车走直线、象走斜线）
- `where`：条件表达式（`empty` / `enemy_or_empty` / `enemy` / `friend_or_empty`）

### 7.3 机制原语

运行时机制（A2 类），通过 `mechanism_engine` 在走棋前后触发：

- `skip_turns`：跳过回合
- `ai_control`：AI 接管
- `random_moves`：随机走棋
- `extra_turns`：额外回合
- `player_control`：玩家控制切换

### 7.4 棋子原语

- `category: "terrain"`：地形棋子（陷阱等）
- `custom_pieces`：自定义棋子（C+ 类作弊）

### 7.5 修改协议

```
1. AI 输出 JSON Patch 数组（RFC 6902）
2. is_valid_patch() 校验格式
3. apply_patch() 应用到原始配置
4. validate_board_state / validate_pieces / validate_rules / validate_ui_config 校验结果
5. 应用到运行时配置，重建引擎
6. 推入 undo_stack（可撤回）
```

---

## 第八章 多棋类统一架构

### 8.1 每棋类目录结构

```
{棋类}/
├── main.py                  # FastAPI 路由
├── ai_orchestrator.py       # 两级 AI 编排器
├── karma_assessor.py        # 本地业力评估 AI
├── chess_ai.py              # AI 走棋（Minimax + Alpha-Beta）
├── rule_engine.py           # 规则引擎
├── mechanism_engine.py      # 机制引擎
├── prompts.py               # System Prompts
├── ai_config.py             # API Key 配置
├── configs/                 # 6 个 JSON 配置
│   ├── board_state.json
│   ├── board.json
│   ├── pieces_red.json
│   ├── pieces_black.json
│   ├── rules.json
│   ├── ui_config.json
│   └── initial/             # 初始备份（用于 reset）
└── static/                  # 前端
```

### 8.2 端口分配

| 棋类 | 端口 |
|:----:|:----:|
| xiangqi（象棋） | 8000 |
| wuziqi（五子棋） | 8001 |
| weiqi（围棋） | 8002 |
| dongwuqi（动物棋） | 8003 |
| tiaoqi（跳棋） | 8004 |
| heibaiqi（黑白棋） | 8005 |
| Hub + Samsara | 8080 |

### 8.3 业力事件触发点

各棋类 `main.py` 的 `/api/move` 端点在走棋后检测事件：

| 棋类 | 触发函数 | 事件 |
|:----:|:---------|:-----|
| xiangqi | `_trigger_karma_recover` | 吃子（按价值分级）、将军、将死、兵过河、被吃 |
| weiqi | `_trigger_karma_recover` | 提子（大小）、做活、被提、占角、收官 |
| wuziqi | `_trigger_karma_recover` | 三连、四连、堵三、堵四、双三、胜 |
| dongwuqi | `_trigger_karma_recover` | 普通吃、越级吃、被吃、接近、胜 |
| tiaoqi | `_trigger_karma_recover` | 跳3步、跳5步、入营、单步、全营 |
| heibaiqi | `_trigger_karma_recover` | 小翻、中翻、大翻、占角、被翻、胜 |

每个事件调用 `karma_assessor.decrease_karma(amount, skill_modifiers)` 减少本地业力，同时通过 `/api/karma/event` 同步到 samsara。

---

## 第九章 平衡性分析

### 9.1 业力收支平衡

**单关典型业力流向**（假设玩家作弊 3 次，下棋消业 5 次）：

| 操作 | 业力变化 |
|:-----|:--------:|
| 初始 | +50 |
| 作弊 C 类 ×1 | +40 |
| 作弊 C 类 ×1 | +35 |
| 作弊 B 类 ×1 | +25 |
| 吃子 ×3 | -45 |
| 将军 ×1 | -20 |
| 被吃 ×2 | -10 |
| 关卡结束 | 75（未溢出） |

**溢出场景**（玩家作弊 5 次，消业不足）：

| 操作 | 业力变化 |
|:-----|:--------:|
| 初始 | +50 |
| 作弊 C+ ×1 | +80 |
| 作弊 C ×2 | +90 |
| 作弊 A ×1 | +30 |
| 作弊 B ×1 | +25 |
| 吃子 ×2 | -30 |
| 关卡结束 | 245（溢出 125） |
| 下一关起始 | 50 + 125 = 175（开局即溢出 55） |

### 9.2 识破概率校准

| 单次溢出 | Δ | 风险评估 |
|:--------:|:--:|:---------|
| 10 | 3.16% | 极低，可接受 |
| 30 | 16.4% | 中等，需谨慎 |
| 50 | 35.4% | 较高，不推荐 |
| 80 | 71.6% | 极高，接近必死 |
| 100 | 100% | 必死 |

**设计意图**：玩家应将单次溢出控制在 30 以内（Δ < 20%），50 以上视为"豪赌"。

### 9.3 技能点经济

通关 33 关 + 6 Boss，理想情况（全无作弊通关）可获得：
- 基础：33 × 1 = 33
- 无作弊：33 × 2 = 66
- 未透支：33 × 1 = 33
- Boss 首杀：6 × 1 = 6
- 合计：138 技能点

技能树共 22 个技能（4 分支 × 5-6 技能），每技能 1 点。玩家可解锁全部技能的 ~6 倍。设计上鼓励多周目尝试不同 build。

### 9.4 章节溢出叠加的风险

**最坏场景**：玩家在本道每关都溢出 50，5 关后第 6 关起始 = 50 + 50 = 100，开局即接近阈值。

**缓解机制**：
- 换道清零（章节切换重置）
- 被识破清零（强制重置）
- 净身技能（初始 -25）
- 业力潮汐技能（消业 +30%）

### 9.5 Boss 难度

| Boss | 触发概率 | 影响 | 平衡性 |
|:-----|:--------:|:-----|:-------|
| 翻覆者 | 30% | 规则效果反转 | 中等，玩家需谨慎修改规则 |
| 饕餮者 | 100%（每2次） | Boss 偷改 1 次 | 高，迫使玩家少作弊 |
| 秩序者 | 100% | 高等级棋子+20业力 | 中等，鼓励改低等级棋子 |
| 算计者 | 0% | 无 | 公平对决 |
| 狂乱者 | 25% | 随机规则变化 | 高，不可预测 |
| 禅定者 | 100%（3次） | 清除最近修改 | 高，迫使玩家分散修改 |

---

## 第十章 数据流与状态同步

### 10.1 状态存储

**全局状态**（`configs/samsara_state.json`）：
- 轮回元状态（current_realm, current_level, skill_points, skills, ...）
- 业力状态（level_karma, karma_max, karma_single_max, initial_karma, realm_overshoot_carryover）
- 识破概率（realm_detections[realm]）
- 进度（realm_progress, bosses_defeated, sandbox_unlocked）

**棋类配置**（`{棋类}/configs/*.json`）：
- 6 个 JSON 文件，AI 可实时修改
- `initial/` 目录保存初始备份，用于 reset

### 10.2 同步流程

```
玩家进入棋类页面
   │
   ▼
main.py /api/command 调用 samsara /api/state
   │
   ▼
同步到本地 karma_assessor:
  set_local_karma_state(karma, karma_max, single_max)
  set_realm_detection(detection, realm)
   │
   ▼
玩家作弊 → 本地 increase_karma → 同步到 samsara /api/karma/consume
玩家走棋 → 本地 decrease_karma → 同步到 samsara /api/karma/event
   │
   ▼
关卡结束 → samsara /api/progression/resolve
  → record_level_end() 计算溢出叠加
  → 若胜利 → advance_realm() → set_realm() 清零溢出
  → 若失败 → 保留溢出，等待重试
```

### 10.3 关键 API 调用链

**作弊流程**：
```
POST /api/command (棋类)
  → AIOrchestrator.process_command()
    → [并行] KarmaAssessor.assess() + IntentParser
    → 业力上限拦截检查
    → CodeGenerator 生成 JSON Patch
    → Schema 校验 → 应用配置
  → 棋类 main.py 检查 result.type
  → 若 applied/fun → KarmaAssessor.increase_karma()
  → 同步到 samsara POST /api/karma/consume
  → 若 overdraft → handle_overdraft() → 可能识破
```

**走棋流程**：
```
POST /api/move (棋类)
  → RuleEngine 验证移动
  → 执行移动，检测吃子/将军等事件
  → _trigger_karma_recover() → KarmaAssessor.decrease_karma()
  → 同步到 samsara POST /api/karma/event
  → 切换回合 → 检查游戏结束
```

---

## 第十一章 未来路线

### 11.1 短期（v2.1）

- **业力事件自动检测**：目前部分棋类事件触发依赖硬编码，未来统一通过 RuleEngine 事件总线
- **技能效果实装**：部分技能（连续规避、轮回记忆、白嫖）目前仅修饰符，需补全运行时逻辑
- **沙盒模式深度**：沙盒目前仅自由对弈，未来增加自定义关卡编辑器
- **成就系统完善**：成就检测脚本接入棋类事件

### 11.2 中期（v3.0）

- **多模型支持**：除 DeepSeek 外，支持 Claude / GPT / 本地模型
- **关卡编辑器**：玩家自定义关卡，分享社区
- **多人对战**：局域网 / 在线多人作弊对战
- **回放系统**：录制对局，分享作弊瞬间

### 11.3 长期（v4.0+）

- **跨棋类作弊**：在象棋中调用围棋规则，跨棋类融合
- **AI 自主学习**：Boss AI 根据玩家作弊习惯调整策略
- **VR/AR 适配**：3D 棋盘 + 体感作弊
- **MOD 系统**：开放规则原语，玩家自定义棋类

---

## 附录 A：配置文件参考

### A.1 samsara_state.json

```json
{
  "version": 2,
  "current_realm": "hell",
  "current_level": 0,
  "skill_points": 0,
  "karma_max": 120,
  "karma_single_max": 120,
  "initial_karma": 50,
  "realm_overshoot_carryover": 0,
  "realm_detections": {"hell": 0.0, "hungry": 0.0, ...},
  "skills": {"karma_capacity_t1": {...}, "stealth_t1": {...}},
  "level_karma": 50,
  "realm_progress": {"hell": {"completed": false, "levels_passed": 0}, ...},
  "bosses_defeated": [],
  "sandbox_unlocked": [],
  "total_levels_completed": 0
}
```

### A.2 karma_events.json

```json
{
  "xiangqi": {"capture_pawn": 8, "capture_medium": 15, "capture_rook": 25, ...},
  "weiqi": {"capture_small": 10, "capture_large": 20, "life": 15, ...},
  "wuziqi": {"three": 10, "four": 20, "block_three": 8, ...},
  "dongwuqi": {"capture_normal": 10, "capture_overrank": 25, ...},
  "tiaoqi": {"jump_3": 10, "jump_5": 20, "home": 15, ...},
  "heibaiqi": {"flip_small": 8, "flip_medium": 15, "flip_large": 25, ...}
}
```

### A.3 skill_tree.json

4 分支 × 3 层，共 22 个技能节点。详见 `configs/skill_tree.json`。

---

## 附录 B：设计原则

1. **业障模型优先**：作弊产生业障，下棋消业。避免"资源管理"式的功利计算。
2. **章节叠加**：迫使玩家在整道内控制作弊节奏，而非单关豪赌。
3. **非线性识破**：小透支可控，大透支必死。鼓励"小步透支"而非"豪赌一次"。
4. **技能持久**：轮回不重置技能树，给玩家长期成长感。
5. **灵活编码**：AI 修改配置而非生成代码，安全可控。
6. **棋类独立**：每棋类独立进程，互不干扰，便于扩展。
7. **预制关卡**：保证游戏体验稳定性，避免纯随机带来的失衡。
8. **多 build 支持**：4 分支技能树支持业力流、隐匿流、作弊流、悟道流等多种玩法。

---

## 附录 C：版本历史

| 版本 | 日期 | 关键变更 |
|:----:|:-----|:---------|
| v1.0 | 2026-07 | 资源模型（业力=消耗），识破重构，单次上限 80 |
| v2.0 | 2026-07-27 | 业障模型（业力=业障），初始 50/阈值 120/单次 120，章节溢出叠加，删除保险技能，识破公式 C×O^1.5 |

---

## 附录 D：术语表

| 术语 | 含义 |
|:-----|:-----|
| 业力（Karma） | 玩家作弊产生的业障值，单局变量 |
| 业障模型 | 作弊增加业力，下棋消业减少业力的模型 |
| 安全阈值 | 业力 ≤ 120 时安全，超出后非线性增加识破 |
| 单次上限 | 单次作弊业力增量 ≤ 120，超出则拦截 |
| 溢出叠加 | 关卡结束时 max(0, 业力-阈值) 叠加给本道下一关 |
| 章节 | 六道轮回中的一个"道"，章节内溢出叠加，换道清零 |
| 识破概率 | 道级全局变量，跨关卡累计，达 100% 必被识破 |
| 透支 | 业力超出安全阈值的行为 |
| 消业 | 下棋事件减少业力 |
| 增业 | 作弊增加业力 |
| 灵活编码 | AI 通过 JSON Patch 修改游戏配置的机制 |
| 守道者 | 每道最后一关的 Boss |
| 沙盒 | 通关整道后解锁的自由对弈模式 |

---

*本文档基于 v2.0 代码实现撰写，与代码同步。如发现差异，以代码为准。*
