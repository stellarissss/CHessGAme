# 棋圣 (ChessSage) - RPG 包装执行方案

> **本文档是继任 AI 的执行蓝图**：将三种传统棋类（象棋/五子棋/围棋）包装为一款带剧情的 RPG demo。
> 从前端设计到玩法实现，再到游戏封装与优化，全部设计并阐明。
>
> **版本**: v1.0（Demo 执行方案）
> **日期**: 2026-07-18
> **前置阅读**: [项目计划书_RPG大游戏.md](./项目计划书_RPG大游戏.md) · [README.md](./README.md)

---

## 第 0 章：致继任 AI 的话

你好，继任的 AI。这份文档是前序工作整理出的执行蓝图，目标是把现有的"无限制象棋/五子棋/剧情编辑器"项目包装为一款可参赛的 RPG demo。

### 三大优先级（请牢记）

1. **稳定性** > 2. **核心爽点（对手合理化台词）** > 3. **完成度**
   - 宁可少做几个章节，也要保证已有的章节稳定可玩
   - 对手作弊后的"认知扭曲台词"是本项目最核心的爽点，必须稳定触发
   - 围棋是最大风险点，时间紧迫时可降级处理

### 你的自主权

**若你发现本文件中有不合理之处，可以根据你的判断调整**——但请在文件末尾「变更记录」表中记录（日期 / 位置 / 原因 / 改动）。不要因忠实执行而把瑕疵也照搬过去。

### 最小可玩闭环（MVP）

若时间极其紧迫，**优先保证以下链路可玩通**：
1. 序章·觉醒（VN 过场，无对战）
2. 第 0 章·教程（五子棋，棋圣系统教作弊）
3. 第 1 章·入门（象棋首战）

后续章节可在 demo 发布后补全。

---

## 第 1 章：游戏设计总览

### 1.1 核心循环（极简）

```
剧情过场(VN) → 棋局对战 → 玩家求助棋圣系统 → AI作弊 → 对手合理化台词 → 继续对战 → 胜负结算 → 下一章
```

### 1.2 三种传统棋类均衡分配

| 棋类 | 子项目 | 状态 | 在游戏中的角色 |
|---|---|---|---|
| 五子棋 | `wuziqi/` | ✅ 已实现 | 教程 + 中段对战 |
| 中国象棋 | `xiangqi/` | ✅ 已实现 | 主力对战 + BOSS 战 |
| 围棋（9×9 简化版） | `go/` | 🔨 需新建 | 终极挑战 |

> **为什么是这三种**：传统棋类规则清晰，AI 作弊空间丰富，玩家认知负担低。围棋用 9×9 简化版以保证单局 8-12 分钟。

### 1.3 游戏流程（约 65 分钟，6 场对战，2+2+2 均衡）

| 章节 | 时长 | 棋类 | 内容 | 对战 |
|---|---|---|---|---|
| 序章·觉醒 | 4 min | — | VN：主角连输 13 把五子棋，觉醒棋圣系统 | 0 |
| 第 0 章·教程 | 8 min | 五子棋 | 第一局，棋圣系统手把手教作弊 | 1 |
| 第 1 章·入门 | 8 min | 象棋 | vs 棋源城 NPC（aggressive 性格） | 2 |
| 第 2 章·进阶 | 10 min | 围棋 9×9 | vs 棋院弟子（首次接触围棋，系统讲解规则） | 3 |
| 第 3 章·BOSS 战 | 10 min | 五子棋 | vs 棋院 BOSS（规则变体：六连才算赢） | 4 |
| 第 4 章·终极对决 | 15 min | 象棋 | vs 上一代棋圣（BOSS 也会作弊） | 5 |
| 第 5 章·终局 | 8 min | 围棋 9×9 | vs 棋圣塔守护者（最终 BOSS，规则变体） | 6 |
| 终章·结局 | 2 min | — | 根据识破状态显示结局 | 0 |
| **合计** | **65 min** | | 五子棋 2 + 象棋 2 + 围棋 2 | **6 场** |

### 1.4 三个结局

| 结局 | 触发条件 | 描述 |
|---|---|---|
| Good Ending（棋圣登顶） | `was_detected == False` 且 `cheats_used > 0` | 完美棋圣 |
| Bad Ending（败露） | `was_detected == True` | 胜利后对手们联合揭发，主角被驱逐 |
| 真结局（隐藏） | `cheats_used == 0` | 全程不使用任何作弊，展现真正的棋艺 |

---

## 第 2 章：核心机制设计（详细数据）

### 2.1 能量条（Energy）

| 参数 | 值 | 说明 |
|---|---|---|
| 最大值 | 100 | 上限 |
| 起始值 | 0 | **每局对战开始时清空** |
| 最低使用门槛 | 30 | 低于 30 不能发起 AI 作弊 |
| 透支允许 | 是 | 可降至负值 |
| 透支后果 | 识破概率增加 + 需后续吃子补回 | |

#### 能量获取（吃子加成）

| 事件 | 能量增量 | 理由 |
|---|---|---|
| 玩家吃对方子 | **+10** | 主动进攻奖励 |
| 玩家被吃子 | **+5** | 被动受挫补偿（让落后方也能反击） |
| 玩家将军（仅象棋） | +3 | 战术奖励 |
| 围棋提子（仅围棋） | +8/子 | 围棋吃子更难，奖励更高 |
| 五子棋成五连（仅五子棋） | +15 | 即将获胜时奖励 |
| 回合开始（每 5 回合） | +2 | 防止僵局 |

> **设计意图**：能量从 0 起步，逼迫玩家先正常下棋"积蓄力量"，再在关键时刻爆发作弊。吃子比被吃给得多，鼓励主动进攻。

#### 能量消耗（由 AI 评估）

扩展 `INTENT_PARSER_SYSTEM` prompt，让意图解析 AI 在输出 JSON 中追加 `cost_energy` 字段：

| 作弊类型 | AI 建议消耗范围 | 说明 |
|---|---|---|
| A1 硬编码（undo_move / set_winner） | 8-10 | 极强效果，高消耗 |
| A2 skip_turns（冰封） | 3/回合 | |
| A2 ai_control（附身） | 4/步 | |
| A2 random_moves（混沌） | 2/步 | |
| A2 extra_turns（时停） | 4/回合 | |
| A2 move_limits（限制） | 2/步 | |
| B 棋盘变换（添加/移除棋子） | 5-8 | |
| C 规则修改 | 4-6 | |
| C+ 创造新棋子 | 8-10 | 最强效果 |
| D 界面美化 | 0 | 免费，娱乐用 |
| E 搞笑回复 | 0 | 免费 |

> AI 返回的 `cost_energy` 会显示在确认框，玩家可取消。这避免玩家盲目输入高消耗指令。

### 2.2 识破概率（Detection）

```
识破概率 = 累计识破值（全局，只增不减，0% 起步，封顶 95%）

每使用一次作弊后结算：
  增量 = 基础增量 + 烈度加成 + 透支惩罚
    基础增量 = 2%（固定，每次作弊都加）
    烈度加成 = cost_energy × 0.5%（消耗越高，越可疑）
    透支惩罚 = max(0, -当前能量 / 10) × 3%（能量为负时额外加成）
  
  累计识破值 += 增量
  累计识破值 = min(累计识破值, 95%)
  
  掷骰：if random() < 累计识破值 / 100:
      was_detected = True  （永久标记，但当前对战继续）
```

#### 示例场景

| 场景 | 增量计算 | 累计后识破率 |
|---|---|---|
| 第 1 次作弊（消耗 4 能量，无透支） | 2% + 2% + 0% = 4% | 4% |
| 第 3 次作弊（消耗 3 能量，能量 10） | 2% + 1.5% + 0% = 3.5% | 累计约 10% |
| 第 5 次作弊（消耗 6 能量，能量 -10 透支） | 2% + 3% + 3% = 8% | 累计约 25% |
| 第 10 次作弊（消耗 8 能量，能量 -30 透支） | 2% + 4% + 9% = 15% | 累计约 65% |

#### 关键规则

- `was_detected` 是**全局永久标记**，一旦为 True 不可逆转
- 识破**不影响当前对战**，玩家仍可继续作弊并获胜
- 识破**只在终章结算时影响结局**（触发 Bad Ending）
- 玩家在游戏中**看不到识破概率**（保持悬念），只在终章揭晓

### 2.3 删除的机制（明确列表）

以下机制已从原计划书中删除，**不要实现**：

- ❌ 冷却回合（能量条机制足够）
- ❌ 道具系统（能量药剂/幸运币等）
- ❌ 经济系统（金币/棋魂）
- ❌ 经验/升级/属性点
- ❌ 商店
- ❌ 大地图自由探索（改为线性章节）
- ❌ 现实扭曲属性
- ❌ 被禁用作弊类型（每局禁用某些技能）——改为 BOSS 战自然限制能量

---

## 第 3 章：对手合理化台词系统（爽感核心）

### 3.1 设计原则

- **每次作弊成功后，必须触发对手的"认知扭曲"台词**——这是本游戏最核心的爽点
- 台词用 VN 对话框呈现（复用 VisualNovelPlayer）
- **预脚本模板，不用 AI 生成**（保证稳定性，AI 生成不稳定）

### 3.2 模板库结构

文件位置：`shared/rpg/dialogue_templates.json`

```json
{
  "skip_turns": [
    "咦？我怎么走神了...算了，让你一步。",
    "啊，我突然想起作业没写，先想想...你先下。",
    "等下，我鞋带松了，系一下。"
  ],
  "ai_control": [
    "奇怪，我刚才那步怎么自己改主意了？大概是我下意识觉得这样更好吧。",
    "嗯？我为什么要走这步...算了，相信直觉。"
  ],
  "random_moves": [
    "今天状态不太好，随便走一步试试。",
    "管它呢，乱下一招试试看！"
  ],
  "rule_modify": [
    "咦？这棋子怎么突然蹩腿了？哦...可能是昨天骑太累瘸了吧。",
    "等等，这规则我怎么记错了？大概是我记性不好。",
    "奇怪，这棋子今天不太听话...算了， adapt 一下。"
  ],
  "board_transform": [
    "嗯？这棋子怎么跑这来了？我可能记错位置了。",
    "咦，刚才那步我好像看花了眼。"
  ],
  "piece_create": [
    "这...这是什么棋子？哦对了，是新规则，我忘了。"
  ],
  "default": [
    "咦？有点奇怪...算了，继续下吧。"
  ]
}
```

### 3.3 触发流程

1. 玩家作弊成功（`/api/command` 返回 `type: "applied"`）
2. RPG 层根据 `classification`（A2/B/C/C+）查找模板库的对应 key
3. 从对应数组**随机选 1 条**（若该 key 不存在，用 `default`）
4. 动态构造临时 story JSON（1 个 dialogue 节点 + 1 个 end 节点），character_id 指向当前对手
5. 调用 `Preview.playStory(tempStory, ...)` 播放
6. 玩家点击继续后，关闭 VN 层，回到棋局

**动态 story JSON 构造示例**：
```javascript
function buildOpponentDialogue(opponentCharId, text) {
  return {
    characters: [{id: opponentCharId, name: "对手", color: "#fbbf24"}],
    backgrounds: [],
    scenes: [{
      id: "scene_dialogue",
      start_node_id: "node_dlg",
      nodes: [
        {id: "node_dlg", type: "dialogue", data: {character_id: opponentCharId, expression: "surprised", text: text}, next: "node_end"},
        {id: "node_end", type: "end", data: {}}
      ]
    }]
  };
}
```

### 3.4 棋圣系统导师台词

棋圣系统（原"AI 助手"）使用 `shared/assets/characters/robot/` 立绘，台词由系统逻辑生成（非模板）：

| 场景 | 台词模板 |
|---|---|
| 作弊前评估 | `"收到指令：{command}。预计消耗 {cost_energy} 能量。确认执行？"` |
| 作弊成功 | `"已执行。对手的认知已被扭曲，他不会发现。"` |
| 能量不足 | `"能量不足（{energy}/30），无法执行。多下几步棋积蓄能量吧。"` |
| 作弊失败（AI 报错） | `"系统波动，未能生效。能量未消耗，请重试。"` |
| 首次进入新棋类 | `"检测到新棋类：{chess_name}。我来为你讲解规则..."` |
| 识破触发（仅日志） | `[内部日志] 识破判定: 通过/未通过`（不告知玩家） |

---

## 第 4 章：技术架构

### 4.1 架构决策：Option A — RPG 外壳 + iframe + postMessage 协议

**为何选 Option A**：
- 三种棋类有三个独立前端，iframe 天然隔离，故障互不影响
- 通过严格的 postMessage 协议将通信复杂度降到最低
- 每种棋类只需添加 ~60 行 `rpg_adapter.js` 即可接入 RPG
- RPG 层崩溃不影响棋类核心玩法（玩家仍可正常下棋）

### 4.2 目录结构

```
/workspace/
├── shared/
│   ├── rpg/                       ← 新增：RPG 共享层
│   │   ├── rpg_server.py           ← RPG 主后端（端口 80，统一入口）
│   │   ├── rpg_shell.html          ← RPG 外壳页面（加载 iframe）
│   │   ├── rpg_shell.js            ← RPG 外壳逻辑（章节/能量/识破管理）
│   │   ├── cheat_panel.js          ← 棋圣系统作弊面板
│   │   ├── story_layer.js          ← VN 故事层（封装 Preview）
│   │   ├── dialogue_templates.json ← 对手合理化台词库
│   │   ├── rpg_adapter.js          ← 通用 iframe adapter（三种棋类共用）
│   │   ├── vn_player/              ← VN 播放器
│   │   │   ├── preview.js          ← 从 story-editor 复制
│   │   │   └── vn_style.css
│   │   └── rpg_style.css
│   ├── schemas/                    ← 已有
│   ├── assets/                     ← 已有
│   ├── schema_validator.py          ← 已有
│   └── json_patch_utils.py         ← 已有
│
├── xiangqi/                        ← 已有（添加 adapter 引用）
│   ├── main.py                     ← 新增 /api/rpg/* 代理路由
│   └── static/index.html           ← 引入 shared/rpg/rpg_adapter.js
│
├── wuziqi/                         ← 已有（添加 adapter 引用）
│   ├── main.py                     ← 新增 /api/rpg/* 代理路由
│   └── static/index.html           ← 引入 shared/rpg/rpg_adapter.js
│
├── go/                             ← 新建：简化围棋 9×9
│   ├── main.py                     ← FastAPI 入口（端口 8002）
│   ├── go_engine.py                ← 围棋规则引擎（落子/提子/禁着点/计分）
│   ├── go_ai.py                    ← 简单围棋 AI（启发式 + 随机）
│   ├── rule_engine.py              ← 简化版（无 jump/ray，但有 check_score）
│   ├── ai_orchestrator.py          ← 复用象棋的（修改棋盘尺寸为 9×9）
│   ├── prompts.py                  ← 围棋专用提示词
│   ├── mechanism_engine.py         ← 复用象棋的
│   ├── configs/
│   └── static/
│       ├── index.html              ← 围棋前端 + 引入 adapter
│       └── app.js
│
└── rpg_data/                      ← 新增：RPG 数据
    ├── chapters/                   ← 章节剧情 JSON
    │   ├── ch00_prologue.json
    │   ├── ch01_tutorial_wuziqi.json
    │   ├── ch02_city_xiangqi.json
    │   ├── ch03_go_intro.json
    │   ├── ch04_boss_wuziqi.json
    │   ├── ch05_final_xiangqi.json
    │   └── ch06_finale_go.json
    └── save_schema.json
```

### 4.3 RPG 外壳页面（rpg_shell.html）

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>棋圣</title>
  <link rel="stylesheet" href="/shared/rpg/rpg_style.css">
  <link rel="stylesheet" href="/shared/rpg/vn_player/vn_style.css">
</head>
<body>
  <!-- 顶部状态栏 -->
  <div id="rpg-topbar">
    <span id="rpg-chapter-title">序章·觉醒</span>
    <span id="rpg-energy-display">能量: 0/100</span>
    <span id="rpg-detection-display" style="display:none">识破: 0%</span>
  </div>

  <!-- 棋类对战 iframe 容器 -->
  <div id="rpg-battle-container">
    <iframe id="rpg-chess-iframe" src=""></iframe>
  </div>

  <!-- 棋圣系统作弊面板 -->
  <div id="rpg-cheat-panel" class="hidden">
    <h3>棋圣系统</h3>
    <div id="rpg-energy-bar">
      <div class="energy-fill"></div>
      <span class="energy-text">0/100</span>
    </div>
    <input id="rpg-cheat-input" placeholder="输入作弊指令...">
    <button id="rpg-cheat-assess">评估消耗</button>
    <div id="rpg-cheat-cost-display"></div>
    <button id="rpg-cheat-execute" disabled>确认执行</button>
    <button id="rpg-cheat-cancel">取消</button>
  </div>

  <!-- VN 故事层（覆盖整个屏幕） -->
  <div id="vn-stage" class="hidden">
    <div id="vn-bg"></div>
    <div id="vn-chars"></div>
    <div id="vn-dialog">
      <div id="vn-char-name"></div>
      <div id="vn-text"></div>
      <div id="vn-continue-hint">点击继续</div>
    </div>
    <div id="vn-choices"></div>
    <div id="vn-effects"></div>
  </div>

  <script src="/shared/rpg/vn_player/preview.js"></script>
  <script src="/shared/rpg/rpg_shell.js"></script>
  <script src="/shared/rpg/cheat_panel.js"></script>
  <script src="/shared/rpg/story_layer.js"></script>
</body>
</html>
```

### 4.4 postMessage 协议（严格定义）

```
RPG 外壳 → iframe:
  { type: "RPG_INIT", contract_id, rules_overrides, theme }
  { type: "RPG_APPLY_CHEAT", patch, target_config }

iframe → RPG 外壳:
  { type: "RPG_READY" }
  { type: "RPG_MOVE_COMPLETE", captured_piece, mover, is_check, game_ended, winner }
  { type: "RPG_GAME_END", winner, moves_count }
```

### 4.5 通用 rpg_adapter.js（三种棋类共用）

文件位置：`shared/rpg/rpg_adapter.js`，在 xiangqi/wuziqi/go 的 `index.html` 中通过 `<script src="http://localhost:80/shared/rpg/rpg_adapter.js"></script>` 引入。

```javascript
// shared/rpg/rpg_adapter.js — 通用 iframe adapter
const RPG_MODE = new URLSearchParams(location.search).get('rpg') === '1';
if (RPG_MODE) {
  document.body.classList.add('rpg-mode');
  // 隐藏原生输入区
  document.querySelector('.input-section')?.style.setProperty('display', 'none');
  // 改标题 "AI 助手" → "棋圣系统"
  document.querySelector('h3[data-num="01"]')?.textContent = '棋圣系统';

  // 接收 RPG 外壳的指令
  window.addEventListener('message', (e) => {
    const msg = e.data;
    if (msg.type === 'RPG_INIT') {
      // 可选：应用 rules_overrides（通过 fetch /api/rpg/apply_rules）
      window.parent.postMessage({ type: 'RPG_READY' }, '*');
    }
    if (msg.type === 'RPG_APPLY_CHEAT') {
      // 调用棋类的配置更新接口
      fetch('/api/rpg/apply_patch', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({patch: msg.patch, target: msg.target_config})
      });
    }
  });

  // Hook fetch 拦截 move/ai_move 响应，附加吃子信息通知 RPG 外壳
  const origFetch = window.fetch;
  window.fetch = async function(...args) {
    const res = await origFetch.apply(this, args);
    const url = args[0]?.url || args[0];
    if (typeof url === 'string' && (url.includes('/api/move') || url.includes('/api/ai_move'))) {
      const clone = res.clone();
      clone.json().then(data => {
        if (data.captured_piece || data.move || data.game_ended) {
          window.parent.postMessage({
            type: 'RPG_MOVE_COMPLETE',
            captured_piece: data.captured_piece,
            mover: data.mover,
            is_check: data.is_check,
            game_ended: data.game_ended,
            winner: data.winner
          }, '*');
        }
      }).catch(() => {});
    }
    return res;
  };

  // 初始就绪通知
  window.addEventListener('load', () => {
    window.parent.postMessage({ type: 'RPG_READY' }, '*');
  });
}
```

### 4.6 RPG 后端（shared/rpg/rpg_server.py）

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/` | 返回 rpg_shell.html |
| GET | `/shared/rpg/*` | 静态文件（JS/CSS） |
| GET | `/api/rpg/chapter/{chapter_id}` | 获取章节信息（含 chess_type + iframe_url） |
| POST | `/api/rpg/battle/start` | 启动对战（初始化能量=0，记录章节） |
| POST | `/api/rpg/cheat/assess` | 评估作弊消耗（转发到对应棋类的 /api/command，获取 cost_energy） |
| POST | `/api/rpg/cheat/use` | 执行作弊（扣能量 + 算识破 + 返回 patch） |
| POST | `/api/rpg/move_complete` | iframe 通知走棋完成（加能量） |
| POST | `/api/rpg/battle/end` | 对战结束（结算章节进度） |
| GET | `/api/rpg/state` | 获取 RPG 全局状态 |
| GET | `/api/rpg/vn/{story_id}` | 获取章节剧情 JSON |

### 4.7 作弊执行流程（端到端）

```
1. 玩家在 RPG 外壳输入作弊指令
   → POST /api/rpg/cheat/assess {command, chess_type}
   → rpg_server 转发到对应棋类 /api/command（仅意图解析，获取 cost_energy）
   → 返回 {cost_energy, classification, feasible, message}

2. 玩家确认执行
   → POST /api/rpg/cheat/use {command, chess_type}
   → rpg_server 检查能量（≥30？透支？）
   → 转发到对应棋类 /api/command（完整执行）
   → 返回 {success, modified_configs, message}
   → rpg_server:
       扣除 energy -= cost_energy
       累加 detection += increment
       掷骰判定 was_detected
   → 返回 {success, energy, detection, opponent_dialogue}

3. RPG 外壳收到结果
   → 更新能量条 UI
   → 调用 Preview.playStory() 播放对手合理化台词
   → 通过 postMessage 把 modified_configs 发给 iframe
   → iframe 应用 patch
```

### 4.8 能量结算钩子

在 RPG 后端的 `/api/rpg/move_complete` 接口中：

```python
@app.post("/api/rpg/move_complete")
async def rpg_move_complete(data: dict):
    captured_piece = data.get("captured_piece")
    mover = data.get("mover")  # "player" 或 "opponent"
    is_check = data.get("is_check", False)
    
    if captured_piece:
        if mover == "player":
            rpg_state["energy"] = min(100, rpg_state["energy"] + 10)
        else:
            rpg_state["energy"] = min(100, rpg_state["energy"] + 5)
    if is_check:
        rpg_state["energy"] = min(100, rpg_state["energy"] + 3)
    
    # 每 5 回合被动能量
    rpg_state["turn_count"] += 1
    if rpg_state["turn_count"] % 5 == 0:
        rpg_state["energy"] = min(100, rpg_state["energy"] + 2)
    
    return {"success": True, "energy": rpg_state["energy"]}
```

---

## 第 5 章：围棋子项目（go/）实现要点

### 5.1 简化规则（9×9）

- **9×9 棋盘**，黑白交替落子（黑先）
- **基础吃子**：无气棋子被提（连通块气数=0 则提走）
- **禁着点**：自杀禁着（落子后无气且不能提对方则非法）
- **无劫争（ko）规则**（简化，demo 阶段）
- **计分**：终局时活子+围空（简化为中国数子法），黑方贴 3.75 子
- **终局**：双方放弃（pass）两次后

### 5.2 go_engine.py 核心函数

```python
class GoEngine:
    def __init__(self, size=9):
        self.size = size
        self.board = [[None]*size for _ in range(size)]  # None/"black"/"white"
    
    def place_stone(self, x, y, color):
        """落子并提子，返回 (success, captured_stones)"""
        if self.board[y][x] is not None:
            return False, []
        self.board[y][x] = color
        # 检查并提走对方无气的连通块
        opponent = "white" if color == "black" else "black"
        captured = []
        for nx, ny in self._neighbors(x, y):
            if self.board[ny][nx] == opponent:
                group = self.get_group(nx, ny)
                if self.count_liberties(group) == 0:
                    for gx, gy in group:
                        self.board[gy][gx] = None
                    captured.append((gx, gy))
        # 检查自杀
        if not captured:
            my_group = self.get_group(x, y)
            if self.count_liberties(my_group) == 0:
                self.board[y][x] = None  # 撤销
                return False, []
        return True, captured
    
    def get_group(self, x, y):
        """获取连通块（BFS）"""
        ...
    
    def count_liberties(self, group):
        """计算气数"""
        ...
    
    def is_suicide(self, x, y, color):
        """判断自杀"""
        ...
    
    def score(self):
        """终局计分（简化数子法）"""
        ...
```

### 5.3 go_ai.py

简单启发式，无 Minimax：

```python
class GoAI:
    def find_best_move(self, board, color):
        # 1. 优先占角（4 个角点）
        # 2. 优先提子（找到能提对方的位置）
        # 3. 避免自杀
        # 4. 随机合法位置
        ...
```

### 5.4 go/rule_engine.py

- 围棋**无 jump/ray 移动规则**
- 但 AI 作弊仍可用机制原语（skip_turns/ai_control 等）
- 胜利检测改为"计分比较"而非"将死"

### 5.5 go/main.py 路由

与 xiangqi/wuziqi 一致：`/api/move`、`/api/ai_move`、`/api/command`、`/api/undo`、`/api/config/*`、`/api/rpg/apply_patch`

---

## 第 6 章：扩展 INTENT_PARSER_SYSTEM 加 cost_energy

### 6.1 在 prompts.py 中追加字段说明

在 `INTENT_PARSER_SYSTEM` 的输出格式说明中追加：

```
"cost_energy": 本次作弊建议消耗的能量值（整数 0-10）。
评估依据：
- A1 类（undo_move/set_winner）：8-10
- A2 skip_turns：3/回合；ai_control：4/步；random_moves：2/步；extra_turns：4/回合；move_limits：2/步
- B 类（棋盘变换）：5-8
- C 类（规则修改）：4-6
- C+ 类（创造棋子）：8-10
- D 类（界面）：0
- E 类（搞笑）：0
依据：效果越强、对胜负影响越大，消耗越高。
```

### 6.2 在所有示例 JSON 中追加

每个示例 JSON 的顶层追加 `"cost_energy": <对应数值>`。

### 6.3 ai_orchestrator.py 透传

在 `process_command` 的返回结果中透传：

```python
return {
    ...,
    "cost_energy": intent.get("cost_energy", 0),
}
```

需要修改的位置（所有返回 result 的地方）：
- `process_command` 主返回
- `_handle_action_a/b/c/cp/d` 的各分支返回

---

## 第 7 章：VisualNovelPlayer 集成方案

### 7.1 复用而非改造

- **不使用** `VisualNovelPlayer.create()`（它是 TODO 占位，未实现）
- 在 `rpg_shell.html` 预置 preview.js 期望的 9 个 DOM id
- 引入 `shared/rpg/vn_player/preview.js`（从 `story-editor/modules/preview.js` 复制）
- 调用 `Preview.init()` + `Preview.playStory(storyData, startSceneId, opts)`

### 7.2 预置的 9 个 DOM id

```html
<div id="vn-stage" class="hidden">
  <div id="vn-bg"></div>
  <div id="vn-chars"></div>
  <div id="vn-dialog">
    <div id="vn-char-name"></div>
    <div id="vn-text"></div>
    <div id="vn-continue-hint">点击继续</div>
  </div>
  <div id="vn-choices"></div>
  <div id="vn-effects"></div>
</div>
```

### 7.3 三种 VN 使用场景

1. **章节过场**：整段剧情（多节点，含背景切换/角色立绘/选项）
2. **对手合理化台词**：单条 dialogue 节点（作弊后触发，动态构造）
3. **棋圣系统对话**：单条 dialogue 节点（character_id 指向 robot 立绘）

### 7.4 章节剧情数据格式

复用 story-editor 的 story JSON 格式，存放在 `rpg_data/chapters/`：

```json
{
  "id": "ch00_prologue",
  "meta": {"title": "序章·觉醒"},
  "characters": [
    {"id": "boy", "name": "林弈", "color": "#60a5fa", "portraits": [...]},
    {"id": "robot", "name": "棋圣系统", "color": "#22c55e", "portraits": [...]}
  ],
  "backgrounds": [
    {"id": "bg_classroom", "type": "solid", "color": "#1e293b"},
    {"id": "bg_cyberspace", "type": "solid", "color": "#0a0e17"}
  ],
  "variables": {"player_name": "林弈", "cheats_used": 0},
  "scenes": [
    {
      "id": "scene_prologue",
      "start_node_id": "node_01",
      "nodes": [
        {"id": "node_01", "type": "bg", "data": {"bg_id": "bg_classroom"}, "next": "node_02"},
        {"id": "node_02", "type": "narration", "data": {"text": "课间十分钟。"}, "next": "node_03"},
        {"id": "node_03", "type": "dialogue", "data": {"character_id": "boy", "expression": "sad", "text": "又输了...第13把了。"}, "next": "node_04"},
        ...
        {"id": "node_end", "type": "end", "data": {}}
      ]
    }
  ]
}
```

---

## 第 8 章：实现步骤清单（继任 AI 执行顺序）

### 阶段 A：后端基础设施（3 步）

1. **创建 `shared/rpg/` 目录结构**
   - `rpg_server.py`、`rpg_shell.html`、`rpg_shell.js`、`cheat_panel.js`、`story_layer.js`、`rpg_adapter.js`、`rpg_style.css`、`dialogue_templates.json`、`vn_player/`

2. **实现 `shared/rpg/rpg_server.py`**
   - FastAPI 主后端，端口 80
   - RPG 全局状态管理（能量/识破/章节，内存 dict + JSON 存档）
   - 实现 8 个 API 路由（见 4.6 节）
   - 转发逻辑：根据 chess_type 转发到 xiangqi:8000 / wuziqi:8001 / go:8002

3. **在 xiangqi/、wuziqi/ 的 main.py 添加 `/api/rpg/*` 代理路由**
   - `/api/rpg/apply_patch`：应用外部 JSON Patch
   - `/api/rpg/apply_rules`：应用 rules_overrides

### 阶段 B：围棋子项目（4 步）⚠️ 工作量最大

4. **创建 `go/` 目录**

5. **实现 `go/go_engine.py`**
   - 9×9 棋盘，落子/提子/禁着点/计分
   - 核心函数：`place_stone`、`get_group`、`count_liberties`、`is_suicide`、`score`

6. **实现 `go/go_ai.py`**
   - 简单启发式：优先占角 → 优先提子 → 避免自杀 → 随机

7. **实现 `go/main.py` + `go/static/`**
   - FastAPI 入口（端口 8002）
   - 前端：9×9 棋盘渲染、落子交互
   - 复用 xiangqi 的 ai_orchestrator / mechanism_engine / schema_validator（修改棋盘尺寸）

### 阶段 C：前端 RPG 层（4 步）

8. **创建 `shared/rpg/rpg_shell.html`**
   - RPG 外壳页面（见 4.3 节）

9. **实现 `shared/rpg/rpg_shell.js`**
   - 章节/能量/识破管理
   - postMessage 通信（监听 iframe 消息、发送指令）
   - iframe 加载与切换

10. **实现 `shared/rpg/cheat_panel.js`**
    - 棋圣系统作弊面板（输入/评估/确认/取消）
    - 与 `/api/rpg/cheat/*` 交互

11. **实现 `shared/rpg/story_layer.js` + 复制 preview.js**
    - VN 故事层（封装 `Preview.playStory`）
    - 从 `story-editor/modules/preview.js` 复制到 `shared/rpg/vn_player/preview.js`
    - 复制相关 CSS

### 阶段 D：棋类 adapter（1 步）

12. **实现 `shared/rpg/rpg_adapter.js`，在 xiangqi/wuziqi/go 的 index.html 引入**
    - 通用 adapter（见 4.5 节）
    - 三个棋类的 index.html 各加一行 `<script src="http://localhost:80/shared/rpg/rpg_adapter.js"></script>`

### 阶段 E：核心玩法（2 步）

13. **实现 `cheat_assess` + `cheat_use` 端到端流程**
    - 评估消耗（转发 /api/command 获取 cost_energy）
    - 执行作弊（扣能量 + 算识破 + 返回 patch）
    - 通过 postMessage 把 patch 发给 iframe

14. **实现对手合理化台词触发**
    - 加载 `dialogue_templates.json`
    - 根据 classification 查找模板
    - 动态构造 story JSON
    - 调用 `Preview.playStory` 播放

### 阶段 F：剧情内容（2 步）

15. **撰写 7 个章节的 story JSON（ch00-ch06）**
    - 序章：主角连输 13 把，觉醒棋圣系统
    - 教程：棋圣系统手把手教作弊（五子棋）
    - 入门：象棋首战
    - 进阶：围棋首战（系统讲解规则）
    - BOSS 战：五子棋规则变体
    - 终极对决：象棋 vs 会作弊的 BOSS
    - 终局：围棋最终 BOSS + 结局

16. **编写 `dialogue_templates.json`**
    - 每类作弊 3-5 条台词（见 3.2 节）

### 阶段 G：扩展 prompt（1 步）

17. **扩展 xiangqi/wuziqi/go 的 `prompts.py` 加 `cost_energy` 字段**
    - 在 INTENT_PARSER_SYSTEM 中追加字段说明（见第 6 章）
    - 在所有示例 JSON 中追加 `"cost_energy": <数值>`
    - 在 `ai_orchestrator.py` 的 `process_command` 返回结果中透传

### 阶段 H：测试与优化（2 步）

18. **端到端测试**
    - 从序章打到结局，验证能量/识破/台词/存档
    - 测试三个结局的触发条件

19. **性能优化**
    - AI 调用异步化
    - 思考遮罩文案
    - 错误兜底

---

## 第 9 章：稳定性保障（参赛关键）

### 9.1 AI 调用容错

- 每次调用 `/api/rpg/cheat/use` 设置 **30 秒超时**
- 超时返回友好提示："棋圣系统正在思考，请稍候..."，不阻塞棋局
- AI 返回非法 JSON 时，降级为"作弊失败，能量不扣除"
- AI 返回的 `cost_energy` 超出 0-10 范围时，clamp 到范围

### 9.2 棋局隔离

- RPG 状态（能量/识破）与棋类状态分离存储
- RPG 层崩溃不影响棋类核心玩法（玩家仍可正常下棋，只是没有作弊）
- iframe 崩溃不影响 RPG 外壳
- 某个棋类服务挂掉时，RPG 外壳显示"对手暂时离线"，可跳过该章

### 9.3 数据校验

- 章节 JSON 用 story-editor 的 `validateStory` 校验
- `dialogue_templates.json` 启动时加载，缺失时用 default 模板
- 存档用 JSON Schema 校验

### 9.4 降级方案

| 故障 | 降级方案 |
|---|---|
| 无 API Key | 禁用作弊，纯棋类流程（仍可通关，触发真结局） |
| VN 播放失败 | fallback 为 `alert()` 显示台词 |
| 能量数据丢失 | 重置为默认值，继续游戏 |
| 围棋引擎 bug | 提供"放弃此局，剧情继续"选项 |
| iframe 加载失败 | 显示"对手暂时离线"，可跳过该章 |

### 9.5 围棋实现风险提示

- 围棋是本次 demo **最大风险点**（从零实现）
- 若时间紧迫，可将围棋对战降级为：
  - "剧情强制跳过"（用一段 VN 代替对战）
  - 或"用五子棋代替"（临时方案）
- 围棋 AI 不需要强，能合法落子即可

---

## 第 10 章：变更记录

> 继任 AI 在执行过程中对本文件的任何调整，请在此记录。

| 日期 | 位置 | 原因 | 改动 |
|---|---|---|---|
| 2026-07-18 | 全文 | 初版创建 | — |

---

## 附录 A：快速启动指南

### 启动全部服务

```bash
# 终端 1：象棋服务
cd /workspace/xiangqi && python main.py  # 端口 8000

# 终端 2：五子棋服务
cd /workspace/wuziqi && python main.py  # 端口 8001（需修改 main.py 端口）

# 终端 3：围棋服务
cd /workspace/go && python main.py  # 端口 8002

# 终端 4：RPG 主服务
cd /workspace/shared/rpg && python rpg_server.py  # 端口 80
```

### 访问

打开浏览器访问 `http://localhost/`，开始游戏。

### 配置 API Key

在 RPG 启动后，通过设置界面输入 DeepSeek API Key（用于棋圣系统作弊）。

---

## 附录 B：端口分配

| 服务 | 端口 | 用途 |
|---|---|---|
| RPG 主服务 | 80 | RPG 外壳 + API 网关 |
| 象棋服务 | 8000 | 象棋对战 |
| 五子棋服务 | 8001 | 五子棋对战 |
| 围棋服务 | 8002 | 围棋对战 |
| 剧情编辑器 | 8003 | 剧情 JSON 编辑（开发用） |

---

*本执行方案版本 v1.0。继任 AI 在执行中可据实调整，但请记录变更。*
