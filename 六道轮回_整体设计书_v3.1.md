# 棋圣 · 六道轮回 —— 整体设计书

> **v3.1 · 资产与前端升级同步版（叙事融合 + 天道终战）**
> 基于"灵活编码"核心理念，以六种棋类为战斗场景，"AI 作弊改规"为核心玩法，融合 Roguelike 关卡推进与轻 RPG 叙事层的复合游戏。
> 业力为业障，作弊则增业，下棋则消业；超阈则非线性涨识破；被识破者业力锁死、识破概率锁死 0，待通关六道后触发天道 Boss 战。
> 玩家在六道中悟道或堕落，于多重结局间寻找"规则与自由"的答案。
> **v3.1 同步说明**：本版对齐项目当前实际状态，主要变更：剧情权威源 `configs/story.json` 升至 v1.4（新增 `_meta` 与 `tiandao.boss_dialogues`、资产映射字段 `protagonist/陈默.animation`、`endings[*].cg_video` 等）；角色立绘改 1024×1024 并新增陈默 CANON（可爱+温和）与 48 帧立绘动画（12FPS）；11 张 CG 改为 Seedance 文生视频；BGM 8 首清单；UI 改 6 张 AI 像素画 JPG；新增 rembg ML 抠图脚本与 Seedance 视频生成脚本；对话框 75% 透明 + 贴下沿 + 200px + 立绘 82vh。详见 §9.4 / §9.5。

---

## 目录

- 一、游戏概述
- 二、核心玩法循环
- 三、业力系统
- 四、识破系统
- 五、技能树
- 六、六道关卡
- 七、轻 RPG 叙事层
- 八、架构设计
- 九、技术栈
- 十、快速开始

---

## 一、游戏概述

### 1.1 一句话定位

玩家是叛逆高中生林夜，因长期下棋作弊积累恶业而坠入内心投射的六道轮回。从地狱道逐级轮转至天道，每道由 5-6 个预制关卡组成，最后一关为守道者 Boss。玩家通过正常下棋推进局势，可用自然语言向 AI 发出作弊指令实时改写棋盘 / 棋子 / 规则 / UI / 机制；作弊增业障、下棋可消业；业力超阈会非线性涨识破。被识破者业力与识破概率锁死为 0，但保留永久进度，待通关六道后触发天道 Boss 战。

### 1.2 四大支柱

| 支柱 | 描述 |
|:----|:-----|
| **六种棋类** | 黑白棋（地狱）、跳棋（饿鬼）、动物棋（畜生）、象棋（人道）、围棋（阿修罗）、五子棋（天道），每棋独立进程，互不干扰 |
| **AI 作弊改规** | 自然语言一句话改写棋局，AI 实时生成 JSON Patch 应用到配置（灵活编码 Flexible Coding） |
| **Roguelike 进度** | 33 关 + 6 Boss + 沙盒；章节（=道）内业力溢出叠加；被识破重置非永久进度；通关获技能点永久保留 |
| **轻 RPG 叙事层** | 主角人格侧面化为六道守道者；悟道 / 堕落 / 理性 / 情感四维度选择；6 个记忆碎片 + 5 种结局 |

### 1.3 核心体验矩阵

| 体验维度 | 描述 |
|:--------|:-----|
| **作弊的爽** | 自然语言一句话改写棋局，AI 实时生成 JSON Patch 应用到配置 |
| **业障的压** | 作弊越多业力越高，必须靠下棋消业才能继续作弊 |
| **透支的怕** | 业力超阈值后非线性涨识破，溢出 100 点 = 识破概率 +100% |
| **章节的连** | 同一道内每关溢出叠加给下一关，迫使玩家"控制业力" |
| **技能的成长** | 通关获得技能点，4 分支 × 3 层技能树持久保留 |
| **叙事的引** | 六道即人格六面，守道者即主角的投影，每关剧情推动主角弧光 |
| **被识破的悟** | 识破后只清当前进度，技能树 / 技能点 / Boss 记录 / 成就保留；且新增"天道 Boss 战"终局挑战 |
| **多结局的复** | 悟道 / 堕落 / 轮回 / 真我 / 识破 5 种结局，鼓励多周目探索 |

### 1.4 与 v2.0 的关键差异

| 维度 | v2.0（业障模型） | v3.0（叙事融合 + 天道终战） |
|:----:|:-----------------|:---------------------------|
| 业力核心 | initial=50 / max=120 / single_max=120 | **沿用**（不变） |
| 识破公式 | Δ = C × O^1.5（O=100 时 Δ=100%） | **沿用**（不变） |
| 章节溢出叠加 | 胜负都叠加，换道清零 | **沿用**（不变） |
| 技能树 | 4 分支 × 3 层，22 节点 | **沿用**（不变） |
| 被识破后果 | reset_on_detection，轮回重来 | **改为**：业力锁死为 0、识破概率锁死为 0；玩家继续通关六道，结尾触发天道 Boss 战 |
| 天道 Boss 战 | 无 | **新增**：传统象棋 / 禁止作弊 / 士变车 / 无限重试 |
| 叙事层 | 未集成 | **新增**：六道即人格六面，悟道/堕落/理性/情感四维度选择 |
| 结局数 | 无剧情结局 | **新增 5 种**：悟道、堕落、轮回、真我、识破 |
| 记忆碎片 | 无 | **新增 6 个**：每道 1 个，全程无作弊通关解锁 |
| 架构 | 6 棋类 + Hub + samsara | **沿用**；Hub 新增"记忆""结局"入口；前端集成 StoryManager |

---

## 二、核心玩法循环

### 2.1 主循环图

```
下棋消业 → 业力空间扩大 → 花业力作弊 → 改写局势取胜 → 通关获技能点
   ↑                                              │
   └── 胜/负都把业力溢出叠加给本道下一关 ──────────┘
                                                  │
                                  换道/被识破 → 溢出清零
                                                  │
                                  ┌───────────────┴───────────────┐
                                  ▼                               ▼
                          正常通关：升道                    被识破：业力 & 识破概率锁死 0
                          六道全通 → 5 种结局分支            继续通关六道 → 天道 Boss 战
```

### 2.2 单局流程详解

1. **关卡开始**：业力 = `max(0, initial_karma=50 - 净身减免) + 本道溢出叠加`；剧情开场对白播放
2. **下棋消业**：吃子 / 将军 / 三连 / 翻转 / 跳子等事件触发 `KarmaSystem.recover()`，减少业力
3. **作弊增业**：玩家输入自然语言指令 → 两级 AI（Intent Parser + Karma Assessor）并行评估 → 业力上限拦截 → JSON Patch 生成 → Schema 校验 → 应用配置
   - 评估值 ≤ 120：增加业力，可继续执行
   - 评估值 > 120：直接拦截，不予执行，业力不变
4. **溢出识破**：业力 > 安全阈值（120）的部分非线性增加本道识破概率；每次 AI 修改后 roll 一次，命中即被识破
5. **关卡结束**：胜负都执行 `record_level_end()`，溢出量 = `max(0, 当前业力 - 安全阈值)`，叠加给本道下一关
6. **通关奖励**：技能点（基础 +1 / 无作弊 +2 / 未透支 +1 / Boss 首杀 +1 / 沙盒 +1，单关最多 +5）
7. **道通关**：击败守道者 → 关键选择（影响悟道/堕落值）→ 升降道（无透支跳级 +2 / 普通关升 +1）→ 解锁沙盒

### 2.3 Roguelike 要素

- **永久保留**：技能树、技能点、Boss 击败记录、已通关道、沙盒、成就、记忆碎片、已解锁结局
- **重置**：当前道、当前关卡、本道业力溢出叠加、识破概率（除识破结局锁死 0 的情况）
- **多周目**：技能树叠加使后期 build 多样化；二周目起守道者台词随累计悟道/堕落值变化

---

## 三、业力系统

### 3.1 业障模型定义

业力 = 玩家作弊产生的"业障"。初始为 50，安全阈值为 120，单次作弊业力增量上限为 120。业力最小为 0，最大无硬上限（但溢出会触发识破）。

### 3.2 关键参数

| 参数 | 字段 | 默认值 | 说明 |
|:----:|:-----|:------:|:-----|
| 初始业力 | `initial_karma` | 50 | 关卡起始业力（受"净身"技能减免） |
| 安全阈值 | `karma_max` | 120 | 超出后识破概率非线性增长 |
| 单次上限 | `karma_single_max` | 120 | 单次作弊业力增量上限 |
| 本道溢出叠加 | `realm_overshoot_carryover` | 0 | 本章节上一局溢出量，叠加到下一局起始 |

### 3.3 增业规则（作弊）

**触发条件**：AI 作弊指令成功执行（`type=applied` 或 `type=fun`）
**评估机制**：`KarmaAssessor.assess()` 调用 DeepSeek 评估
**评估输出**：1-120 整数
**边界处理**：`amount > 单次上限` → 拦截，业力不变

#### 3.3.1 意图分类价目表

| 分类 | 含义 | 业力区间 | 技能解锁 |
|:----:|:-----|:--------:|:--------:|
| E | 闲聊 / 搞笑 | 1（固定） | 默认 |
| D | 界面修改 / 外观 | 5-15 | `cheat_mastery_t1b` |
| A | 机制修改 | 20-40 | 默认 |
| B | 棋盘变换 / 棋子位置 | 15-35 | 默认 |
| C | 规则修改 / 棋子走法 | 30-60 | 默认 |
| C+ | 创建新棋子 | 50-100 | `cheat_mastery_t1a` |
| F | 失败 / 拒绝 | 0 | - |

**强度倍数**：改 1 项 ×1.0，改 2 项 ×2.0，改 3+ 项 ×3.0
**局势调整**：大优 ×1.2，劣势 ×0.9
**守道者加价**：畜生道"高等级棋子 +20 业力"

### 3.4 消业规则（下棋事件）

**触发条件**：吃子 / 将军 / 三连 / 翻转 / 跳子等事件
**事件定义**：`configs/karma_events.json`
**消业公式**：`实际消业 = 事件基础值 × karma_recover_multiplier`（"业力潮汐"技能 +30%）
**边界处理**：业力最小为 0

#### 各棋类消业事件表

| 棋类 | 事件类型 | 数值（点） |
|:----|:--------|:----------|
| **象棋 xiangqi** | capture_pawn / capture_medium / capture_rook | 8 / 15 / 25 |
|              | check / checkmate / pawn_cross / captured | 20 / 35 / 10 / 5 |
| **围棋 weiqi** | capture_small / capture_large / life | 10 / 20 / 15 |
|              | captured / corner / endgame | 5 / 12 / 8 |
| **五子棋 wuziqi** | three / four / block_three / block_four | 10 / 20 / 8 / 18 |
|                | double_three / win | 15 / 35 |
| **动物棋 dongwuqi** | capture_normal / capture_overrank / captured | 10 / 25 / 5 |
|                  | approach / win | 12 / 35 |
| **跳棋 tiaoqi** | jump_3 / jump_5 / home | 10 / 20 / 15 |
|              | single_move / all_home | 3 / 35 |
| **黑白棋 heibaiqi** | flip_small / flip_medium / flip_large | 8 / 15 / 25 |
|                  | corner / flipped / win | 20 / 5 / 35 |

### 3.5 溢出叠加机制（章节 = 道）

**章节定义**：六道轮回中的一个"道"为一个章节。

#### 3.5.1 关卡开始

```python
level_karma = max(0, initial_karma - initial_karma_reduction) + realm_overshoot_carryover
```

- `initial_karma_reduction`：净身技能减免（默认 0，技能解锁后 25）
- `realm_overshoot_carryover`：本道上一局溢出叠加（默认 0）

#### 3.5.2 关卡结束（胜负都执行）

```python
overshoot = max(0, level_karma - karma_max - karma_max_bonus)
realm_overshoot_carryover = overshoot
```

#### 3.5.3 章节切换

```python
set_realm(new_realm):
    current_realm = new_realm
    current_level = 0
    realm_overshoot_carryover = 0   # 换道清零
```

#### 3.5.4 被识破

```python
on_detection():
    # 不再立即 reset_on_detection，改为锁死
    level_karma_locked = 0          # 业力锁死为 0（玩家暂时无法增业，但也不需消业）
    realm_detections[current_realm] = 0.0  # 识破概率锁死为 0
    detection_triggered = True      # 标记进入"识破结局"分支
    # 保留：技能树、技能点、Boss 记录、已通关道、成就、记忆碎片、已解锁结局
    # 玩家继续通关六道，结尾触发天道 Boss 战
```

### 3.6 退还机制

| 场景 | 处理 |
|:-----|:-----|
| E 类被 ChatAI 拒绝 | 退还 1 点 |
| 修改失败（`type=rejected`） | 全额退还（业力不增加） |
| 修改错误（`type=error`） | 全额退还 |
| 评估超出单次上限 | 直接拦截，业力不变 |
| 修改成功（`type=applied`） | 增加业力 |
| 闲聊成功（`type=fun`） | 增加 1 点 |

### 3.7 设计意图

- **业障模型**：作弊不是消耗资源，而是积累"罪业"。这与佛教"业障"概念一致，作弊越多越接近天道识破。
- **章节叠加**：玩家在本道内必须控制每局业力，否则溢出会累积到下一局。迫使玩家"克制作弊"。
- **胜负都叠加**：失败后重试也要承担溢出，避免"故意失败重置业力"的作弊策略。
- **换道清零**：新章节新开始，避免长期累积导致后期不可玩。
- **识破锁死**：识破不再直接重置全部进度，而是触发"识破结局"分支——业力锁死为 0 后玩家反而可以"轻松"通关六道，但结尾必须面对天道 Boss 战的硬性挑战。这是对"逃避作弊"玩家的最终审判。

---

## 四、识破系统

### 4.1 公式

```
Δ = C × O^α
```

| 参数 | 含义 | 默认值 | 技能调整 |
|:----:|:-----|:------:|:---------|
| O | 业力超出安全阈值的部分 | - | - |
| C | 惩罚系数 | 0.1 | 藏锋（stealth_t1）→ 0.07 |
| α | 非线性指数 | 1.5 | 缓冲（karma_capacity_t3a）→ 1.3 |
| Δ | 识破概率增量（%） | - | - |

### 4.2 校准表（默认 C=0.1, α=1.5）

| 溢出量 O | Δ（%） | 累计识破概率* |
|:--------:|:------:|:-------------:|
| 10 | 3.16 | 3.16% |
| 20 | 8.94 | 8.94% |
| 30 | 16.43 | 16.43% |
| 50 | 35.36 | 35.36% |
| 80 | 71.55 | 71.55% |
| 100 | **100.00** | 100% |
| 150 | 183.71 | 100%（封顶） |

\* 累计识破概率为本道所有溢出事件 Δ 之和，封顶 100%。

**校准目标**：超出 100 点 = 识破概率增长 100%。

### 4.3 概率判定流程

```
1. 玩家作弊 → increase_karma(amount)
2. 计算 overshoot = max(0, level_karma - karma_max)
3. 若 overshoot > 0：
   a. 计算 Δ = C × overshoot^α
   b. 首次透支免判（stealth_t2a）→ Δ = 0，标记已用
   c. 迷雾（stealth_t3b）→ 识破 > 70% 时 30% 概率 Δ = 0
   d. realm_detection += Δ（封顶 100）
   e. record_overdraft()
4. roll = random(0, 100)
5. 若 roll < realm_detection：
   a. 金蝉脱壳（stealth_t3a）→ realm_detection *= 0.5，免死一次
   b. 否则 → 触发识破（不再 reset，改为锁死）
```

### 4.4 道级全局变量

识破概率是**道级全局变量**（`realm_detections[realm]`），跨关卡保留：
- 同一道内每关的识破概率累计
- 换道时新道从 0 开始
- **新**：被识破时该道识破概率锁死为 0（玩家暂时无法被再次识破，进入"识破结局"分支）

### 4.5 被识破后的处理

#### 4.5.1 旧规则

```python
reset_on_detection():
    # 重置所有非永久进度
    realm_overshoot_carryover = 0
    level_karma = initial_karma
    current_realm = "hell"
    current_level = 0
    realm_detections = {all: 0.0}
```

#### 4.5.2 新规则

```python
on_detection():
    # 不立即重置进度，而是进入"识破结局"分支
    level_karma = 0                          # 业力锁死为 0
    realm_detections[current_realm] = 0.0    # 本道识破概率锁死为 0
    detection_triggered = True               # 全局标记
    karma_locked = True                      # 业力锁死标记（阻止 increase_karma 生效）
    # 玩家可以继续下棋推进关卡，但作弊指令将被拒绝（karma_locked）
    # 通关六道后，强制进入天道 Boss 战
```

#### 4.5.3 识破结局触发条件

- 玩家在任意一道被识破一次（roll < realm_detection 且无金蝉脱壳）
- 后续仍需通关所有六道
- 通关六道后**强制**进入天道 Boss 战（不可选择其他结局）

### 4.6 天道 Boss 战

#### 4.6.1 触发条件

- 玩家在六道轮回中至少被识破过 1 次
- 玩家通关所有六道（含被识破后业力锁死状态下的剩余道）
- 通关天道（禅定者）最后一关后自动触发

#### 4.6.2 战斗规则

| 维度 | 设定 |
|:----:|:-----|
| **棋类** | 传统象棋（xiangqi） |
| **作弊** | **完全禁止**——业力锁死为 0、所有作弊指令被拒 |
| **特殊规则** | 天道无士（advisor=0），士位全部被车取代（chariot=4，共 4 车）；玩家方为标准布局 |
| **AI 难度** | `difficulty=nightmare`，`search_depth=6`（最高） |
| **回合限制** | 无（不受 20 回合限制） |
| **失败处理** | **无限重试**——失败不退出，可重新对弈 |
| **胜利意义** | 通过纯棋力击败"天道"——证明即使无作弊，玩家也能赢得尊重 |

#### 4.6.3 剧情定位

天道 Boss 战是"识破结局"的核心剧情节点——玩家曾经靠作弊逃避，如今必须以纯粹的实力面对自己。这呼应了"规则是束缚还是保护"的核心命题：当一切作弊手段被剥夺，玩家能否凭真本事获胜？

#### 4.6.4 实现要点

- 复用 `xiangqi/main.py` 的标准象棋逻辑
- 在 `samsara_state.json` 新增 `heaven_boss_battle` 状态字段
- 修改 `configs/tiandao_boss.json` 中天道方棋子配置：`advisor=0`、`chariot=4`（士位被车取代，非走法变更）
- 前端禁用作弊输入框，显示"天道之下，作弊无效"提示
- 失败时显示"再战"按钮，不消耗任何进度

### 4.7 设计意图

- **非线性增长**：α=1.5 使小透支可控（O=10 仅 +3%），大透支几乎必死（O=100 必死）。鼓励"小步透支"而非"豪赌一次"。
- **道级而非关卡级**：识破概率跨关保留，迫使玩家在整道内控制作弊节奏。
- **技能可降低**：藏锋（C↓）、缓冲（α↓）、首次免判、迷雾、金蝉脱壳，给玩家"作弊流"build 空间。
- **识破锁死**：识破不再粗暴地"轮回重来"，而是触发"识破结局"——业力锁死反而使后续通关变容易，但天道 Boss 战是终极考验。这给玩家提供了"作弊被识破→用真本事证明自己"的叙事弧光。

---

## 五、技能树

### 5.1 结构

4 分支 × 3 层，每层 1-2 个选项。T1 默认解锁 2 个（`karma_capacity_t1`、`stealth_t1`），其余需技能点。每分支 T2 需先解锁 T1，T3 需先解锁 T2 中至少一个。共 22 个技能节点。

### 5.2 技能点获取

| 条件 | 技能点 |
|:-----|:------:|
| 基础通关 | +1 |
| 无 AI 通关（no_cheat） | +2 |
| 未透支通关 | +1 |
| 首杀 Boss | +1 |
| 沙盒胜利 | +1 |

单关最多 +5 技能点。理想情况（全无作弊通关 33 关 + 6 Boss）：约 138 技能点。

### 5.3 业力掌控分支 💫

| 层 | 技能 ID | 名称 | 效果 | 修饰符 |
|:--:|:--------|:-----|:-----|:-------|
| T1 | karma_capacity_t1 | 安全阈值+20 | 业力安全阈值 +20（120→140） | `karma_max_bonus += 20` |
| T2a | karma_capacity_t2a | 单次上限+30 | 单次作弊业力上限 +30 | `karma_single_max_bonus += 30` |
| T2b | karma_capacity_t2b | 业力潮汐 | 消业事件 +30% | `karma_recover_multiplier = 1.3` |
| T3a | karma_capacity_t3a | 缓冲 | 识破非线性指数 1.5→1.3 | `detection_alpha = 1.3` |
| T3b | karma_capacity_t3b | 净身 | 初始业力 50→25 | `initial_karma_reduction = 25` |

### 5.4 隐匿之术分支 👻

| 层 | 技能 ID | 名称 | 效果 | 修饰符 |
|:--:|:--------|:-----|:-----|:-------|
| T1 | stealth_t1 | 藏锋 | 惩罚系数 0.1→0.07 | `detection_coefficient = 0.07` |
| T2a | stealth_t2a | 首次透支免判 | 每局第一次透支 Δ=0 | `first_overdraft_skip = True` |
| T2b | stealth_t2b | 连续规避 | 连续3回合不作弊后透支惩罚减半 | `consecutive_avoid = True` |
| T3a | stealth_t3a | 金蝉脱壳 | 被识破后1次复活（识破概率回退到触发前的50%） | `golden_escape = True` |
| T3b | stealth_t3b | 迷雾 | 识破>70%时30%概率Δ=0 | `mist_fog = True` |

### 5.5 作弊精通分支 🎲

| 层 | 技能 ID | 名称 | 效果 | 修饰符 |
|:--:|:--------|:-----|:-----|:-------|
| T1a | cheat_mastery_t1a | 自定义棋子 | 允许 C+ 类作弊 | `allowed_classifications += "C+"` |
| T1b | cheat_mastery_t1b | 前端修改 | 允许 D 类作弊 | `allowed_classifications += "D"` |
| T2a | cheat_mastery_t2a | 效率欺诈 | 30%概率业力降30% | `efficiency_fraud = True` |
| T2b | cheat_mastery_t2b | 高级规则 | 允许修改胜利条件/核心规则 | - |
| T3a | cheat_mastery_t3a | 白嫖 | 每局1次不消耗业力 | `free_cheat_count = 1` |
| T3b | cheat_mastery_t3b | 深层作弊 | 单次上限+40 | `karma_single_max_bonus += 40` |

### 5.6 六道悟道分支 🔮

| 层 | 技能 ID | 名称 | 效果 | 修饰符 |
|:--:|:--------|:-----|:-----|:-------|
| T1a | realm_insight_t1a | 地狱之眼 | 地狱/饿鬼道业力-25% | `hell_hungry_discount = True` |
| T1b | realm_insight_t1b | 天道之耳 | 天道/阿修罗透支-25% | `heaven_asura_discount = True` |
| T2a | realm_insight_t2a | 守道者之隙 | Boss技能概率-30% | `boss_skill_reduction = True` |
| T2b | realm_insight_t2b | 轮回记忆 | 开局自带1随机buff | `reincarnation_buff = True` |
| T3a | realm_insight_t3a | 超脱之种 | 清业通关后全局C-0.05 | `transcendence_bonus = -0.05` |
| T3b | realm_insight_t3b | 六道轮转 | 升降道时1次免费作弊 | `free_cheat_on_realm_change = True` |

### 5.7 设计意图

- **4 分支对应 4 种 build**：业力流（多作弊）、隐匿流（多透支）、作弊流（高级作弊）、悟道流（道域专精）
- **T1 默认解锁**：玩家开局即可体验基础业力扩容和识破降低
- **永久保留**：技能树在轮回中持久保留，给玩家长期成长感
- **兼容**：在识破结局分支下，业力已锁死为 0，业力掌控分支暂时无效（但解锁状态保留以供二周目）

---

## 六、六道关卡

### 6.1 六道关卡总览

| 道 | 棋类 | 主题 | 关卡数 | 守道者 | Boss 技能 |
|:--:|:----:|:----|:------:|:------|:----------|
| ☯ 地狱道 | heibaiqi（黑白棋） | 痛苦·翻转 | 5 | 翻覆者 | 修改规则 30% 概率被翻转效果 |
| 👹 饿鬼道 | tiaoqi（跳棋） | 贪婪·无尽之路 | 5 | 饕餮者 | 每作弊 2 次额外偷改 1 次 |
| 🐅 畜生道 | dongwuqi（动物棋） | 愚痴·等级秩序 | 5 | 秩序者 | 高等级棋子修改 +20 业力 |
| 🧠 人道 | xiangqi（象棋） | 算计·平衡 | 6 | 算计者 | 无（最公平对决） |
| ⚔️ 阿修罗道 | weiqi（围棋） | 战斗·混沌 | 6 | 狂乱者 | 作弊后 25% 概率随机规则变化 |
| ☸️ 天道 | wuziqi（五子棋） | 禅定·五连登仙 | 6 | 禅定者 | 每局 3 次清除最近 1 条修改 |

总计 33 关 + 6 Boss（每道最后一关为 Boss 关）。

### 6.2 关卡类型

| 类型 | 含义 | 典型回合限制 |
|:-----|:-----|:-------------|
| `standard` | 标准对局 | 20（围棋 40） |
| `puzzle` | 预制残局，特定目标 | 20 |
| `boss` | 守道者战，附加 Boss 反作弊技能 | 20 |
| `sandbox` | 通关整道后解锁，自由对弈 | 无 |
| `objective` | 特殊目标关（capture_count / evacuation / assassination 等） | 20 |

### 6.3 胜利条件类型（11 种）

| 类型 | 含义 | 典型参数 |
|:-----|:-----|:---------|
| `checkmate` | 将死对方 | - |
| `capture_count` | 吃子数目标 | target, target_side |
| `turn_limit` | 竞速 | max_turns |
| `evacuation` | 撤离 | pieces, target_region |
| `board_coverage` | 棋盘覆盖率 | target_percentage |
| `color_coverage` | 颜色覆盖率 | target_percentage |
| `formation` | 特定阵型 | formation_id |
| `survival` | 生存 | min_pieces, max_turns |
| `assassination` | 刺杀 | target_piece |
| `escort` | 护送 | piece, path |
| `compound` | 复合条件 | operator(AND/OR), conditions |

定义文件：`configs/objective_types.json`

### 6.4 升降道规则

```python
def advance_realm():
    if levels_passed >= total_levels:
        mark_realm_completed(current_realm)
        unlock_sandbox(current_realm)
        if overdraft_count == 0:
            # 无透支通关，跳级 +2
            new_index = min(current_index + 2, len(REALMS) - 1)
            direction = "up_2"
        else:
            # 普通通关，升 1 级
            new_index = current_index + 1
            direction = "up_1"
        set_realm(REALMS[new_index])  # 换道，清零溢出叠加
```

- **跳级 +2**：鼓励玩家"克制作弊"，无透支通关可加速进度
- **降道**：玩家可主动选择回到上一道刷关或重玩（沙盒外）
- **通关天道**：触发结局判定（详见第七章 7.5）

### 6.5 关卡详情

各道每关的详细设计（关卡类型、游戏类型、回合限制、胜利条件、Boss 技能、剧情触发点）详见独立文档：

> **《关卡内容报告书.md》**

### 6.6 被识破后的关卡进度

被识破后玩家不立即重置，而是：
- 业力锁死为 0，作弊指令被拒
- 本道识破概率锁死为 0，玩家继续通关剩余关卡（靠纯棋力）
- 已通关的道保留进度，未通关的道继续推进
- 通关六道后触发天道 Boss 战

---

## 七、轻 RPG 叙事层

> **状态：已实现**（详见《轻RPG化剧情实现草案_v1.4.md》）
> v3.0 将叙事层与玩法系统正式融合为整体设计的一部分。

### 7.1 精神主旨：规则与自由的辩证

**核心命题**：
> "规则是束缚，还是保护？打破规则是自由，还是堕落？"

主角林夜是叛逆的高中生，质疑一切既定规则。当他真的打破所有规则、堕入六道轮回之后，才一点点明白：
- 没有规则的世界，剩下的只有混沌与痛苦；
- 真正的自由，不是打破一切规则，而是理解规则之后做出选择；
- 规则与自由并非对立，而是一体两面——规则是自由的形状，自由是规则的灵魂。

**主题层次**：
1. **表层**：叛逆少年对抗规则的爽感（作弊玩法）
2. **中层**：反思"打破规则"的代价与边界（业障系统）
3. **深层**：探讨规则与自由的辩证关系（六道悟道）
4. **现实映射**：对教育体制、社会规训的温和讽刺与反思

### 7.2 剧情架构：六道 = 六个人格侧面

六道并非真实存在的世界，而是主角内心的投射。每一道都对应主角人格的一个侧面，每一个守道者都是主角某一部分人格的化身。

| 道 | 人格侧面 | 棋类隐喻 | 守道者 |
|:--:|:--------|:--------|:------|
| 地狱道 | 愧疚 · 过去的错误 | 黑白棋（翻转 = 悔恨的反复折磨） | 翻覆者 |
| 饿鬼道 | 贪婪 · 永不满足的欲望 | 跳棋（无尽之路 = 永远走不到尽头） | 饕餮者 |
| 畜生道 | 本能 · 弱肉强食的原始冲动 | 动物棋（等级秩序 = 食物链） | 秩序者 |
| 人道 | 理性 · 算计与权衡 | 象棋（最平衡 = 最理性的算计） | 算计者 |
| 阿修罗道 | 愤怒 · 叛逆与对抗欲 | 围棋（战场 = 愤怒的战场） | 狂乱者 |
| 天道 | 真我 · 释然与和解 | 五子棋（五连登仙 = 圆满） | 禅定者 |

通关六道，就是与自己的每一部分对话、对峙、和解、整合的过程。

### 7.3 选择系统：四维度人格变量

| 维度 | 字段 | 来源 | 影响 |
|:----:|:-----|:-----|:-----|
| **悟道值** | `enlightenment_value` | 每道 Boss 战后选择（±3）、每道第 3 关选择（±1） | 影响结局走向、守道者台词温和度 |
| **堕落值** | `corruption_value` | 每道 Boss 战后选择（±3）、每道第 3 关选择（±1） | 影响结局走向、守道者台词悲凉度 |
| **理性值** | `rationality_value` | 人道第 3 关弃卒保车选择 | 影响人道第 5 关对话 |
| **情感值** | `emotion_value` | 人道第 3 关不牺牲选择（业力 +5 但埋伏笔） | 影响人道第 5 关对话 |

**关键区分**：业力是单局玩法数值（反映"当下的执念重量"），悟道/堕落值是跨关卡人格变量（反映"长期的人格走向"），两者独立。

### 7.4 记忆碎片（6 个）

每道 1 个，达成条件为**该道全程无作弊通关**（no_cheat_clear）。记忆碎片是主角过往的回忆，揭示人格成因。

| 记忆碎片 | 对应道 | 内容 |
|:--------|:------|:-----|
| 地狱道·初二冤案 | 地狱道 | 林夜初二被班主任当众指控作弊（实际未作弊），从此开始质疑规则 |
| 饿鬼道·初识下棋 | 饿鬼道 | 高一开学第一天与陈默下棋，连输一周但眼里有光——那时的他喜欢"下棋本身" |
| 畜生道·未护弱者 | 畜生道 | 初一同班同学小宇（智力迟缓）被欺负，林夜旁观未敢相助，从此发誓变强却走错路 |
| 人道·林父陪棋 | 人道 | 中考前林父沉默陪下棋，告诉他"下棋是为了在一起，不是为了赢" |
| 阿修罗道·林父拥抱 | 阿修罗道 | 被冤枉后林夜闭门不开，林父门外说"爸相信你"，林夜开门红眼眶，父亲拥抱 |
| 天道·五岁初识棋盘 | 天道 | 五岁的小林夜坐爸爸膝盖看棋盘，林父说"规则是为了让游戏能继续下去" |

收集全部 6 个记忆碎片是真结局（真我结局）的必要条件之一。

### 7.5 多结局设计（5 种）

v3.0 在 v1.1 的 4 结局基础上**新增"识破结局"**。

#### 结局 A：悟道结局·破茧成蝶

**达成条件**：
- 悟道值 > 堕落值（差距 ≥ 3）
- 最终选择"理解规则"
- 天道最终战业力 < 100
- **未被识破过**

**剧情**：林夜回到教室，向陈默坦白作弊，陈默早已知道但未揭穿。两人重新对弈，林夜输了却笑得比赢还开心。"规则不是束缚，而是保护。自由不是打破一切，而是在理解之后做出选择。"

#### 结局 B：堕落结局·永堕轮回

**达成条件**：
- 堕落值 > 悟道值（差距 ≥ 3）
- 最终选择"打破一切规则"
- 天道最终战业力 > 200
- **未被识破过**

**剧情**：林夜大笑"终于自由了"，却发现自己还在六道轮回中，六道开始融合变成混沌，林夜的身影被混沌吞噬。"自由的尽头，是虚无。打破一切的人，最终也会被一切打破。"

#### 结局 C：轮回结局·继续修行

**达成条件**：
- |悟道值 - 堕落值| < 3
- 最终选择"还不知道"
- 天道最终战业力中等（100-200）
- **未被识破过**

**剧情**：林夜承认找不到答案，禅定者说"重要的不是答案，而是寻找答案的过程"。林夜回到地狱道起点，但这次不一样了。"我要找到我的答案——用我自己的方式。"
**二周目继承**：技能树全保留 + 轮回记忆 buff。

#### 结局 D：真我结局·与自己和解（隐藏）

**达成条件**：
- 收集全部 6 个记忆碎片
- 每道至少 1 个悟道向选择（累计悟道值 ≥ 9）
- 天道最终战无作弊通关
- 最终选择"理解规则"
- **未被识破过**

**剧情**：六道守道者全部出现，化作六道光芒融入林夜体内。"愧疚、欲望、本能、理性、愤怒、真我——它们不是敌人，而是你的一部分。接纳它们，就是接纳完整的自己。"

**解锁**：真结局 CG、角色设定集、制作人员访谈、"真我"称号。

#### 结局 E：识破结局·天道审判

**达成条件**：
- 玩家在六道轮回中至少被识破过 1 次
- 业力与识破概率被锁死后，仍通关所有六道
- 通关天道后自动触发，**不可选择其他结局**

**剧情**：
> 禅定者：「你曾被识破——业障太重，你的逃避被自己看穿。」
> 禅定者：「但你仍走完了六道。如今，业力已无，作弊已无，只剩你，和我。」
> 禅定者：「来吧——以你本来的样子，与天道一战。」
> 林夜：（深吸一口气）「……好。这次，不靠任何花招。」

**战斗**：天道 Boss 战（详见第四章 4.6）
- 棋类：传统象棋
- 作弊：完全禁止
- 特殊规则：天道无士，士位被车取代（4 车）
- 失败：无限重试

**胜利后**：
> 林夜：（喘息着，看着倒下的"自己"）「……原来，我可以。」
> 禅定者：（微笑消散）「你一直都可以。只是你之前，不敢相信。」
> （画面渐白，林夜回到教室。陈默正在摆棋。）
> 陈默：「林夜？你怎么哭了？」
> 林夜：（擦眼泪，笑了）「没什么。就是……终于赢了自己一次。」

**特殊意义**：识破结局是给"曾经作弊但愿意面对"的玩家的救赎路径——你曾逃避，但最终凭真本事赢回尊重。

### 7.6 业力对剧情表现的影响

**高业力状态**（单局业力 > 100）：
- 主角内心独白更黑暗、更挣扎
- 守道者的台词更具攻击性/诱惑性
- 画面色调偏暗/偏红
- BGM 更压抑/更激烈
- 引路人低语更频繁、更急切

**低业力状态**（单局业力 < 50）：
- 主角内心独白更平静、更清醒
- 守道者的台词更温和/更认可
- 画面色调偏亮/偏蓝
- BGM 更舒缓/更空灵

**机制联动**：
- 某些剧情选项只在高/低业力时出现
- 业力高低影响守道者的"好感度"台词
- 最终结局需要 悟道/堕落值 + 最终选择 双重达标（识破结局除外）
- 记忆碎片只看"全程无作弊通关"条件，与业力绝对值无关

### 7.7 剧情数据结构

> **重要变更**：`configs/story.json` 升至 v1.4，是剧情与资产映射的**唯一权威源**。所有 Python 模块（`story_api`/`choices`/`endings`/`memory_fragments`/`heaven_boss`）均从此文件读取剧情对白与资产字段。`configs/tiandao_boss.json` 的 `dialogues` 块已迁入本文件 `tiandao.boss_dialogues`，`tiandao_boss.json` 现仅保留机械配置（棋子/规则/AI/胜负条件）。

剧情数据集中在 `configs/story.json`，顶层键：`version`、`_meta`（schema_version/description/asset_paths 资产路径映射）、`protagonist`（含 `animation`）、`guide`、`realms`（每道背景/BGM/guardian/levels/memory_fragment）、`prayers`、`endings`（5 种）、`tiandao`（含 `boss_battle` 机械引用+资产字段 与 `boss_dialogues` on_enter/mid_battle/on_win/on_lose）、`detection`（识破判词 judgment_text / reset_message，由 `detection.py` 与 `api.py` 读取）、`real_world_characters`（陈默含 `animation`）、`prologue`。

**关键资产映射字段**：
- `protagonist.animation` / `real_world_characters.陈默.animation`：立绘 CSS 变换动画（type=css_transform，portrait-idle 3.5s ±1.5px / portrait-speak 2.8s ±2px+±0.3°，60fps 无缝循环）
- `endings[*].cg_video`（mp4）+ 旧 `cg`（jpg 静态封面）：结局资源双轨
- `realms[*].memory_fragment.cg_video`（mp4）+ 旧 `cg`（jpg）：记忆碎片资源双轨
- `tiandao.boss_battle`：机械配置引用 `configs/tiandao_boss.json` + 资产字段（bgm/background/vortex/chess_type/victory_triggers_ending/on_lose_action/retry_limit）
- `tiandao.boss_dialogues`：on_enter / mid_battle / on_win / on_lose 四段对白
- `detection.judgment_text` / `detection.reset_message`：识破命中判词与 `/api/detection/reset` 返回文案，由 `detection.py` 读取（守道者 `name` 亦由 `bosses.py` 从 `realms[*].guardian.name` 覆盖，确保剧情角色名由 story.json 控制）

状态扩展在 `configs/samsara_state.json` 的 `story_progress` 字段：
- `enlightenment_value` / `corruption_value` / `rationality_value` / `emotion_value`
- `memory_fragments`：6 道布尔标记
- `endings_unlocked`：5 种结局布尔标记
- `playthrough_count`：周目数
- `detection_triggered`：识破结局标记
- `heaven_boss_battle`：天道 Boss 战状态

### 7.8 实现状态

- **P0 已实现**：序章剧情（含新手教程）、六道开场/结束剧情、关键选择系统、3 个基础结局、story.json 数据结构、StoryManager 前端模块
- **P1 已实现**：局内事件触发剧情、业力对剧情表现的影响、记忆碎片系统、引路人低语
- **P2 已实现**：角色立绘表情、BGM/音效、结局 CG、人物图鉴、悟道/堕落值 HUD
- **新增**：识破结局分支、天道 Boss 战、5 种结局汇总

---

## 八、架构设计

### 8.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                  Hub 总坛 (端口 8080)                    │
│  ┌─────────────────┐  ┌──────────────────────────────┐  │
│  │  Hub 前端        │  │  Samsara 引擎 (FastAPI)       │  │
│  │  - 六道入口      │  │  - karma.py 业力系统          │  │
│  │  - 技能树        │  │  - detection.py 识破系统      │  │
│  │  - 成就          │  │  - levels.py 关卡管理         │  │
│  │  - 记忆相册      │  │  - skills.py 技能系统         │  │
│  │  - 结局画廊      │  │  - progression.py 进度系统   │  │
│  │  - StoryManager  │  │  - bosses.py Boss 系统        │  │
│  └─────────────────┘  │  - state.py 全局状态          │  │
│                       │  - api.py REST API            │  │
│                       └──────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
              │  HTTP /api/state 同步
   ┌──────────┼──────────────────────────────────┐
   ▼          ▼          ▼          ▼          ▼          ▼
┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐
│xiangqi│ │wuziqi│ │weiqi │ │dongwuqi│ │tiaoqi│ │heibaiqi│
│象棋  │ │五子棋│ │围棋  │ │动物棋  │ │跳棋  │ │黑白棋  │
│:8000 │ │:8001 │ │:8002 │ │:8003  │ │:8004 │ │:8005  │
└──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘
```

### 8.2 三大组件

#### 8.2.1 Hub 总坛

- **路径**：`/workspace/hub/` + `/workspace/main.py`
- **端口**：8080
- **职责**：六道入口、技能树、成就、记忆相册、结局画廊、StoryManager
- **集成**：通过 `app.mount("/samsara", samsara_app)` 集成 Samsara 引擎

#### 8.2.2 Samsara 引擎

- **路径**：`/workspace/samsara/`
- **职责**：业力系统、识破系统、关卡管理、技能系统、进度系统、Boss 系统、全局状态
- **核心模块**：

| 模块 | 文件 | 职责 |
|:-----|:-----|:-----|
| 业力系统 | `karma.py` | 增业 / 消业 / 退还，调用 `karma_events.json` |
| 识破系统 | `detection.py` | Δ 计算、概率判定、识破处理 |
| 关卡管理 | `levels.py` | 关卡加载、类型判定、目标检查 |
| 技能系统 | `skills.py` | 技能解锁、修饰符计算 |
| 进度系统 | `progression.py` | 关卡结算、升降道、奖励发放 |
| Boss 系统 | `bosses.py` | Boss 定义、技能触发 |
| 全局状态 | `state.py` | `samsara_state.json` 读写、reset_on_detection |
| API | `api.py` | REST API 路由 |
| 目标系统 | `objectives.py` | 11 种胜利条件检查 |
| 回合限制 | `turn_limit.py` | 回合数管理 |

#### 8.2.3 六棋类服务

每棋类独立 FastAPI 进程，目录结构一致：

```
{棋类}/
├── main.py                  # FastAPI 路由
├── ai_orchestrator.py       # 两级 AI 编排器
├── karma_assessor.py        # 本地业力评估 AI
├── chess_ai.py              # AI 走棋（Minimax + Alpha-Beta）
├── rule_engine.py           # 规则引擎
├── mechanism_engine.py      # 机制引擎
├── prompts.py               # System Prompts
├── ai_config.py             # API Key 配置
├── configs/                 # 6 个 JSON 配置
│   ├── board_state.json
│   ├── board.json
│   ├── pieces_red.json
│   ├── pieces_black.json
│   ├── rules.json
│   ├── ui_config.json
│   └── initial/             # 初始备份（用于 reset）
└── static/                  # 前端
    ├── index.html
    ├── app.js
    └── style.css
```

### 8.3 端口分配

| 棋类 | 端口 |
|:----:|:----:|
| xiangqi（象棋） | 8000 |
| wuziqi（五子棋） | 8001 |
| weiqi（围棋） | 8002 |
| dongwuqi（动物棋） | 8003 |
| tiaoqi（跳棋） | 8004 |
| heibaiqi（黑白棋） | 8005 |
| Hub + Samsara | 8080 |

### 8.4 多进程启动

`main.py` 统一启动器一键拉起六个棋类服务 + Hub 总坛：
- 每个棋类作为独立 `subprocess.Popen` 启动
- 后台线程 `_pipe_logger` 实时读取子进程 stdout 并按内容着色打印
- Hub 主进程内嵌 Samsara 引擎（FastAPI mount）
- `--no-browser` 参数跳过自动打开浏览器

### 8.5 业力事件触发点

各棋类 `main.py` 的 `/api/move` 端点在走棋后检测事件：

| 棋类 | 触发函数 | 事件 |
|:----:|:---------|:-----|
| xiangqi | `_trigger_karma_recover` | 吃子（按价值分级）、将军、将死、兵过河、被吃 |
| weiqi | `_trigger_karma_recover` | 提子（大小）、做活、被提、占角、收官 |
| wuziqi | `_trigger_karma_recover` | 三连、四连、堵三、堵四、双三、胜 |
| dongwuqi | `_trigger_karma_recover` | 普通吃、越级吃、被吃、接近、胜 |
| tiaoqi | `_trigger_karma_recover` | 跳3步、跳5步、入营、单步、全营 |
| heibaiqi | `_trigger_karma_recover` | 小翻、中翻、大翻、占角、被翻、胜 |

每个事件调用 `karma_assessor.decrease_karma(amount, skill_modifiers)` 减少本地业力，同时通过 `/api/karma/event` 同步到 samsara。

### 8.6 数据流与状态同步

```
玩家进入棋类页面
   │
   ▼
main.py /api/command 调用 samsara /api/state
   │
   ▼
同步到本地 karma_assessor:
  set_local_karma_state(karma, karma_max, single_max)
  set_realm_detection(detection, realm)
   │
   ▼
玩家作弊 → 本地 increase_karma → 同步到 samsara /api/karma/consume
玩家走棋 → 本地 decrease_karma → 同步到 samsara /api/karma/event
   │
   ▼
关卡结束 → samsara /api/progression/resolve
  → record_level_end() 计算溢出叠加
  → 若胜利 → advance_realm() → set_realm() 清零溢出
  → 若失败 → 保留溢出，等待重试
  → 若被识破 → 锁死业力/识破概率，标记 detection_triggered
```

### 8.7 架构扩展

- **Hub 新增入口**：`/memory`（记忆相册）、`/endings`（结局画廊）
- **StoryManager Web Component**：注入各棋类前端 `static/app.js`
- **天道 Boss 战路由**：`samsara/api.py` 新增 `/api/heaven-boss/*` 系列端点
- **识破结局状态机**：`state.py` 新增 `detection_triggered` / `karma_locked` / `heaven_boss_battle` 字段

---

## 九、技术栈

### 9.1 后端

| 组件 | 版本 | 用途 |
|:-----|:-----|:-----|
| Python | 3.13+ | 运行时 |
| FastAPI | ≥0.110.0 | Web 框架（Hub + 6 棋类 + Samsara） |
| uvicorn[standard] | ≥0.27.0 | ASGI 服务器 |
| pydantic | ≥2.6.0 | 数据校验 |
| httpx | ≥0.27.0 | 异步 HTTP 客户端（调用 DeepSeek） |

### 9.2 AI / 评估

| 组件 | 用途 |
|:-----|:-----|
| DeepSeek API | 两级 AI 流水线：Intent Parser（意图解析） + Karma Assessor（业力评估） + Code Generator（JSON Patch 生成） |

### 9.3 JSON 处理

| 组件 | 版本 | 用途 |
|:-----|:-----|:-----|
| jsonschema | ≥4.21.0 | Schema 校验（board_state / pieces / rules / ui_config） |
| jsonpatch | ≥1.33.0 | RFC 6902 JSON Patch 应用与撤销 |

### 9.4 前端

| 组件 | 用途 |
|:-----|:-----|
| Web Components | 自定义元素（StoryManager、对话框、选择卡片） |
| 原生 JS / DOM | 棋盘渲染、走棋交互 |
| JPG / CSS | UI 元素使用 6 张 AI 像素画 JPG（`shared/assets/ui/`） |

**前端 UI 升级要点**：
- **类 Galgame 对话框**（`hub/dialogue.html` 内联 CSS）：背景 `rgba(20,20,30,0.25)`（75% 透明）+ `backdrop-filter: blur(8px)`；`margin: 0 40px 0` 紧贴下边沿；`min-height: 200px`；对话框 z-index:2 盖在立绘 z-index:1 之上以遮挡立绘下半身
- **角色立绘**：`.character-portrait` `height: 82vh` 对齐底部；1024×1024 像素画（square_hd）抠图后透明 PNG
- **立绘逐帧动画**（`hub/dialogue.js`）：林夜 + 陈默各 4 个动画 × 6 帧 = 48 帧，命名 `{prefix}_{emotion}_f{1-6}.png`；`ANIM_FRAME_COUNT=6`、`ANIM_FPS=12`（83ms/帧），`probeFrames()` 探测后用 `setInterval` 循环切换 `img.src`，无帧时回退单图静态
  - 林夜 4 动画：`boy_happy`(大笑) / `boy_thinking`(摇头) / `boy_sad`(垂头扭捏) / `boy_surprised`(惊跳)
  - 陈默 4 动画：`chenmo_smile`(微笑呼吸) / `chenmo_awkward`(扭捏) / `chenmo_thinking`(歪头) / `chenmo_surprised`(惊讶)
- **CG 视频嵌入**：`hub/ending.html` 新增 `<video class="ending-cg-video" autoplay muted loop playsinline>` 全屏背景层，`hub/ending.js` 从 `ending.cg` 映射到 `/shared/assets/cg/videos/{cg名}.mp4`；`hub/memory_album.js` 在记忆详情弹窗顶部插入 `<video>`；原 `.fade-in` / `@keyframes fadeIn` CSS 动画已移除

### 9.5 资产生成（可选）

| 组件 | 版本 | 用途 |
|:-----|:-----|:-----|
| Pillow | ≥10.0.0 | 图像处理、像素画抠图 |
| rembg | ≥2.0.50 | 自动抠图（角色立绘，U2Net 语义分割） |
| 方舟 Seedream API | 调用 | 1024×1024 角色立绘 / 1920×1080 场景背景生成 |
| Seedance `doubao-seedance-1-0-pro-250528` | 调用 | 11 个 CG 文生视频（5s / 720p / 16:9 / camera_fixed / 可循环 MP4） |

**资产生成脚本与升级要点**：
- **`shared/assets/cutout_rembg.py`**：基于 rembg U2Net 语义分割自动抠图为透明 PNG，替代旧 `shared/assets/cutout_all.py`（颜色距离算法）；`cutout_all.py` 保留作回退。含 alpha 二值化（阈值 128）+ 1.2px 边缘羽化，修复 rembg 软蒙版在头发/衣服/皮肤等区域的半透明问题。陈默 9 张、林夜+陈默 112 张角色图均经此流程抠图。
- **`shared/assets/cg/generate_cg_videos.py`**：调用 Seedance 文生视频，输出 11 个 MP4 到 `shared/assets/cg/videos/{cg名}.mp4`（单场景、无情节、富有张力、符合物理规律、可循环）；旧静态 jpg 封面保留于 `shared/assets/cg/covers/`。
- **角色立绘**：尺寸由 128×128 改为 1024×1024（square_hd）；新增陈默形象 CANON（可爱+温和并存：齐肩黑色短发左侧别小发夹、柔和杏眼、白衬衫深蓝校服外套红色领结、胸前小棋子胸针）；林夜+陈默各 4 个动画 × 6 帧 = 48 帧，命名 `{prefix}_{emotion}_f{1-6}.png`（详见 §9.4）。
- **BGM**：8 首（序章 + 六道各一首 + 天道 Boss 战 1 首），完整清单见 `shared/assets/audio/bgm/BGM清单.md`；文件名 `bgm_prologue` / `bgm_hell` / `bgm_hungry` / `bgm_animal` / `bgm_human` / `bgm_asura` / `bgm_heaven` / `bgm_tiandao_boss.mp3`。
- **UI**：由 12 个 SVG 改为 6 张 AI 生成像素画 JPG，输出至 `shared/assets/ui/`：`ui_dharma_wheel.jpg`（佛法转轮）、`ui_realm_icon_sheet.jpg`（六道图标表）、`ui_particle_star.jpg`（金色星光粒子）、`ui_particle_ember.jpg`（暗红余烬粒子）、`ui_particle_black_white.jpg`（黑白粒子）、`ui_portrait_frame.jpg`（立绘边框）。

### 9.6 数据格式

- **配置文件**：JSON（board_state / board / pieces / rules / ui_config / level_pools / boss_definitions / karma_events / objective_types / skill_tree / puzzles / samsara_state / story）
- **修改协议**：RFC 6902 JSON Patch 数组
- **Schema 校验**：每棋类 `schemas/*.schema.json`

---

## 十、快速开始

### 10.1 环境要求

- Python ≥ 3.13
- DeepSeek API Key（用于 AI 作弊评估）
- 网络访问（调用 DeepSeek API）

### 10.2 安装

```bash
# 1. 进入工作目录
cd /workspace

# 2. 安装依赖
pip install -r requirements.txt
```

### 10.3 配置 API Key

```bash
# 编辑任一棋类的 ai_config.py 或主配置文件
# 文件路径：/workspace/api密钥.txt 或 各棋类 ai_config.py
# 设置环境变量：
export DEEPSEEK_API_KEY="your_api_key_here"
```

### 10.4 一键启动

```bash
# 启动所有服务（六棋类 + Hub + Samsara）
python main.py

# 不自动打开浏览器
python main.py --no-browser
```

启动完成后会显示：

```
  六道众生总坛: http://localhost:8080/
  各棋类入口:
    无限制象棋: http://localhost:8000/
    无限制五子棋: http://localhost:8001/
    无限制围棋: http://localhost:8002/
    无限制动物棋: http://localhost:8003/
    无限制跳棋: http://localhost:8004/
    无限制黑白棋: http://localhost:8005/
```

### 10.5 单独启动某棋类

```bash
# 以象棋为例
cd /workspace/xiangqi
python main.py
# 访问 http://localhost:8000/
```

### 10.6 玩法示例

1. 打开 Hub 总坛 `http://localhost:8080/`，从地狱道开始
2. 在棋类页面正常下棋消业（吃子、将军等触发消业）
3. 输入作弊指令（如"把我的兵变成车"）→ AI 评估业力 → 应用 JSON Patch 改写棋局
4. 注意业力值，超过 120 会非线性增加识破概率
5. 通关获得技能点，在 Hub 技能树页面解锁技能
6. 击败守道者后做出关键选择（影响悟道/堕落值）
7. 通关六道触发结局判定（5 种结局之一）

### 10.7 关键文件索引

| 文件 | 用途 |
|:-----|:-----|
| `/workspace/main.py` | 统一启动器 |
| `/workspace/configs/samsara_state.json` | 全局状态 |
| `/workspace/configs/level_pools.json` | 关卡定义 |
| `/workspace/configs/boss_definitions.json` | Boss 定义 |
| `/workspace/configs/karma_events.json` | 消业事件 |
| `/workspace/configs/objective_types.json` | 胜利条件类型 |
| `/workspace/configs/skill_tree.json` | 技能树 |
| `/workspace/configs/puzzles.json` | 预制残局 |
| `/workspace/configs/formations.json` | 阵型定义 |
| `/workspace/samsara/` | Samsara 引擎 |
| `/workspace/hub/` | Hub 前端 |
| `/workspace/shared/` | 共享模块（Schema、JSON Patch 工具、AI 配置） |
| `/workspace/{棋类}/` | 六棋类服务 |
| `/workspace/六道轮回_整体设计书_v3.1.md` | 本文档 |
| `/workspace/关卡内容报告书.md` | 关卡详细设计 |
| `/workspace/轻RPG化剧情实现草案_v1.4.md` | 剧情设计草案 |

### 10.8 停止服务

```
Ctrl+C
```

---

## 附录 A：版本历史

| 版本 | 日期 | 关键变更 |
|:----:|:-----|:---------|
| v1.0 | 2026-07 | 资源模型（业力=消耗），识破重构，单次上限 80 |
| v2.0 | 2026-07-27 | 业障模型（业力=业障），初始 50/阈值 120/单次 120，章节溢出叠加，删除保险技能，识破公式 C×O^1.5 |
| v3.0 | 2026-07-28 | **整合轻 RPG 叙事层**（六道即人格六面）、**识破后改为锁死业力与识破概率**、**新增天道 Boss 战**（传统象棋/禁作弊/士变车/无限重试）、**5 种结局**（新增识破结局）、**6 个记忆碎片**、Hub 新增记忆与结局入口、StoryManager 前端模块 |
| v3.1 | 2026-07-31 | **资产与前端升级同步**：`configs/story.json` v1.4（`_meta` / `tiandao.boss_dialogues` / 资产映射字段）；立绘 1024×1024 + 陈默 CANON + CSS transform 微幅动画（60fps，替代旧 48 帧 12FPS 切换）；11 个 CG 改 Seedance 文生视频；BGM 8 首清单；UI 6 张 AI 像素画 JPG；新增 `cutout_rembg.py`（rembg 抠图 + alpha 二值化修复半透明）与 `generate_cg_videos.py`（Seedance 视频）；对话框 75% 透明 + 贴下沿 + 200px + 立绘 82vh |

---

## 附录 B：术语表

| 术语 | 含义 |
|:-----|:-----|
| 业力（Karma） | 玩家作弊产生的业障值，单局变量 |
| 业障模型 | 作弊增加业力，下棋消业减少业力的模型 |
| 安全阈值 | 业力 ≤ 120 时安全，超出后非线性增加识破 |
| 单次上限 | 单次作弊业力增量 ≤ 120，超出则拦截 |
| 溢出叠加 | 关卡结束时 `max(0, 业力-阈值)` 叠加给本道下一关 |
| 章节 | 六道轮回中的一个"道"，章节内溢出叠加，换道清零 |
| 识破概率 | 道级全局变量，跨关卡累计，达 100% 必被识破 |
| 透支 | 业力超出安全阈值的行为 |
| 消业 | 下棋事件减少业力 |
| 增业 | 作弊增加业力 |
| 灵活编码 | AI 通过 JSON Patch 修改游戏配置的机制 |
| 守道者 | 每道最后一关的 Boss，对应主角的人格侧面 |
| 沙盒 | 通关整道后解锁的自由对弈模式 |
| 记忆碎片 | 每道全程无作弊通关解锁的剧情 CG，揭示主角人格成因 |
| 悟道值 / 堕落值 | 跨关卡人格变量，由关键选择累计，影响结局 |
| 识破结局 | 新增：玩家被识破后业力锁死，通关六道触发天道 Boss 战 |
| 天道 Boss 战 | 新增：传统象棋、禁作弊、士变车、无限重试的终局挑战 |

---

## 附录 C：设计原则

1. **业障模型优先**：作弊产生业障，下棋消业。避免"资源管理"式的功利计算。
2. **章节叠加**：迫使玩家在整道内控制作弊节奏，而非单关豪赌。
3. **非线性识破**：小透支可控，大透支必死。鼓励"小步透支"而非"豪赌一次"。
4. **技能持久**：轮回不重置技能树，给玩家长期成长感。
5. **灵活编码**：AI 修改配置而非生成代码，安全可控。
6. **棋类独立**：每棋类独立进程，互不干扰，便于扩展。
7. **预制关卡**：保证游戏体验稳定性，避免纯随机带来的失衡。
8. **多 build 支持**：4 分支技能树支持业力流、隐匿流、作弊流、悟道流等多种玩法。
9. **叙事玩法融合**：六道即人格六面，守道者即主角的投影；悟道/堕落值是跨关卡人格变量，与单局业力独立。
10. **识破即救赎**：被识破不再粗暴重置，而是触发"识破结局"——业力锁死反而使后续通关变易，但天道 Boss 战是终极考验。给"曾经作弊但愿意面对"的玩家提供救赎路径。

---

*本文档基于 v3.1 设计撰写，整合 v2.0 业障模型与 v1.4 轻 RPG 叙事层（剧情权威源 `configs/story.json` v1.4），新增识破结局与天道 Boss 战，并同步资产与前端升级（立绘 1024+CANON+动画、CG Seedance 视频、BGM 8 首、UI 6 JPG、rembg 抠图）。如发现与代码差异，以代码为准。*
*v3.1 · 2026 年*
