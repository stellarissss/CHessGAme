# 无限制黑白棋 — 前端剩余实施计划（续）

> **背景**：后端（6 Python + 8 config + 共享层 + 启动器）已完成。前端 `app.js` 的 todos 1-3（类名/HTML 模板/CSS 样式）已完成。本计划覆盖**剩余的 todos 4-9**：renderBoard/renderPieces 重写、交互层重写、冒烟测试、README、集成验证。
> **设计哲学**：灵活编码（最高纲领）— 一切新功能由 AI 编码实现，不可硬编码。
> **前端一致性**：复刻象棋的 Shadow DOM Web Component + Playfair/DM Sans/JetBrains 极简美学，仅适配黑白棋落子语义。

---

## 一、Summary 概要

完成 `/workspace/heibaiqi/static/app.js` 的核心改造（renderBoard/renderPieces/交互层），使黑白棋前端可玩：8×8 绿盘 + 单元格中心实心圆 disc + 点合法空格直接落子 + 翻转动画 + AI 走棋。然后跑后端/前端冒烟、更新 README、集成验证。

---

## 二、Current State Analysis 现状分析

### 2.1 已完成（Phase 1 实读确认，无需再动）

- **类与注册**：`HeibaiqiBoard` / `customElements.define('heibaiqi-board',…)` / `playerSide` 默认 `'black'`（app.js L1, L50）
- **HTML 模板**：标题"无限制黑白棋"、回合"黑方回合"、难度 `easy/normal/hard/master`、hints/placeholder 黑白棋语境（L94-258）
- **CSS _getStyles**：
  - `--board-bg: #1a5d3a`、`--board-line: #000000`（L288-289）
  - `#board-container` 1:1 正方形 `width/height: min(90vmin,600px)`（L467-477）
  - `.piece.black` 实心黑圆（L787-794）、`.piece.white` 实心白圆带黑边（L796-803）
  - `.valid-move-indicator` 金色半透明圆点 + hover scale 1.35（L826-844）
  - `@keyframes pieceFlip` rotateY 0→180° + `.piece.flipping` 0.45s（L846-854）
  - `h1::before content: '黑 白 棋'`（L370-371）

### 2.2 待完成（本计划范围）

| 模块 | 当前状态（象棋残留） | 行号 |
|---|---|---|
| `_getBoardLayoutConfig` defaults | river_gap:true / palace.enabled:true / river.enabled:true / #f0d9b5 / viewbox_padding 0.444 | L2350-2423 |
| `renderBoard` | 含 river_gap 分段、palace 对角线、river-text；viewBox 用 `(width-1)+padding`（交叉点模型） | L2425-2634 |
| `renderPieces`/`createPieceElement` | 创建 `.piece-text` span；交叉点定位 `5+(x/(width-1))*90`；按 `red`/`black` 上色；点击调 `onPieceClick` | L2659-2730 |
| `onPieceClick` | 选子语义；fetch valid_moves 用 `{piece_id, to}`（错） | L2732-2773 |
| `showValidMoves` | 交叉点定位；点击调 `executeMove(pieceId,[x,y])` | L2783-2818 |
| `executeMove` | fetch /api/move 用 `{piece_id, to}`（错）；无翻转动画 | L2833-2884 |
| `makeAIMove` | 用 `data.ai_move.piece_id`/`captured`（错）；无翻转动画 | L2886-2939 |
| `_isCurrentTurn*` | 默认 `'red'`（应 `'black'`） | L2982,2990,2996 |
| `init` | 未调 `renderValidPlacements()` | L2312-2327 |
| 缺失函数 | 无 `renderValidPlacements()`、无 `placeStone(x,y)` | — |

### 2.3 API 契约（Phase 1 实读 main.py 确认）

| 端点 | 请求 | 响应 |
|---|---|---|
| `POST /api/valid_moves` | `{side:"black"\|"white"}` | `{success, moves:[[x,y],...], side}` (L581-587) |
| `POST /api/move` | `{to:[x,y]}`（**无 piece_id**） | `{success, board_state, flipped:[id...], mechanisms}` (L424-429) |
| `POST /api/ai_move` | `{}` | `{success, board_state, ai_move:{to,flipped:[id...],side}, is_random, mechanisms}` (L527-530) |
| `POST /api/undo` | `{}` | `{success, board_state, message}` (L628) |

### 2.4 数据结构（Phase 1 实读 configs 确认）

- `board.json.geometry`: `{width:8, height:8, river_line:null, palace:null, regions:{...}}`
- `board.json.appearance`: `{background_color:"#1a5d3a", line_color:"#000000", ...}`
- `board_state.json`: 4 初始 disc（white_disc_1@(3,3), black_disc_1@(4,3), black_disc_2@(3,4), white_disc_2@(4,4)），`current_turn:"black"`，`pieces[].side` ∈ `black|white`，`pieces[].position:[x,y]`
- `rules.json.ai_difficulty.levels`: easy/normal/hard/master
- `ui_config.json.theme.pieces`: black #000000 / white #ffffff / highlight #ffd700

### 2.5 坐标模型差异（核心）

| | 象棋（交叉点） | 黑白棋（单元格中心） |
|---|---|---|
| width 含义 | 9 个交叉点 | 8 个单元格 |
| 线条数 | 9 竖+10 横（含河界分段） | 9 竖+9 横（落格边界） |
| viewBox | `-(pad) -(pad) (width-1+2pad) (height-1+2pad)` | `0 0 width height`（即 `0 0 8 8`） |
| 落子定位 | `5+(x/(width-1))*90` % | `((x+0.5)/width)*100` % |

---

## 三、Proposed Changes 拟议变更

### 3.1 改造 `_getBoardLayoutConfig()` defaults（L2350-2423）

**What**：把 defaults 对象改为 Othello 配置。

**How**：替换 defaults 为：
```js
const defaults = {
    grid: { line_thickness: 0.03, show_horizontal: true, show_vertical: true, river_gap: false },
    palace: { enabled: false, show_diagonals: false, line_thickness: 0.03 },
    river: { enabled: false, text: '', text_size: 0.5, gap_ratio: 1.0 },
    appearance: { background_color: '#1a5d3a', line_color: '#000000', palace_line_color: null },
    layout: { viewbox_padding_left: 0, viewbox_padding_right: 0, viewbox_padding_top: 0, viewbox_padding_bottom: 0, board_size: '90vmin' },
    decorations: { border: { enabled: true, thickness: 0.06, color: '#000000' }, custom_lines: [], background_pattern: null }
};
```
保留 merge 逻辑与 try/catch 不变。

**Why**：黑白棋无 river/palace；绿底黑线；单元格网格无需 padding（viewBox 直接 `0 0 8 8`）；边框启用强化棋盘边界。

### 3.2 重写 `renderBoard()`（L2425-2634）

**What**：删除 river_gap 分段、palace 对角线、river-text 创建；改为 8×8 单元格网格（9 竖+9 横线），viewBox `0 0 width height`。

**How**：替换整个函数体为：
```js
renderBoard() {
    const container = this.shadowRoot.getElementById('board-container');
    container.innerHTML = '';

    const layoutConfig = this._getBoardLayoutConfig();
    const geometry = this.configs.board?.geometry || {};
    const width = geometry.width || 8;
    const height = geometry.height || 8;

    const bgColor = layoutConfig.appearance.background_color;
    container.style.backgroundColor = bgColor;
    this.style.setProperty('--board-bg', bgColor);

    const svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
    svg.classList.add('board-grid');
    // 单元格模型：viewBox 0 0 width height，线条落格边界
    svg.setAttribute('viewBox', `0 0 ${width} ${height}`);
    svg.setAttribute('preserveAspectRatio', 'none');
    svg.style.width = '100%';
    svg.style.height = '100%';

    const lineColor = layoutConfig.appearance.line_color;
    const sw = String(layoutConfig.grid.line_thickness);

    // 9 条竖线 x=0..width
    if (layoutConfig.grid.show_vertical) {
        for (let i = 0; i <= width; i++) {
            const line = document.createElementNS('http://www.w3.org/2000/svg', 'line');
            line.setAttribute('x1', i);
            line.setAttribute('y1', 0);
            line.setAttribute('x2', i);
            line.setAttribute('y2', height);
            line.setAttribute('stroke', lineColor);
            line.setAttribute('stroke-width', sw);
            svg.appendChild(line);
        }
    }
    // 9 条横线 y=0..height
    if (layoutConfig.grid.show_horizontal) {
        for (let i = 0; i <= height; i++) {
            const line = document.createElementNS('http://www.w3.org/2000/svg', 'line');
            line.setAttribute('x1', 0);
            line.setAttribute('y1', i);
            line.setAttribute('x2', width);
            line.setAttribute('y2', i);
            line.setAttribute('stroke', lineColor);
            line.setAttribute('stroke-width', sw);
            svg.appendChild(line);
        }
    }

    // 边框
    if (layoutConfig.decorations.border.enabled) {
        const borderThickness = layoutConfig.decorations.border.thickness;
        const borderColor = layoutConfig.decorations.border.color || lineColor;
        const borderRect = document.createElementNS('http://www.w3.org/2000/svg', 'rect');
        const halfThick = borderThickness / 2;
        borderRect.setAttribute('x', -halfThick);
        borderRect.setAttribute('y', -halfThick);
        borderRect.setAttribute('width', width + borderThickness);
        borderRect.setAttribute('height', height + borderThickness);
        borderRect.setAttribute('fill', 'none');
        borderRect.setAttribute('stroke', borderColor);
        borderRect.setAttribute('stroke-width', String(borderThickness));
        svg.appendChild(borderRect);
    }

    container.appendChild(svg);
    this.applyUiConfig();
}
```

**Why**：单元格模型 viewBox `0 0 8 8`，9 竖+9 横线落格边界；删除 river/palace/orientation 分支（黑白棋无方向性）。border 用 `width+thickness`（非 `width-1`）适配单元格模型。

### 3.3 重写 `renderPieces()` + `createPieceElement()`（L2659-2730）

**What**：删除 `.piece-text` span；单元格中心定位；按 `black`/`white` 上色；点击改为空操作（仅 coordInsertMode 分支保留）。

**How**：

`renderPieces()`（L2659-2676）— 保留结构，删 `lastMove` 高亮中对 `piece_id` 的依赖改为按 `lastMove.to` 定位：
```js
renderPieces() {
    const container = this.shadowRoot.getElementById('board-container');
    container.querySelectorAll('.piece').forEach(el => el.remove());
    const piecesTheme = this.uiConfig?.theme?.pieces || {};
    const pieces = this.boardState?.pieces || [];
    pieces.forEach(piece => {
        if (!piece.is_alive) return;
        this.createPieceElement(piece, piecesTheme);
    });
    // 高亮上一步落子点
    if (this.lastMove?.to) {
        const el = container.querySelector(`[data-pos="${this.lastMove.to[0]},${this.lastMove.to[1]}"]`);
        if (el) el.classList.add('last-moved');
    }
}
```

`createPieceElement()`（L2678-2730）— 替换为：
```js
createPieceElement(piece, theme) {
    const container = this.shadowRoot.getElementById('board-container');
    const el = document.createElement('div');
    el.className = `piece ${piece.side}`;
    el.dataset.pieceId = piece.id;
    el.dataset.pos = `${piece.position[0]},${piece.position[1]}`;

    const geometry = this.configs.board?.geometry || {};
    const width = geometry.width || 8;
    const height = geometry.height || 8;

    const [x, y] = piece.position;
    // 单元格中心定位
    const leftPct = ((x + 0.5) / width) * 100;
    const topPct = ((y + 0.5) / height) * 100;
    el.style.left = `${leftPct}%`;
    el.style.top = `${topPct}%`;
    el.style.transform = 'translate(-50%, -50%)';

    // 按 side 上色（支持 ui_config 与 custom_properties 覆盖）
    if (piece.side === 'black') {
        el.style.background = theme.black || '#000000';
        el.style.borderColor = theme.black || '#000000';
    } else {
        el.style.background = theme.white || '#ffffff';
        el.style.borderColor = '#1a1a1a';
    }
    if (piece.custom_properties) {
        const cp = piece.custom_properties;
        if (cp.color) el.style.background = cp.color;
        if (cp.bg) el.style.background = cp.bg;
    }

    // 仅 coordInsertMode 下可点（选坐标）；正常落子由 valid-placement 指示器处理
    el.addEventListener('click', (e) => {
        e.stopPropagation();
        if (this.coordInsertMode) {
            this.onCoordDotClick(x, y);
        }
        // 非选坐标模式：点击已有棋子无操作（黑白棋点空格落子）
    });

    container.appendChild(el);
}
```

**Why**：disc 无文字（删 `.piece-text`）；单元格中心 `((x+0.5)/width)*100`；点击已有棋子无操作（黑白棋语义）。

### 3.4 交互层重写（核心差异）

**What**：删除选子语义；新增 `renderValidPlacements()` + `placeStone(x,y)`；重写 `executeMove`→`placeStone`；重写 `makeAIMove` 处理翻转动画；修 `_isCurrentTurn*` 默认；`init` 调 `renderValidPlacements`。

#### 3.4.1 删除/简化选子函数（L2732-2831）

把 `onPieceClick`、`highlightPiece`、`clearSelection` 替换为空操作或删除调用点。具体：
- `onPieceClick`（L2732-2773）：整个函数体改为 `return;`（空操作，保留函数名避免外部调用报错）
- `highlightPiece`（L2775-2781）：改为空函数体
- `clearSelection`（L2824-2831）：改为空函数体（保留以兼容 sendCommand 等调用点）
- `showValidMoves`（L2783-2818）：删除（被 `renderValidPlacements` 取代）；或改为空函数

#### 3.4.2 新增 `renderValidPlacements()`

在 `clearValidMoves`（L2820）后新增：
```js
async renderValidPlacements() {
    this.clearValidMoves();
    if (this.aiThinking) return;
    if (this.boardState?.game_status?.state === 'ended') return;
    const side = this.boardState?.current_turn || 'black';
    try {
        const resp = await fetch(`${this.apiBase}/api/valid_moves`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ side })
        });
        const data = await resp.json();
        this.validMoves = data.moves || [];
        const container = this.shadowRoot.getElementById('board-container');
        const geometry = this.configs.board?.geometry || {};
        const width = geometry.width || 8;
        const height = geometry.height || 8;
        this.validMoves.forEach(([x, y]) => {
            const indicator = document.createElement('div');
            indicator.className = 'valid-move-indicator';
            indicator.dataset.pos = `${x},${y}`;
            indicator.style.left = `${((x + 0.5) / width) * 100}%`;
            indicator.style.top = `${((y + 0.5) / height) * 100}%`;
            indicator.addEventListener('click', (e) => {
                e.stopPropagation();
                if (this.coordInsertMode) {
                    this.onCoordDotClick(x, y);
                    return;
                }
                this.placeStone(x, y);
            });
            container.appendChild(indicator);
        });
    } catch (e) {
        console.error('获取合法落子点失败:', e);
    }
}
```

#### 3.4.3 新增 `placeStone(x,y)`（替换 `executeMove`，L2833-2884）

```js
async placeStone(x, y) {
    if (this.aiThinking) return;
    if (this.boardState?.game_status?.state === 'ended') return;
    if (!this._isCurrentTurnPlayerControlled()) return;

    this.clearValidMoves();
    this.aiThinking = true;

    try {
        const resp = await fetch(`${this.apiBase}/api/move`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ to: [x, y] })
        });
        const data = await resp.json();

        if (data.success) {
            this.boardState = data.board_state;
            this.lastMove = this.boardState.move_history?.slice(-1)[0] || null;
            this.renderPieces();
            // 翻转动画：对 flipped 中的 disc 播放 pieceFlip
            await this._animateFlips(data.flipped || []);
            this.updateTurnIndicator();
            this.updateActiveRules();
            this.updateGameObjectives();
            this.updateMechanisms();
            this.loadTokenStats();
            this._dispatchMoveEvent();

            if (this.boardState.game_status.state === 'ended') {
                this.showGameOver();
                this._dispatchGameEndEvent();
                this.aiThinking = false;
                return;
            }
            await this.renderValidPlacements();
            await this.sleep(800);
            if (this._isCurrentTurnAITurn()) {
                await this.makeAIMove();
            }
        } else {
            this.addMessage(data.message || '落子失败', 'error');
            await this.renderValidPlacements();
        }
    } catch (e) {
        this.addMessage(`网络错误: ${e.message}`, 'error');
        this._dispatchError('落子失败', e);
        await this.renderValidPlacements();
    }
    this.aiThinking = false;
}
```

#### 3.4.4 新增 `_animateFlips(flippedIds)` 辅助

```js
_animateFlips(flippedIds) {
    return new Promise(resolve => {
        if (!flippedIds || flippedIds.length === 0) { resolve(); return; }
        let pending = flippedIds.length;
        flippedIds.forEach(id => {
            const el = this.shadowRoot.querySelector(`[data-piece-id="${id}"]`);
            if (el) {
                el.classList.add('flipping');
                const onEnd = () => {
                    el.classList.remove('flipping');
                    el.removeEventListener('animationend', onEnd);
                    pending--;
                    if (pending <= 0) resolve();
                };
                el.addEventListener('animationend', onEnd);
                // 兜底：0.5s 后强制结束
                setTimeout(() => { if (pending > 0) { el.classList.remove('flipping'); pending--; if (pending<=0) resolve(); } }, 500);
            } else {
                pending--;
                if (pending <= 0) resolve();
            }
        });
    });
}
```

#### 3.4.5 重写 `makeAIMove()`（L2886-2939）

```js
async makeAIMove(depth = 0) {
    if (depth > 10) return;
    this.addMessage('AI思考中...', 'info');

    try {
        const resp = await fetch(`${this.apiBase}/api/ai_move`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({})
        });
        const data = await resp.json();

        if (data.success) {
            this.boardState = data.board_state;
            this.lastMove = data.ai_move || null;
            this.renderPieces();
            await this._animateFlips(data.ai_move?.flipped || []);
            // 高亮 AI 落子点
            if (data.ai_move?.to) {
                const el = this.shadowRoot.querySelector(`[data-pos="${data.ai_move.to[0]},${data.ai_move.to[1]}"]`);
                if (el) { this.shadowRoot.querySelectorAll('.piece').forEach(p=>p.classList.remove('ai-moved')); el.classList.add('ai-moved'); }
            }
            this.updateTurnIndicator();
            this.updateActiveRules();
            this.updateGameObjectives();
            this.updateMechanisms();

            const messages = this.shadowRoot.getElementById('ai-messages');
            const lastMsg = messages.lastElementChild;
            if (lastMsg && lastMsg.textContent.includes('思考中')) lastMsg.remove();
            this.addMessage(`AI落子于 (${data.ai_move.to[0]},${data.ai_move.to[1]})，翻转 ${data.ai_move.flipped?.length||0} 子`, 'info');

            this._dispatchMoveEvent();

            if (this.boardState.game_status.state === 'ended') {
                this.showGameOver();
                this._dispatchGameEndEvent();
                return;
            }
            await this.renderValidPlacements();
            if (this._isCurrentTurnAITurn()) {
                await this.sleep(600);
                await this.makeAIMove(depth + 1);
            }
        } else {
            this.addMessage(data.message || 'AI移动失败', 'error');
        }
    } catch (e) {
        this.addMessage(`AI错误: ${e.message}`, 'error');
        this._dispatchError('AI移动失败', e);
    }
}
```

#### 3.4.6 修 `_isCurrentTurn*` 默认值（L2982, 2990, 2996）

3 处 `|| 'red'` 改为 `|| 'black'`。

#### 3.4.7 `init()` 调整（L2312-2327）

在 `this.renderPieces();` 后、`this.updateTurnIndicator();` 后新增 `await this.renderValidPlacements();`：
```js
async init() {
    if (this._initialized) return;
    await this.loadConfigs();
    this.renderBoard();
    this.renderPieces();
    this.updateTurnIndicator();
    await this.renderValidPlacements();
    this.updateActiveRules();
    this.updateGameObjectives();
    this.updateAIPersonality();
    this.updateMechanisms();
    this.bindEvents();
    this.checkApiKey();
    this.loadTokenStats();
    this._initialized = true;
    this.dispatchEvent(new CustomEvent('ready', { bubbles: true, composed: true }));
}
```

#### 3.4.8 `sendCommand` 配置应用后刷新合法点（L3023+，约 L3048-3055）

在 `sendCommand` 中 `this.renderBoard(); this.renderPieces();` 后追加 `await this.renderValidPlacements();`（2 处：applied 分支 + refresh_page 之外的刷新）。

#### 3.4.9 `updateTurnIndicator()` 文案

确认按 `current_turn` 动态显示"黑方回合"/"白方回合"（原象棋可能写死"红方"）。需读该函数确认，若写死则改为动态：`黑方回合` if black else `白方回合`。

### 3.5 跳过 `static/style.css`

不创建（象棋自身不加载，heibaiqi index.html 也不引用）。

### 3.6 更新 `/workspace/README.md`

**What**：增补黑白棋条目：
- 项目简介：补充"黑白棋"
- 访问地址：新增 `- **黑白棋**: http://localhost:8003/`
- 项目结构：新增 `heibaiqi/` 目录块
- 游戏玩法：新增"### 无限制黑白棋"小节（flip 原语 / AI 自由修改 / 多子者胜）

**Why**：统一启动器已含 8003，README 需同步。

---

## 四、Assumptions & Decisions 假设与决策

| # | 决策项 | 选择 | 理由 |
|---|---|---|---|
| D1 | viewBox 模型 | `0 0 width height`（单元格） | 黑白棋 disc 落格内，线条落格边界 |
| D2 | 落子定位 | `((x+0.5)/width)*100`% | 单元格中心 |
| D3 | 交互模型 | 点合法空格直接落子，无选子 | 黑白棋无棋子选择 |
| D4 | 翻转动画 | `pieceFlip` rotateY 0→180°，0.45s，对 flipped 数组逐个播放 | CSS 3D 翻转，视觉直观 |
| D5 | 合法点指示器 | 金色圆点 `.valid-move-indicator`（CSS 已就绪） | 复用现有样式与定位逻辑 |
| D6 | `_isCurrentTurn*` 默认 | `'black'` | 黑白棋先手为黑 |
| D7 | 删除选子函数 | 改空函数体（保留签名） | 避免外部调用报错，最小改动 |
| D8 | lastMove 高亮 | 按 `lastMove.to` 用 `data-pos` 定位 | 黑白棋 move_history 项含 `to`/`flipped`/`placed_disc_id` |

**假设**：浏览器支持 Web Components + Shadow DOM + CSS 3D；后端 8003 可启动（已验证 main.py 语法）。

---

## 五、Verification Steps 验证步骤

### 5.1 后端冒烟
1. `cd /workspace/heibaiqi && timeout 5 python -c "import main; print('OK')"` → 无报错
2. 启动 `python main.py`（后台）→ 8003 监听
3. `curl -s http://localhost:8003/api/config/all` → 含 6 配置
4. `curl -s -X POST http://localhost:8003/api/valid_moves -H "Content-Type: application/json" -d '{"side":"black"}'` → moves 含 4 初始合法点
5. `curl -s -X POST http://localhost:8003/api/move -H "Content-Type: application/json" -d '{"to":[2,3]}'` → success:true，flipped 含 1 id

### 5.2 前端冒烟
6. 访问 `http://localhost:8003/` → 无 console 报错；8×8 绿盘 + 中心 4 子 + 4 金色合法点
7. 点合法点 → 黑棋落下 + 翻转动画 + 回合切白 + 合法点刷新
8. 触发 AI 走棋 → AI 落子 + 翻转动画
9. 模态框：设置/日志可弹；悔棋/撤回/重启/重置可用

### 5.3 集成
10. `cd /workspace && python main.py` → 4 服务（8000-8003）启动
11. README 含黑白棋 + 端口 8003

### 5.4 完成标准
- [ ] app.js 改造完成，浏览器无报错，落子/翻转/AI/指令通
- [ ] README 更新
- [ ] 5.1/5.2/5.3 通过
- [ ] frontend-design 审查（保持象棋 Playfair/DM Sans 极简美学 + 绿色 Othello 棋盘）

---

## 六、实施顺序（TodoList）

1. 改造 `_getBoardLayoutConfig` defaults + 重写 `renderBoard`（3.1, 3.2）
2. 重写 `renderPieces`/`createPieceElement`（3.3）
3. 交互层重写：删选子 + 新增 renderValidPlacements/placeStone/_animateFlips + 重写 makeAIMove + 修 _isCurrentTurn* + init（3.4）
4. sendCommand 刷新合法点 + updateTurnIndicator 文案核查（3.4.8, 3.4.9）
5. 启动 8003 跑 5.1 后端冒烟（curl）
6. 前端冒烟 5.2（页面/落子/翻转/AI/模态框）
7. 更新 /workspace/README.md（3.6）
8. 集成验证 5.3 + frontend-design 审查
